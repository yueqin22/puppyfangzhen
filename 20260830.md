# Puppy 机器人 + MiniCPM-RobotTrack 仿真运行全流程操作指南

> **编制日期**：2026-08-30  
> **文档版本**：v1.0.0  
> **面向对象**：机器人算法工程师、ROS 2 开发人员、测试工程师  
> **涵盖环境**：Windows 11 本地仿真、WSL 2 (Ubuntu 22.04) + ROS 2 Humble + Gazebo 11 / RViz 3D 物理仿真、CoppeliaSim ZMQ 仿真

---

## 目录

- [1. 仿真架构与数据流](#1-仿真架构与数据流)
- [2. 环境依赖与前置检查](#2-环境依赖与前置检查)
- [3. 运行方式一：Windows 本地快速独立闭环仿真（免 WSL，即开即测）](#3-运行方式一windows-本地快速独立闭环仿真免-wsl即开即测)
- [4. 运行方式二：WSL 2 + ROS 2 Humble + Gazebo 3D 物理仿真（完整闭环）](#4-运行方式二wsl-2--ros-2-humble--gazebo-3d-物理仿真完整闭环)
  - [4.1 快捷方式：Windows 桌面一键双击启动](#41-快捷方式windows-桌面一键双击启动)
  - [4.2 标准方式：WSL 终端分步手动启动与交互](#42-标准方式wsl-终端分步手动启动与交互)
- [5. 运行方式三：CoppeliaSim ZMQ 轻量级物理仿真](#5-运行方式三coppeliasim-zmq-轻量级物理仿真)
- [6. 核心 ROS 2 话题与配置文件速查](#6-核心-ros-2-话题与配置文件速查)
- [7. 常见问题排查与 FAQ](#7-常见问题排查与-faq)

---

## 1. 仿真架构与数据流

本系统实现了“**自然语言任务解析 ➔ 视觉大模型目标跟踪 ➔ 多层安全仲裁与限速 ➔ 底层运动控制/仿真**”的完整闭环：

```mermaid
flowchart TD
    subgraph NaturalLanguage [任务层]
        NL[自然语言指令\n例: '巡视后院并检查是否有可疑物品'] --> MG[mission_grounder_node\n多阶段任务状态机: 导航至区域 ➔ 视觉跟踪 ➔ 检查 ➔ 返航]
    end

    subgraph Perception [感知与图像层]
        GazeboCam[Gazebo 仿真 RGB-D 相机\n/camera/color/image_raw] --> VB[vision_bridge_node\n图像统一裁剪 384x384 / 10 FPS / 时间戳同步]
        RealCam[Go2 VideoClient / RealSense] --> VB
        VB --> TN[minicpm_track_node\nMiniCPM-RobotTrack 推理客户端]
    end

    subgraph Decision [大模型决策与安全层]
        MG --> |跟踪目标提示词| TN
        TN --> |TrackIntent\n3D Waypoint / 航向建议 / 置信度| TCA[track_cmd_adapter_node\n限速 / EMA平滑 / 350ms超时停车]
        LiDAR[/scan 2D 激光雷达点云] --> TCA
    end

    subgraph Control [底层执行与仿真]
        TCA --> |/cmd_vel_safe\nvx<=0.15m/s, wz<=0.30rad/s| MotAdp[puppypi_adapter / motion_adapter_node]
        MotAdp --> |/cmd_vel| SimWorld[Gazebo 3D 物理引擎 / CoppeliaSim / 实机]
    end
```

---

## 2. 环境依赖与前置检查

### 2.1 硬件要求
- **CPU**：4 核及以上 x86_64 处理器
- **内存**：建议 8GB 及以上（Gazebo 运行推荐 16GB）
- **显卡**：NVIDIA 独立显卡（GTX 1650 / RTX 系列及以上）或集成显卡（支持 WSLg 硬件加速）

### 2.2 软件要求
- **宿主机系统**：Windows 10 / 11（启用 WSL 2）
- **WSL 子系统**：Ubuntu 22.04 LTS
- **ROS 2 版本**：ROS 2 Humble Hawksbill (`ros-humble-desktop`)
- **仿真器**：Gazebo Classic 11 (`gazebo11`, `ros-humble-gazebo-ros-pkgs`)
- **Python 环境**：Python 3.10+ / 3.12 (含 `numpy`, `pytest`, `pyyaml`, `opencv-python`)

### 2.3 WSL 环境一键依赖安装（仅需首次执行）
在 WSL Ubuntu 终端中执行：
```bash
sudo apt update && sudo apt install -y \
  ros-humble-desktop \
  ros-humble-gazebo-ros-pkgs \
  ros-humble-navigation2 \
  ros-humble-nav2-bringup \
  ros-humble-slam-toolbox \
  ros-humble-xacro \
  ros-humble-tf-transformations \
  python3-pip python3-colcon-common-extensions

pip install numpy pyyaml opencv-python pytest
```

---

## 3. 运行方式一：Windows 本地快速独立闭环仿真（免 WSL，即开即测）

该方式**无需进入 WSL 和启动重型 3D 渲染**，适合快速验证自然语言解析、视觉跟踪算法逻辑、安全限速断流与 4 大场景性能评测。

### 3.1 运行 4 大核心场景自动化基准评测
在 Windows PowerShell 终端中进入项目目录并执行：
```powershell
cd E:\puppyfangzhen
python src/puppy_minicpm_robot/scripts/benchmark_suite.py --trials 10
```
**测试包含以下场景：**
1. **`open_area`（开阔区域）**：评测目标在宽阔视野下的稳定跟踪与 P95 推理延迟；
2. **`narrow_corridor`（窄通道）**：评测侧方转弯时的角速度平滑与航向对齐；
3. **`dynamic_obstacle`（动态障碍物）**：模拟突发障碍物（<0.35m）触发激光雷达安全截断；
4. **`target_occlusion`（目标遮挡/丢失）**：评测置信度衰减时的自动安全驻留停车。

**产出报告：**
- Markdown 简报：[`logs/minicpm_benchmark_report.md`](file:///e:/puppyfangzhen/logs/minicpm_benchmark_report.md)
- JSON 详细数据：[`logs/minicpm_benchmark_report.json`](file:///e:/puppyfangzhen/logs/minicpm_benchmark_report.json)

---

### 3.2 运行单帧自然语言推断验收（阶段 A 出口）
测试单帧图像输入与自然语言指令的意图计算：
```powershell
python src/puppy_minicpm_robot/scripts/offline_inference_demo.py --instruction "巡视后院并跟随前方的蓝色目标" --output logs/offline_inference_result.json
```
**查看输出结果：**
- 控制台将打印生成的 3D Waypoint `(dx, dy, dz)`、建议航向速度 `(vx, wz)` 以及模型耗时；
- 结构化数据保存至 [`logs/offline_inference_result.json`](file:///e:/puppyfangzhen/logs/offline_inference_result.json)。

---

### 3.3 运行相机视频流与数据集动态回放模拟（阶段 B）
以 10 FPS 模拟前置 RGB 摄像头连续输出图像帧并进行时间戳同步：
```powershell
python src/puppy_minicpm_robot/scripts/dataset_player.py --fps 10 --count 30
```

---

### 3.4 运行硬件与软件环境预检诊断（阶段 F）
```powershell
python src/puppy_minicpm_robot/scripts/preflight_check.py
```
自动检查当前主机的 Python 版本、CUDA/PyTorch 加速、TensorRT、CycloneDDS、网络端口与模型权重状态。

---

### 3.5 运行全量自动化测试套件
```powershell
python -m pytest src/puppy_minicpm_robot/test/ -v
```
> 应输出 `25 passed`，确保所有单元测试和集成逻辑 100% 正常。

---

## 4. 运行方式二：WSL 2 + ROS 2 Humble + Gazebo 3D 物理仿真（完整闭环）

该方式在 Gazebo 3D 物理世界中完整渲染四足机器人模型、激光雷达传感器、前置 RGB-D 摄像头及室内家具环境。

---

### 4.1 快捷方式：Windows 桌面一键双击启动

在 Windows 资源管理器打开 [`E:\puppyfangzhen`](file:///e:/puppyfangzhen)：

1. **第一步（启动 3D 仿真世界）**：
   - 双击运行 👉 [`start_3d_simulation.bat`](file:///e:/puppyfangzhen/start_3d_simulation.bat)
   - 脚本将自动同步源码至 WSL `~/puppy_ws`、执行 `colcon build`，并在 Windows 桌面弹出 **Gazebo (Ubuntu-22.04)** 物理窗口与 **RViz2** 可视化窗口。

2. **第二步（启动视觉跟踪与任务系统）**：
   - 双击运行 👉 [`start_minicpm_track_wsl.bat`](file:///e:/puppyfangzhen/start_minicpm_track_wsl.bat)
   - 启动视觉流桥接、MiniCPM 跟踪节点、安全限速适配器与任务解耦状态机。

---

### 4.2 标准方式：WSL 终端分步手动启动与交互

#### 步骤 1：进入 WSL 2 并同步编译工作空间
打开 Windows 终端，执行：
```bash
wsl -d Ubuntu-22.04

# 设置路径变量
export PUPPY_SRC=/mnt/e/puppyfangzhen
export PUPPY_WS=~/puppy_ws

# 同步代码
mkdir -p "$PUPPY_WS/src"
cp -r "$PUPPY_SRC/src/"* "$PUPPY_WS/src/"

# 编译相关包
source /opt/ros/humble/setup.bash
cd "$PUPPY_WS"
colcon build --symlink-install --packages-up-to puppy_minicpm_robot puppy_bringup puppy_description

# 激活环境变量
source "$PUPPY_WS/install/setup.bash"
```

#### 步骤 2：启动 Gazebo 3D 物理世界与建图
```bash
# 启动小房间场景（可选场景：small_room 或 office）
ros2 launch puppy_bringup mapping.launch.py world:=small_room gui:=true
```
> **观察要点**：
> - 桌面将弹出 Gazebo 3D 窗口，可以看到 Puppy 四足机器人平稳站立在地面上；
> - RViz2 窗口显示激光雷达点云（`/scan`）、机器人 TF 骨骼以及实时建图。

#### 步骤 3：在新建终端中启动 MiniCPM-RobotTrack 视觉跟踪系统
打开第二个 WSL 终端窗口：
```bash
wsl -d Ubuntu-22.04
source /opt/ros/humble/setup.bash
source ~/puppy_ws/install/setup.bash

# 启动视觉跟踪与任务节点（仿真模式）
ros2 launch puppy_minicpm_robot minicpm_robot_sim.launch.py mode:=sim camera_source:=sim
```

#### 步骤 4：下发自然语言任务指令测试闭环
打开第三个 WSL 终端窗口（或使用已有终端）：
```bash
# 1. 发送自然语言巡逻与检查指令
ros2 topic pub --once /mission/command std_msgs/msg/String "{data: '巡视后院并检查是否有可疑物品'}"

# 2. 实时观察受控安全速度输出（严格限制在 0.15 m/s 与 0.30 rad/s 以内）
ros2 topic echo /cmd_vel_safe

# 3. 观察任务状态机流转 (NAVIGATE_TO_ZONE -> VISUAL_TRACKING -> INSPECT_VERIFY -> RETURN_HOME)
ros2 topic echo /minicpm_robot/mission_status

# 4. 观察视觉跟踪意图与 3D Waypoint 输出
ros2 topic echo /minicpm_robot/track_intent
```

---

## 5. 运行方式三：CoppeliaSim ZMQ 轻量级物理仿真

适用于需要极低内存占用、或使用 CoppeliaSim 进行动力学算法快速验证的场景。

1. 打开 CoppeliaSim 软件并载入场景；
2. 启动 ZMQ 桥接节点（该节点会自动发布 `/odom`、`/scan`、`/clock`、`/tf` 以及 `/camera/color/image_raw`）：
```bash
python3 src/puppy_bringup/scripts/coppelia_bridge.py
```
3. 启动 `puppy_minicpm_robot` 节点即可完成控制闭环：
```bash
ros2 launch puppy_minicpm_robot minicpm_robot_sim.launch.py mode:=sim camera_source:=sim
```

---

## 6. 核心 ROS 2 话题与配置文件速查

### 6.1 关键通信话题列表

| 话题名称 | 消息类型 | 发布节点 | 订阅节点 | 说明 |
|---|---|---|---|---|
| `/camera/color/image_raw` | `sensor_msgs/Image` | `vision_bridge_node` / Gazebo | `minicpm_track_node` | 统一标准 RGB 画面（384x384） |
| `/camera/color/camera_info` | `sensor_msgs/CameraInfo` | `vision_bridge_node` / Gazebo | 视觉下游节点 | 相机内参及标定参数 |
| `/mission/command` | `std_msgs/String` | 人机交互 / 键盘 / 脚本 | `mission_grounder_node` | 自然语言任务指令输入 |
| `/minicpm_robot/track_intent` | `std_msgs/String` (JSON) | `minicpm_track_node` | `track_cmd_adapter_node` | 3D Waypoint、航向角、置信度 |
| `/minicpm_robot/mission_status` | `std_msgs/String` (JSON) | `mission_grounder_node` | 状态监视器 | 当前任务阶段（导航/跟踪/检查/返航） |
| `/scan` | `sensor_msgs/LaserScan` | Gazebo / LiDAR | `track_cmd_adapter_node` | 2D 激光防撞覆盖数据源 |
| `/cmd_vel_safe` | `geometry_msgs/Twist` | `track_cmd_adapter_node` | `motion_adapter_node` | 限速、防撞滤波后的安全控制速度 |
| `/cmd_vel` | `geometry_msgs/Twist` | `motion_adapter_node` | Gazebo 插件 / 实机 | 最终底层执行速度 |

### 6.2 核心参数配置 ([`config/minicpm_robot.yaml`](file:///e:/puppyfangzhen/src/puppy_minicpm_robot/config/minicpm_robot.yaml))

```yaml
track_cmd_adapter_node:
  ros__parameters:
    mode: "dry-run"                  # 模式: dry-run(安全监视) / sim(仿真驱动) / live-arm(实机)
    max_vx: 0.15                     # 最大线速度限制: 0.15 m/s
    max_wz: 0.30                     # 最大角速度限制: 0.30 rad/s
    vx_positive_scale: 1.5           # 前向速度增益比例
    wz_scale: 1.2                    # 转向速度增益比例
    vx_positive_deadband: 0.03       # 速度死区抑制 (m/s)
    ema_alpha: 1.0                   # EMA 平滑滤波系数 (0.0~1.0)
    cmd_timeout_ms: 350.0            # 命令超时停车阈值 (毫秒)
    min_obstacle_distance: 0.35      # 激光雷达防撞最小安全距离 (米)
    confidence_threshold: 0.35       # 视觉目标识别置信度衰减门限
```

---

## 7. 常见问题排查与 FAQ

### Q1: 运行 `start_3d_simulation.bat` 后，桌面没有弹出 Gazebo 窗口？
**排查步骤**：
1. 确认使用的是 Windows 11 或已安装 WSLg（Windows Subsystem for Linux GUI）；
2. 在 Windows PowerShell 中运行 `wsl --update` 确保 WSL 处于最新版本；
3. 在 Windows 任务栏或 Alt+Tab 切换窗口中寻找名为 **`Gazebo (Ubuntu-22.04)`** 的窗口（WSLg 将 Linux GUI 作为独立 Windows 窗口托管）；
4. 如果使用 VcXsrv 第三方 X11 转发，请确保先双击运行 `start_vcxsrv.bat`。

---

### Q2: 为什么发送任务指令后机器人没有向前移动？
**排查步骤**：
1. **检查运行模式**：检查 `minicpm_robot.yaml` 中的 `mode` 是否为 `sim`。若处于 `dry-run` 模式，系统出于安全保护会抑制底层输出，请在 Launch 命令中指定 `mode:=sim`；
2. **检查超时停车机制**：MiniCPM-RobotTrack 设有 350ms 严格超时机制，若视觉图像断流或推理停止，机器人会立即刹车归零；
3. **检查激光雷达避障**：如果机器人正前方 `0.35m` 内存在墙体或障碍物，`/scan` 会触发主动安全截断，强制将前向线速度压为 0。

---

### Q3: 提示 `colcon: command not found` 或 ROS 2 环境变量缺失？
**解决方法**：
在 WSL 终端中执行：
```bash
source /opt/ros/humble/setup.bash
sudo apt install -y python3-colcon-common-extensions
```
建议将 `source /opt/ros/humble/setup.bash` 添加至 WSL 中的 `~/.bashrc` 文件末尾。

---

### Q4: 机器人在 Gazebo 中出现抖动或站立不稳？
**解决方法**：
1. 降低 Gazebo 物理更新步长，或修改 `src/puppy_gait/config/gait_params.yaml`；
2. 适当降低 `body_height`（降低重心）并减小 `step_length`（减小步长）。

---

### Q5: 如何结束所有仿真进程？
- **关闭图形窗口**：直接关闭 Gazebo 和 RViz 窗口；
- **终止终端进程**：在运行的终端窗口中按下 `Ctrl + C`；
- **完全重置 WSL**：若后台有残留进程，在 Windows PowerShell 中运行 `wsl --shutdown`。
