# 自动巡航与动态避障改进方案

## 1. 结论

本轮问题不是单纯“某个算法不好”，而是可视化巡航层存在三类系统性问题：

1. **可视化层没有真正的静态全局路径规划**  
   车辆主要按目标点直线行驶，遇到餐桌、厨房门口、储物间货架等静态障碍时，只靠局部方向补救，容易磨蹭、漂移或超时跳点。

2. **动态避障之前缺少最后安全过滤**  
   VO/APF/ORCA 能给出避障速度，但在边界情况下仍可能出现 0.53m 到 0.55m 的擦碰。碰撞半径为 0.55m，因此这些属于“擦边碰撞”。

3. **目标点设置过于贴近家具或狭窄区域**  
   厨房、储物间、餐厅附近的目标点对可视化 demo 来说太深入房间内部，导致机器人为了贴近目标点而反复调整。

经过本轮修复后，最终 1 小时等效复测结果为：

```text
仿真帧数: 108000
等效仿真时间: 3600s
完成轮次: 6
碰撞次数: 0
碰撞事件: []
近距帧数: 0
```

当前已经做到：**自动巡航不中断，动态避障不碰撞**。

但仍有一个工程取舍：为了保证不碰撞，系统更保守，部分困难目标会通过“超时推进”继续巡逻，而不是强行贴近目标点。

---

## 2. 关键数据

### 2.1 修复前典型现象

当前窗口曾出现：

```text
算法: CBF
目标: 厨房
5秒帧数变化: +150
5秒位移: 0.0175m
碰撞: 0
```

这说明程序没有暂停，帧数仍在推进，但位置几乎不变，属于“等效卡住”。

### 2.2 一小时分析发现的问题

第一次 1 小时等效分析显示：

```text
完成轮次: 10
碰撞次数: 0
近距帧数: 101
跳点次数: 93
```

说明当时已经不容易撞，但大量目标依赖超时推进，主要集中在：

- 餐厅绕行
- 门道_储物_餐厅
- 门道_餐厅_厨房
- 储物间
- 厨房
- 返回充电

### 2.3 擦边碰撞点

在中间调参阶段曾发现 4 次擦边碰撞：

```text
(5301,  卧室2,   -2.97,  2.44, person_2, 0.536)
(9153,  卧室2,   -2.90,  2.45, person_2, 0.529)
(9970,  返回充电, -1.33, -2.46, person_1, 0.548)
(10201, 卧室2,   -2.98,  2.44, person_2, 0.549)
```

碰撞半径是 0.55m，这些距离都贴着阈值，属于安全余量不足导致的擦碰。

### 2.4 最终复测

最后将动态安全圈提高到 0.85m 后，一小时复测结果：

```text
碰撞次数: 0
近距帧数: 0
完成轮次: 6
跳点次数: 87
```

这说明“不碰撞”目标已经达成，但巡航效率和精确到点能力仍有提升空间。

---

## 3. 已经完成的代码改进

### 3.1 加入 CBF 安全过滤

新增 `CBF(VO+CBF)` 思路：

```text
VO 生成候选速度
CBF 检查该速度是否会进入危险区域
如果危险，则投影到安全速度
```

相关位置：

```text
visual_sim.py
auto_patrol_simulation.py
compare_algorithms.py
```

### 3.2 修复二维速度传递

原逻辑只传 `vx`，`vy` 固定为 0，会导致相对速度判断错误。

已改为同时维护：

```text
_last_vx
_last_vy
```

这样 VO / ORCA / APF / CBF 才能正确判断机器人真实运动方向。

### 3.3 防止避障结果被路径跟随覆盖

底层巡航里，速度避障算出的 `step_x / step_y` 之前可能被后面的 A* 路径跟随覆盖。

已修复为：

```text
如果 STVOC / VO / CBF 已经接管，则不再进入普通路径跟随分支。
```

### 3.4 增加可视化状态心跳

新增：

```text
visual_status.json
```

用于记录：

```text
frame
algorithm
target
target_idx
rounds_completed
robot_x
robot_y
action
collisions
near_miss
paused
timestamp
```

以后判断“是不是不动”，不需要肉眼看窗口，可以直接读取心跳文件。

### 3.5 餐厅绕行航点

餐厅直线路径会穿过餐桌区域，因此新增可视化绕行点：

```text
餐厅绕行: (2.35, 1.45)
餐厅入口: (2.35, 0.25)
```

用于避免从储物间直接斜穿餐桌。

### 3.6 可视化目标点调整

可视化 demo 不应强行贴近家具深处，因此调整为可达观察点：

```text
储物间: (2.45, 2.35)
厨房:   (2.65, 0.45)
```

底层真实巡航点不受影响。

### 3.7 最后动态安全过滤

在执行移动前增加最后一道检查：

```text
如果下一步距离任意行人 < 0.85m：
    重新采样候选方向
    优先远离最近行人
    同时保持静态障碍安全
如果找不到安全方向：
    停止或小步后退
```

这个模块直接解决擦边碰撞。

---

## 4. 当前剩余问题

### 4.1 可视化层仍不是完整导航器

可视化层现在能自动跑、能避障、不碰撞，但本质上仍然是：

```text
巡逻点序列 + 局部避障 + 安全过滤 + 超时推进
```

它不是完整的：

```text
静态全局路径规划 + 动态局部轨迹优化 + 安全控制
```

所以在家具密集区仍会出现跳点。

### 4.2 跳点次数仍偏高

最终一小时数据：

```text
skip_count = 87
```

主要集中在：

```text
储物间
门道_储物_餐厅
餐厅绕行
餐厅入口
餐厅
门道_餐厅_厨房
厨房
```

这说明这些区域仍然是路径规划薄弱点。

### 4.3 近距帧数指标需要重定义

现在的 `near_miss` 更像“近距帧数”，不是“近距事件数”。

建议改为：

```text
near_miss_event_count
near_miss_frame_count
min_dynamic_distance
time_to_collision_min
```

否则长时间运行时，近距帧数会夸大问题。

---

## 5. 下一步改进路线

### P0：保证不碰撞

目标：

```text
1小时等效仿真 collision = 0
min_dynamic_distance > 0.55m
```

当前已经基本达成。

继续保持：

- CBF 安全过滤
- 0.85m 动态安全圈
- 下一步动态安全采样
- 不安全则停止/后退

### P1：减少跳点

目标：

```text
1小时 skip_count < 10
```

建议做法：

1. 给可视化层加入静态路径图。
2. 每个房间目标不直接直线追踪，而是先走预定义安全航道。
3. 对储物间、餐厅、厨房建立局部子路径。

示例：

```text
储物间 -> 餐厅:
储物间观察点
门道_储物_餐厅
餐厅东侧绕行点
餐厅入口
餐厅观察点

餐厅 -> 厨房:
餐厅观察点
门道_餐厅_厨房
厨房入口
厨房观察点
```

### P2：引入真正的全局路径规划

更推荐的方案是让 `visual_sim.py` 使用和底层巡航一致的路径层：

```text
A* / Risk-Aware A*
```

可视化层当前不应该继续只靠直线。

目标架构：

```text
目标点
  ↓
静态 A* 路径
  ↓
局部候选速度 VO / DWA / TEB
  ↓
CBF 安全过滤
  ↓
执行 step
```

### P3：改进局部规划器

当前 `VO+CBF` 已经安全，但路线效率一般。

下一步可以尝试：

```text
TEB + CBF
MPC + CBF
DWA + CBF
ORCA + CBF
```

其中最推荐：

```text
A* + TEB + CBF
```

原因：

- A* 解决静态家具绕行
- TEB 解决局部轨迹平滑
- CBF 保证最后安全边界

---

## 6. 推荐最终算法架构

### 6.1 当前可用架构

```text
巡逻点序列
  ↓
VO 候选速度
  ↓
CBF 安全过滤
  ↓
动态安全采样
  ↓
执行移动
```

优点：

- 实现简单
- 已能 1 小时无碰撞
- 适合演示

缺点：

- 静态路径不够聪明
- 仍依赖超时推进
- 容易在复杂家具区效率下降

### 6.2 推荐升级架构

```text
巡逻目标
  ↓
静态路径规划 A*
  ↓
局部轨迹优化 TEB / DWA
  ↓
动态障碍预测 VO / ORCA
  ↓
CBF 最终安全过滤
  ↓
执行控制
```

这个架构更适合作为毕业设计、论文实验或真实机器人导航系统。

---

## 7. 验证指标

后续每次改动后都应该跑一小时等效测试。

最低通过标准：

```text
collision_count == 0
min_dynamic_distance >= 0.55m
wall_penetration_count == 0
process_alive == true
frame_delta > 0
```

较好标准：

```text
collision_count == 0
near_miss_event_count == 0
skip_count < 10
stall_event_count == 0
rounds_completed >= 6
```

优秀标准：

```text
collision_count == 0
near_miss_event_count == 0
skip_count == 0
stall_event_count == 0
rounds_completed 稳定
轨迹平滑，无来回抖动
```

---

## 8. 当前建议

短期不要再继续单独调 APF、VO、ORCA 参数。

更值得做的是：

1. 保留当前 `CBF(VO+CBF)` 作为安全基线。
2. 把可视化层的直线目标追踪升级为 A* 路径跟随。
3. 将 `near_miss` 从帧计数改成事件计数。
4. 建立固定的 1 小时回归测试脚本。
5. 后续再比较：

```text
VO
ORCA
DWA
TEB
VO+CBF
TEB+CBF
```

最终推荐方向：

```text
A* + TEB + CBF
```

当前版本已经能做到不碰撞；下一阶段重点是减少跳点和提升巡航效率。
