# UE 5 三维仿真问题诊断报告

**日期：** 2026-08-13
**对象：** `E:\puppyfangzhen\cpp_src\bridge\nav_ue_bridge.cpp` + `config/scene_home.json` + `D:\puppy_ue`
**结论一句话：** 导航核心本身（AMCL/A*/RVO/CBF）没崩溃，但**机器人从第一帧就被卡死，且没有任何脱困机制能救它**；此外巡逻目标被过度过滤、UE↔Bridge 握手失败、代码被乱码淹没。

---

## 一、运行方式与实际结果

本机无 GPU/显示器，无法把真实 UE5 三维画面跑起来（且 UE↔Bridge 握手本身就是坏的，见第四节）。因此采用项目自带的**独立代理模式** `nav_ue_bridge.exe --standalone` 来验证——它复用**同一套导航核心、同一份场景、同一套运动积分逻辑**，是判断"导航逻辑本身对不对"的最佳代理（不依赖 GPU/UE）。

执行：
```
cpp_src/bridge/build/Release/Release/nav_ue_bridge.exe \
    --standalone --seed 1 --frames 1800 --scene "../../config/scene_home.json"
```

输出（见 `cpp_src/bridge/standalone_test_20260813.log`）：
```
SUMMARY: collisions=1 ue_collision_events=0 astar_rate=100.0 avg_err=0.096
         max_err=0.102 rooms=1 rounds=0 targets=0 gt_distance=0.00
         motion_frames=0 stuck_frames=1800 stuck_ratio=100.0
         boundary_frames=0 boundary_ratio=0.0 confidence=0.980 frames=1800
```

跑满 1800 帧（60 秒 @30Hz），**机器人一步没动**：`motion_frames=0`、`gt_distance=0.00`、`stuck_ratio=100.0`。但 `astar_rate=100.0`（每次重规划都找到了路径），说明 A*/路径规划正常——问题出在"算出了路却迈不开腿"。

---

## 二、核心问题①（P0，致命）：机器人起点被硬编码在家具内部

`run_standalone()` 第 490 行：
```cpp
double robot_x = -1.0, robot_y = -3.0, robot_yaw = 0.0;   // 硬编码起点
```

而 `scene_home.json` 里 `coffee_table` 的范围是 `x∈[-2.5,-0.5], y∈[-3.8,-2.8]`。**点 (-1.0,-3.0) 正好落在茶几里面！**

- 场景实际规定的初始位姿是 `initial_pose = (0.5,-5.0)`（dock 点，自由空间），但 standalone 完全忽略了它，自己写死了 (-1,-3)。
- 起点在障碍内 → 每一步 `is_position_safe()`（见 `simulation.h:140`，判点是否落在任一障碍扩展 0.05m 内）都返回不安全 → 整步移动被拒、单轴移动也被拒（`nav_ue_bridge.cpp:768-777`）→ 机器人原地不动。

---

## 三、核心问题②（P0，致命）：脱困逻辑是死代码

`run_standalone()` 主循环第 808–828 行：
```cpp
if (g_frame >= max_frames) {
    ... printf(SUMMARY); ...
    return 0;
}
continue;                       // ← 无条件 continue，位于 if 块之外、循环层级上
if (actual_move < 0.005) { stall_timer++; }   // 永远执行不到
...                            // 卡死计时 + 脱困方向搜索 + 90 帧恢复推进，全部不可达
```

这个 `continue` 放在了终止分支 `if` 的**外面**，导致后面整段"卡死检测（`stall_timer`）/ 脱困方向搜索 / `recovery_mode` 推进"全部成为**不可达的死代码**。一旦卡住（从第一帧起就卡），没有任何机制把机器人拉出来 → 永久卡死。

> 两个 P0 叠加 = 100% 永久卡死。只要（a）把起点改成场景里的合法位姿 `(0.5,-5.0)`，（b）删掉第 827 行那个多余的 `continue`，机器人就能正常巡逻。

---

## 四、问题③（P1）：巡逻目标被过度过滤 —— 安全框比房间还小

`is_patrol_target_safe()` 第 80–90 行用了一个比房间还小的"安全框"：
```cpp
constexpr double margin = 0.40;
if (target.x < -4.60 || target.x > 4.60 ||
    target.y < -3.60 || target.y > 3.60) return false;   // 房间其实是 16m×12m！
```
房间实际是 `x∈[-8,8], y∈[-6,6]`。这个 ±4.6/±3.6 的框把房间边缘（门、厨房、卫生间、主卧、返回点）全部判为"不安全"。

运行日志实锤（`standalone_test_20260813.log` 第 1–9 行）有 **9 个目标被错误剔除**，包括：
- `dock (0.50,-5.00)` —— **其实就是场景规定的机器人初始位姿**，竟被当成"不安全"；
- `living_room_w (-5.50,-2.00)`、`master_bed (-5.91,3.23)`、`kitchen (6.00,-0.50)`、`bathroom (2.20,4.00)`、`return_south (3.50,-4.00)` 等合法目标。

后果：机器人即便能跑，也只会去 11 个"框内"目标，绕开所有房间边缘和门——导航策略严重退化。
**建议：** 框边界直接取场景外边界（约 ±7.8/±5.8），或干脆删掉这个框，只保留"离障碍 0.4m"那一条判据。

---

## 五、问题④（P1）：UE 与 Bridge 握手失败 —— 真实三维仿真起不来

`D:\puppy_ue` 的 `ue_editor_r7.log` 中大量重复：
```
[PuppyRobotPawn] NOT connected (state=0, -1=noSock 0=NotConn 1=Conn 2=Error). Not sending
```
- UE 端的 `PuppyRobotPawn` 始终连不上 Bridge（localhost:7777）。
- 启动脚本 `launch_ue_sim.bat` 先起 UE，**只 ping 13 秒就起 Bridge**，存在明显启动竞态：UE 还没准备好 socket，Bridge 已连不上（或反之）。
- **建议：** 改成双向等待/重试握手（Bridge 启动后重试 `connect` 若干次，或 UE 端做带超时的自动重连），不能只 ping 固定 13 秒。

---

## 六、问题⑤（P2）：代码卫生 —— `nav_ue_bridge.cpp` 被乱码注释淹没

- 当前 `nav_ue_bridge.cpp` 约 9.4MB，包含海量 **GBK/UTF-8 解码错乱的中文乱码注释（闂傚倸鍊…）**，几乎无法进行正常代码审查。
- 清理前的备份 `nav_ue_bridge_before_comment_cleanup_20260809.cpp` 曾达 89MB。
- **建议：** 用脚本剥离/重转这些坏注释（或 `git blame` 定位引入提交），恢复成纯 ASCII/UTF-8 可读注释，否则后续维护成本极高。

---

## 七、问题⑥（P2）：场景文件在编译后被改动 —— 潜在不一致

- Bridge 可执行文件编译于 **Aug 10**；`config/scene_home.json` 最后修改 **Aug 12 23:35**（v5.5）。
- standalone 运行时从 JSON 加载场景，用的是最新场景；但若 exe 里硬编码了任何场景相关常量，就可能与 v5.5 不一致。
- **建议：** 任何场景调整后重新编译 bridge，并在 CI 里加"场景版本 vs 代码版本"校验。

---

## 八、优先级与修复清单

| 优先级 | 问题 | 影响 | 修复动作 |
|--------|------|------|----------|
| **P0** | 起点硬编码在茶几内 `(-1,-3)` | 100% 卡死 | 改用场景 `initial_pose = (0.5,-5.0)` |
| **P0** | 多余 `continue` 使脱困逻辑死代码 | 卡死无解 | 删除第 827 行 `continue` |
| P1 | 巡逻安全框太小（±4.6/±3.6） | 9/20 目标被删，导航退化 | 框扩到房间边界或删除 |
| P1 | UE↔Bridge 握手竞态 | 真实 3D 起不来 | 双向重试握手 |
| P2 | 乱码注释淹没代码 | 无法维护 | 剥离/重转坏注释 |
| P2 | 场景改动后未重编 | 潜在不一致 | 重编 + 版本校验 |

**建议的下一步：** 先修两个 P0（起点 + 多余 continue）→ 重编 → 再跑 standalone 验证机器人能正常巡逻、能到多个房间 → 然后处理 P1（UE 握手 + 巡逻框），最后做 P2 清理。

> 注：以上均为诊断结论，未改动任何源码。是否要我直接动手修 P0 并重新编译验证？
