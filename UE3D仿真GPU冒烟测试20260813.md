# UE 3D 仿真 · GPU 冒烟测试诊断报告
**日期**：2026-08-13 ｜ **测试方式**：`UnrealEditor.exe -game` + `nav_ue_bridge.exe`（端口 7777，等待 UE 30s 后启动 Bridge）｜ **时长**：约 97s / 5420 帧

## 一、结论速览
- ✅ **GPU 可以跑**：GTX 1650 SUPER 上 D3D12 RHI 正常初始化，3D 场景确实在显卡上渲染。
- ✅ **UE↔Bridge 握手这次成功了**：之前日志里的 `PuppyRobotPawn NOT connected` 是启动竞态（原脚本只 ping 13s）。本次等了 30s，LiDAR/GT/Ack/cmd_vel 双向全通。
- ⚠️ **但仿真功能上是"假跑"**：机器狗开局就卡在茶几里、巡逻目标被砍掉 9/11、AMCL 定位全程 0 次更新、Bridge 与 UE 坐标对不上。最后狗只在房间里挪了 ~6.78m 就停在一个角落空转，根本没在巡逻。

> GPU 渲染没问题，问题全在导航核心逻辑。修完逻辑，这个 3D 仿真就能真正跑起来。

---

## 二、GPU / 渲染：✅ 通过
来自 UE 日志（`D:\puppy_ue\Saved\Logs\puppy_ue.log`）：
```
LogD3D12RHI: ID3D12Device12 is supported
LogD3D12RHI: Bindless resources are supported
LogD3D12RHI: Mesh shader tier 1.0 is supported
LogD3D12RHI: Wave Operations are supported (wave size: 32)
LogRHI: Texture pool is 2104 MB (70% of 3006 MB)
LogVRS: Current RHI supports Variable Rate Shading
```
- 显卡 **GTX 1650 SUPER / 4GB / 驱动 32.0.15.9186**，D3D12 全特性可用，纹理池 2.1GB。
- 结合 Aug12 工程编译已通过着色器编译（PuppyNav.dll 链接成功），GPU/驱动链路在"渲染 + 编译"两端都验证过。
- 机器挂着向日葵 / GameViewer / ToDesk 虚拟显示适配器，UE 用 `-windowed` 渲染到虚拟屏，远程桌面可直接看到画面。

## 三、UE↔Bridge 握手：✅ 本次通过（但仍有竞态隐患）
UE 侧 Pawn 日志确认双向通信：
```
[PuppyRobotPawn] LiDAR: rays=72 max=800.0cm sent=1
[PuppyRobotPawn] Sent frame: GT@(-403.7,-243.4) sent=1 Ped(5)=1 Ack=1 StepAckWait=0
[PuppyRobotPawn] HandleCmdVel vx=...m/s ... |UE cm/s|=(4.0,-4.7) speed=6.2cm/s
[PuppyRobotPawn] MOVE-3s: Intended=0.15cm Actual=0.15cm | ... | Vel=(2.8,-3.3)cm/s
```
- Pawn 能发 LiDAR/GT、能收 Ack、能收 cmd_vel、能实际移动（MOVE 的 Before→After 有位移）→ 闭环是通的。
- **竞态根因**：`launch_ue_sim.bat` 只 `ping 13s` 就起 Bridge，UE 冷启动（着色器/关卡流送）往往 >13s，导致 Pawn 连不上。本次手动等到 30s 即正常。**修复很简单**：把等待从 13s 提到 30~40s，或加"端口可连接才启动"的探测。

## 四、功能缺陷（P0/P1）—— 这才是真问题

### P0-1 开局出生点在家具里
Bridge 收到的初始真值：`GT_INIT x=-1.15 y=-3.45`。
场景里 `coffee_table` 范围 `x∈[-2.5,-0.5], y∈[-3.8,-2.8]` → `(-1.15,-3.45)` **正好落在茶几内部**。
（standalone 里硬编码 `(-1.0,-3.0)` 同样在茶几里，两处同源。）
后果：开局就被障碍包围，LiDAR/定位全被污染。

### P0-2 巡逻目标被过度过滤（9/11 被砍）
Bridge 启动日志实锤 9 条 `skip unsafe target`：
```
dock (0.50,-5.00)  living_room_w (-5.50,-2.00)  door_mb_in (-3.50,2.68)
master_bed (-5.91,3.23)  door_study_in (0.10,2.68)  door_bath_in (2.50,2.68)
bathroom (2.20,4.00)  kitchen (6.00,-0.50)  return_south (3.50,-4.00)
```
`scene_home.json` 里 `dock` 本就是机器人初始位姿，却被当成不安全删掉。
原因：`is_patrol_target_safe()` 安全框 `±4.6 / ±3.6` 比 16m×12m 房间小太多。
后果：只剩 1 个有效目标 `(-2.00,-2.00)` → 退化成"开到一点就停"，根本没有巡逻。

### P0-3 AMCL 定位全程 0 次更新（致命）
Bridge 末尾统计：
```
AMCL 更新次数:       0
AMCL 平均耗时:       0.000 ms/帧
AMCL 当前置信度:     0.675
AMCL 观测似然:       0.5215
AMCL 限幅触发(P1-2.1): 3 次
```
- 5420 帧里 AMCL **一次都没更新**，只在第 3 帧做了一次 `AMCL-RECOVER (STARTFIX_x3)` 后就冻结。
- 估计位姿 `est=(-1.99,-2.00)` 被钉死，而真实 GT 一直在漂移 → `avg_err=1.540m, max_err=2.243m`。
- **推断**：AMCL 的 per-frame 更新调用很可能只在 standalone 模式里执行、UE 模式漏接。意味着"真正上 UE 部署时机器人完全没有自定位"——这是比 standalone 卡死更严重的架构 bug。

### P1-1 Bridge 与 UE 坐标对不上
- Bridge `est` ≈ `(-1.99,-2.00)`（米）。
- UE Pawn 世界坐标 `Loc=(-403.7,-243.4)`，即 `(-4.04,-2.43)` 米量级，且带偏移。
- 既非纯 ×100（cm/m），也非纯平移，**存在缩放 + 偏移双重错位**。结果：规划器认为狗在目标点，狗实际在别处；画面里狗停的位置和逻辑里"已到达"的位置不一致。

### P1-2 运动账目自相矛盾
```
SUMMARY: ... gt_distance=6.78 motion_frames=78 stuck_frames=5342 stuck_ratio=98.6 ...
MOV: 97s-displacement=-1.00m path_accum=0.0m collisions_total=0
```
- `gt_distance=6.78m`：UE 真实真值显示狗挪了 6.78m。
- `path_accum=0.0m` / `stuck_ratio=98.6%`：Bridge 自己的运动账目却说几乎没动。
- 二者矛盾，根因同上——Bridge 的 `est` 冻结，所以它"以为狗没动"，而狗实际被 UE 推着挪了一段后停在错误位置。
- 表面指标全绿（0 碰撞、A* 100%、AMCL conf 0.675）极具迷惑性，**掩盖了狗根本没在巡逻的事实**。

---

## 五、修复优先级与建议
| 优先级 | 问题 | 改法 |
|---|---|---|
| P0 | 出生点在茶几里 | standalone/UE 都用 `scene_home.json` 的 `dock(0.5,-5.0)` 作初始位姿，删掉硬编码 |
| P0 | 巡逻目标过度过滤 | `is_patrol_target_safe()` 安全框改为房间边界（±7.5/±5.5）并按障碍 margin 判定 |
| P0 | AMCL 不更新 | 确认 UE 模式下 AMCL per-frame 更新调用被接通（与 standalone 对齐） |
| P1 | 握手竞态 | `launch_ue_sim.bat` 等待 13s→30~40s，或改成"端口探测通过才启动 Bridge" |
| P1 | 坐标错位 | 统一 Bridge(米) 与 UE(厘米) 的缩放/偏移变换，加一组单测 |
| P2 | 代码卫生 | `nav_ue_bridge.cpp` 9.4MB 乱码注释需清理（清理前备份 `nav_ue_bridge_before_comment_cleanup_20260809.cpp` 89MB） |
| P2 | 场景滞后 | `scene_home.json`(Aug12) 晚于 bridge exe(Aug10) 编译，重编前先对齐 |

## 六、一句话给开发
**GPU 跑得动、画面出得来、握手也连得上；但导航核心三处 P0（出生点/目标过滤/AMCL 失效）让狗既没定位也没巡逻目标，只能原地空转。** 先修这三处并重编，这个 3D 仿真才能算"真能跑"。

---
附：原始证据 `_smoke_report.txt`（GPU/RHI + Pawn 通信 + Bridge 输出 + SUMMARY）、`_smoke_bridge.log`（完整 Bridge 运行日志）。
