# UE 房间重做 v6.0（2026-08-13）

用户诉求：仿真"根本不像个正常的室内"，要求把屋子好好弄弄、简洁一些。

## 根因：之前两套场景描述互相矛盾
- `config/scene_home.json`：65 个障碍（m），含大量 0.04m 宽的"镜子"、枕头、碎墙段等怪异碎块，且 9/11 巡逻目标被过度过滤。
- `D:\puppy_ue\Source\PuppyNav\PuppyRobotPawn.cpp` 的 `HOME_OBSTACLES`：另有一套 **33 个障碍（cm）**，和 json **完全对不上**——导航按一套图走，画面渲染另一套图。
- 导航核心出生点硬编码 `(-1.15,-3.45)` = 落在茶几里；UE 侧 `INIT` 也硬编码同点。

→ 这就是"不像正常室内"的核心：场景碎乱 + 逻辑/画面两套布局。

## 改动（一份简洁布局，两边同步）
1. **重写 `scene_home.json`** 为 9m×7m 简洁四居室：
   - 外壳墙 + 一道 y=0.8 隔断（带卧室门洞 x[-1.2,0.3]、卫生间门洞 x[1.8,2.8]）+ 卧室/卫生间竖向隔断。
   - 房间：客厅(SW)、厨房(SE 开放式)、卧室(NW)、卫生间(NE)。
   - 家具仅 10 件：沙发、茶几、电视柜、橱柜、餐桌、床、衣柜、马桶、浴缸、洗手盆。
   - 障碍总数 **65 → 18**；巡逻点 **5 个**（均在桥安全框 ±4.6/±3.6 内且离障碍 >0.4m，不再被误删）；行人 **6 → 3**；出生点改到开阔客厅 **(0.5,-2.2)**。
2. **同步改写 UE 端 `PuppyRobotPawn.cpp`**（重编 PuppyNav 模块成功，2m25s）：
   - `HOME_OBSTACLES` 替换为与 json 完全一致的 18 个（cm 单位）。
   - `INIT_X_M/INIT_Y_M` → 0.5 / -2.2（与 json 出生点一致）。
   - `HOME_BOUND` 收缩到房间内边界 (-440..440, -340..340) cm。
   - 行人配置同步为 3 个、新坐标。
   - 行人碰墙用世界几何重叠检测，自动跟随新墙。

## 验证结果（GPU 冒烟测试，约 387s）
- ✅ GPU/D3D12 正常渲染（仅无害的 "Cannot end block when stack is empty" 噪声）。
- ✅ UE↔Bridge 握手正常。
- ✅ **0 条 "skip unsafe target"** —— 5 个巡逻点全部保留，过度过滤 bug 随简洁布局消失。
- ✅ 狗在开阔客厅出生并真正驱动（2 m/s 朝客厅目标移动），家具碰撞回退(BBOX-COLLIDE/REVERT)机制生效。
- ⚠️ **遗留：狗卡在茶几上反复冲撞**（collisions_total=11586 / 387s ≈ 30 次/秒，始终停在目标#1，rounds=0）。
  - 这是**导航核心 P0**（非房间问题）：path-follower 在 UE 模式下走"朝目标的直线"而不是跟随 A\* 路径，叠加 AMCL 漂移估计，导致狗直冲家具被卡死。
  - A\* 本身正常（calls=580 ok=580 rate=100%）。

## 建议下一步（待确认）
房间已修复。要让狗真正巡逻，需另修导航核心（`nav_ue_bridge.cpp`，9.4MB）：
- 让 path-follower 跟随 A\* 路径而非直线冲目标；
- 修复 AMCL 在 UE 模式下不更新（avg_err 0.5~1.5m 漂移）。
确认后我再动手。
