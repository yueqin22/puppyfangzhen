# 机器狗自主导航仿真系统 — 完善与提升任务书

**文档编号**: wanshan2026080201
**编写日期**: 2026-08-02
**基线版本**: v3.2.18i（依据 HANDOFF_FINAL.md，10 种子 1 小时测试全部零碰撞）
**面向对象**: 接手开发的工程师
**使用方式**: 按优先级（P0→P4）逐项执行；每项任务含背景、工作内容、验收标准，完成后在对应复选框打勾并记录实际结果。

---

## 0. 现状一句话总结

核心算法指标已**全部达标**（零碰撞、A* 97.2~100%、定位误差 0.064~0.161m），当前瓶颈不在算法，而在**工程基础设施薄弱**（构建、测试、配置、代码组织）和**真机就绪度不足**。后续工作重心应从"调指标"转向"固成果、补工程、备真机"。

---

## 1. P0 — 工程基础设施（最优先，预计第 1~2 周完成）

> 这一层的问题不解决，后续任何算法改动都无法安全验证，"零碰撞"成果可能在一次重构中悄悄丢失。

### 任务 1.1 修复并完善 CMake 构建系统

- **背景/问题**：
  - 当前 `cpp_src/sim/build.bat` 是一条硬编码的 `cl.exe` 命令，VS2022 路径写死为 `C:\Program Files\Microsoft Visual Studio\2022\Community\...`，换机器/换版本即失效。
  - `cpp_src/sim/CMakeLists.txt` **目前是不完整的**：只有 `add_executable(sim_test main.cpp)`，缺少 `occupancy_grid.cpp / costmap.cpp / astar_planner.cpp / amcl.cpp` 四个源文件和 `include` 目录，直接 cmake 构建会链接失败。
- **工作内容**：
  1. 重写 `cpp_src/sim/CMakeLists.txt`：补齐全部源文件、`target_include_directories`（`../puppy_nav_core/include` 和 `.`）、C++17、`/O2 /MT /utf-8` 等编译选项与 build.bat 对齐。
  2. 保留 `build.bat` 作为一键入口，但内部改为调用 `cmake -B build && cmake --build build --config Release`，不再直接调 cl.exe。
  3. 支持 MSVC 之外的生成器（至少验证 Ninja + MSVC 可用），为将来 Linux 构建留口。
  4. 把 `HANDOFF_FINAL.md` 短期待办"固化 Visual Studio 编译环境"一条随本任务一并关闭。
- **验收标准**：
  - 在一台**没有**装 VS2022 Community 版（或路径不同）的机器上，仅安装任意 VS2022 SKU + CMake 即可构建出 `sim_test.exe`。
  - 构建产物跑 `sim_test.exe 54000 27000 8` 结果与基线一致（0 碰撞）。
- **涉及文件**: `cpp_src/sim/CMakeLists.txt`、`cpp_src/sim/build.bat`
- **预估工作量**: 0.5 天

### 任务 1.2 24 小时连续压测 + 内存泄漏检测

- **背景/问题**：HANDOFF_FINAL.md 短期待办已列出，尚未执行。当前最长验证仅 1 小时（108000 帧），真机目标要求 100+ 小时无故障，24h 是中间必经台阶。
- **工作内容**：
  1. 用 `sim_test.exe` 跑 24 小时等效帧数（`108000 * 24 = 2592000` 帧），10 个种子各一次，可并行。
  2. 同时用 Windows Performance Monitor 或代码内嵌计数（`mallinfo` 替代方案 / CRT `_CrtMemCheckpoint`）记录内存曲线，确认无单调增长。
  3. 记录每小时的碰撞数、A* 成功率、AMCL 耗时分布，观察是否有随时间退化的指标（如地图膨胀累积、粒子数漂移）。
  4. 压测脚本固化为 `cpp_src/sim/stress_24h.ps1`，输出 CSV 汇总。
- **验收标准**：
  - 10 种子 24h 等效测试**零碰撞、零穿墙**，A* 成功率不低于 1h 基线对应值 2 个百分点。
  - 内存曲线平稳（末段相对首段增长 < 5%），无句柄/内存泄漏结论性证据。
  - 若发现退化指标，开新任务单独立项分析。
- **涉及文件**: 新增 `cpp_src/sim/stress_24h.ps1`、压测结果存档到 `cpp_src/sim/results/`
- **预估工作量**: 1 天（含机器时间）

### 任务 1.3 建立单元测试体系

- **背景/问题**：当前只有 2 个集成测试（`astar_corridor_test.cpp` 149 行、`nav_integration_test.cpp` 183 行），没有单元测试框架，核心算法（AMCL 聚类估计、costmap 膨胀、A* 启发式、RVO）任何内部改动都只能靠 1 小时仿真盲验，回归成本极高。
- **工作内容**：
  1. 引入轻量测试框架：首选 **GoogleTest**（vcpkg 或源码引入均可）；若坚持零依赖原则，可写 100 行内的极简断言宏框架。
  2. 首批覆盖以下纯函数/类（输入确定、易断言）：
     - `OccupancyGrid`：坐标转换、`is_in_bounds`、栅格读写；
     - `Costmap`：膨胀层距离衰减、门宽 1m 场景下膨胀后可通行性；
     - `AStarPlanner`：固定地图上的路径存在性、octile 启发式可采纳性（h ≤ 真实代价）、UNKNOWN 软惩罚生效；
     - `AMCL`：聚类估计在人工构造的双簇粒子云上选最大簇、限幅平滑 MAX_STEP=0.3m 生效、scan_score 归一化范围 [0,1]；
     - `RVO`：双智能体对向场景速度解算不发生穿透。
  3. 测试纳入 CMake（`enable_testing()` + `ctest`），`build.bat` 增加 `--test` 选项。
- **验收标准**：
  - `ctest` 全绿；核心模块行覆盖率 ≥ 60%（可用 OpenCppCoverage 出报告）。
  - 后续每个算法 PR 必须附带对应单测（写入贡献规范）。
- **涉及文件**: 新增 `cpp_src/tests/` 目录、`cpp_src/sim/CMakeLists.txt`
- **预估工作量**: 2~3 天

### 任务 1.4 回归基线与一键验证流水线

- **背景/问题**：`batch_test.ps1` 是手工 PowerShell 循环，结果提取靠人眼；历史结果（如 `v3218i_10seed.txt`）散落在 sim 目录，没有"基线对比"机制——改了代码后无法自动判断指标是变好还是变坏。
- **工作内容**：
  1. 把 v3.2.18i 的 10 种子结果固化为 **baseline JSON**（`cpp_src/sim/baseline/v3218i.json`），字段：seed、碰撞、A* 成功率、avg_err、置信度、房间数。
  2. 改造 `batch_test.ps1`：跑完后自动与 baseline 对比，输出逐项 diff 表，任一指标退化即非零退出。
  3. 串成一键流水线：`build → unit test → 10 种子回归 → baseline 对比`，单命令执行。
  4. （可选）接入 GitHub Actions / Gitee Go 的 Windows runner 做 CI；暂无 CI 条件则至少保证本地一键。
- **验收标准**：
  - 单条命令完成全部验证并输出"通过/退化"结论；故意改坏一个参数（如 flee_speed 调 1.5）能被自动检出。
- **涉及文件**: `cpp_src/sim/batch_test.ps1`、新增 `cpp_src/sim/baseline/v3218i.json`
- **预估工作量**: 1 天

---

## 2. P1 — 算法指标收尾（第 2~4 周）

> 指标已达标，以下是"最后一公里"的收口工作，全部基于 HANDOFF_FINAL.md 第 8 节"已知限制"。

### 任务 2.1 max_err 尖峰治理（门道粒子云分裂）

- **背景/问题**：门道穿越时粒子云短暂分裂，max_err 瞬时达 2~8m，虽 50 帧内自愈，但均值指标好看的同时尖峰可能触发下游误判（如未来接 TEB 后瞬时错误位姿会产生危险速度指令）。
- **工作内容**：
  1. 先在仿真中加 **尖峰事件日志**：记录每次 |est - true| > 1m 的起止帧、粒子簇数、置信度，量化尖峰频率（当前只有定性描述）。
  2. 方案 A（保守）：尖峰检测期间对外输出位姿冻结/降权，下游规划器收到 `pose_uncertain` 标志时减速。
  3. 方案 B（根治）：门道场景下扩大 KLD bin 尺寸或提高最小粒子数，避免过窄门道导致的粒子贫化；或对穿门过程做"虚拟墙"先验。
  4. 两方案都实现后用消融对比，保留更优者。
- **验收标准**：10 种子回归中 max_err > 1m 的尖峰次数相对基线下降 ≥ 50%，且不引起 avg_err / A* 成功率退化。
- **涉及文件**: `cpp_src/puppy_nav_core/src/amcl.cpp`、`cpp_src/sim/nav_bridge.h`
- **预估工作量**: 2~3 天

### 任务 2.2 seed 6 A* 成功率 97.2% → ≥99%

- **背景/问题**：剩余失败全部为"真无路径"——AMCL 估计漂移使规划起点落入墙内。HANDOFF 已定位根因，未修复。
- **工作内容**：
  1. 在 `nav_bridge.h` 规划入口处加**起点合法性自愈**：起点落在膨胀层障碍内时，以螺旋/广度优先搜索最近的自由栅格作为替代起点（ROS Nav2 的 `clearStartPose` 同款思路），并记录修正距离。
  2. 修正距离 > 0.5m 时打警告日志（说明定位漂移已较大），供任务 2.1 数据交叉验证。
  3. 注意保持"真无路径"用例仍能正确返回失败（不可达目标过滤逻辑不能误伤）。
- **验收标准**：seed 6 A* 成功率 ≥ 99%，其余 9 个种子不退化；新增起点修正次数日志纳入回归报告。
- **涉及文件**: `cpp_src/sim/nav_bridge.h`、`cpp_src/puppy_nav_core/src/astar_planner.cpp`
- **预估工作量**: 1~2 天

### 任务 2.3 ICP 去留决策（反馈粒子云 or 移除）

- **背景/问题**：ICP 代码保留但 `USE_ICP=0` 禁用，根因是校正量只改 `est_x/y` 不反馈粒子云导致状态不一致。死代码长期留存会增加误用风险和维护负担。
- **工作内容**：
  1. 二选一，**不要继续搁置**：
     - 方案 A（修复）：ICP 校正量以"整体平移粒子云 + 重采样"方式反馈回 AMCL，小范围验证（3 种子 × 10 分钟）确认 max_err 改善后启用；
     - 方案 B（移除）：从 amcl.cpp 删除 ICP 代码与环境变量，Git 历史保留可查。
  2. 决策依据写入本文件对应复选框旁（一句话即可）。
- **验收标准**：二选一落地；若选 A，消融实验显示 max_err 改善且 10 种子回归零碰撞；若选 B，代码库不再出现 ICP 引用。
- **涉及文件**: `cpp_src/puppy_nav_core/src/amcl.cpp`、`cpp_src/puppy_nav_core/include/puppy_nav_core/amcl.h`
- **预估工作量**: 1~2 天（方案 B 半天）

### 任务 2.4 仿真参数配置化（环境变量 → 配置文件）

- **背景/问题**：仿真开关靠 `USE_AMCL / USE_IMU_FUSION / USE_ICP / USE_RVO / SEED` 五个环境变量，而 `config/` 下已有 8 个 YAML（runtime.yaml、sim.yaml 等）但 C++ 仿真器**并不读它们**——两套配置体系并存，ROS2 侧和仿真侧参数已经漂移，真机切换时极易出错。
- **工作内容**：
  1. `sim_test.exe` 增加 `--config <yaml>` 参数，读取 `config/sim.yaml`（可自行扩展字段）；环境变量仅作为覆盖项保留（优先级：命令行 > 环境变量 > YAML > 默认值）。
  2. 不引入第三方 YAML 库的话，写一个仅支持本配置所需子集（key: value、一层嵌套、注释）的 200 行迷你解析器，符合项目"零依赖"约束。
  3. 算法内部常量（ROBOT_RADIUS、INFLATION_RADIUS、MAX_STEP、flee 速度分档、追逐阈值 0.3/0.8 等）集中到一个 `sim_params` 结构体，从 YAML 加载，禁止散落魔法数字。
- **验收标准**：改 YAML 不改代码即可复现 v3.2.18i 全部实验配置；`USE_AMCL=0` 消融实验通过配置切换完成；10 种子回归结果与基线一致。
- **涉及文件**: `cpp_src/sim/main.cpp`、`cpp_src/sim/simulation.h`、`config/sim.yaml`
- **预估工作量**: 1~2 天

---

## 3. P2 — 代码质量与架构（第 3~5 周，可与 P1 部分并行）

### 任务 3.1 拆分 simulation.h（1403 行头文件）

- **背景/问题**：仿真核心全塞在一个头文件里（场景构建、行人运动、flee 避障、碰撞检测、诊断回溯混在一起），每次改动全量重编译，且无法对单一职责做单元测试。
- **工作内容**：
  1. 按职责拆分为：`scene.h/.cpp`（房间/家具布局）、`pedestrian.h/.cpp`（行人运动模型）、`flee.h/.cpp`（避让策略）、`collision.h/.cpp`（碰撞检测+环形缓冲诊断）、`simulation.h`（仅保留编排逻辑）。
  2. 拆分过程**不改任何行为**：以任务 1.4 的回归流水线全绿为拆分正确的判据。
- **验收标准**：单文件均 < 500 行；10 种子回归与基线逐指标一致；新模块可被单测直接链接。
- **涉及文件**: `cpp_src/sim/simulation.h`
- **预估工作量**: 2 天

### 任务 3.2 拆分 amcl.cpp（1804 行）

- **背景/问题**：粒子滤波主流程、聚类估计、IMU 融合、MHT（已禁用）、ICP（已禁用）五块逻辑同文件，核心路径与实验代码纠缠。
- **工作内容**：
  1. 拆为 `amcl_core.cpp`（预测/更新/重采样/KLD）、`amcl_cluster.cpp`（聚类估计+限幅平滑）、`amcl_imu.cpp`（互补滤波）；MHT 与 ICP 按任务 2.3 的决策处理（移除或独立成 `amcl_experimental/`）。
  2. 头文件同步收窄：`amcl.h` 只暴露主接口，内部结构移入 `src/` 私有头。
- **验收标准**：同任务 3.1；公开头文件只保留稳定 API。
- **涉及文件**: `cpp_src/puppy_nav_core/src/amcl.cpp`、`include/puppy_nav_core/amcl.h`
- **预估工作量**: 2 天

### 任务 3.3 禁用代码与实验分支清理

- **背景/问题**：MHT、ICP、CBF（`USE_RVO=0` 时才用）三套代码处于"保留但不用"状态；HANDOFF 的教训章节已证明 MHT/ICP 在当前场景不如简单方案。
- **工作内容**：
  1. 对每套禁用代码做明确处置：删除（Git 历史可查）/ 移入 `experimental/` 目录并在 README 标注"未验证，勿用于基线" / 修复启用（仅任务 2.3 选 A 时的 ICP）。
  2. CBF 与 RVO 二选一作为默认安全层，另一个降级为 experimental；`USE_RVO` 开关语义写进配置文档。
- **验收标准**：主构建路径中不存在永不执行的死代码；`grep -r "USE_ICP\|MHT"` 结果符合处置决策。
- **预估工作量**: 0.5~1 天

### 任务 3.4 日志与诊断规范化

- **背景/问题**：当前诊断靠 `printf` + 碰撞环形缓冲区回溯，正式/调试输出不分级，批量测试时日志无法程序化消费。
- **工作内容**：
  1. 引入分级日志宏（DEBUG/INFO/WARN/ERROR），默认 INFO，环境变量或配置控制级别。
  2. 关键事件（碰撞、尖峰、起点修正、追逐触发）输出**单行结构化格式**（如 `EVT,collision,frame=12345,x=..,y=..`），便于脚本统计。
  3. 碰撞回溯缓冲从 30 帧改为可配置，且支持落盘。
- **验收标准**：批量测试日志可被一条 `findstr / grep` 直接统计出事件次数；INFO 级 1 小时日志 < 10MB。
- **预估工作量**: 1 天

---

## 4. P3 — 真机就绪（第 4~8 周）

### 任务 4.1 IMU/里程计速度来源抽象

- **背景/问题**：仿真中 IMU 速度从真值位姿差分得到（HANDOFF 已知限制 #3），真机必须用轮式/关节里程计 + IMU 实测，现在接口直接依赖真值，换真机即断。
- **工作内容**：
  1. 定义 `IOdometrySource` 接口（`get_velocity() -> (vx, vy, wz)`），仿真侧实现 `SimOdometry`（真值差分 + 可调噪声模型），真机侧预留 `PiOdometry` 桩。
  2. 噪声模型参数化（零偏、随机游走），为后续 EKF 替换互补滤波做数据准备。
  3. 仿真默认注入**真实量级噪声**重新跑 10 种子回归，量化"理想 IMU → 含噪 IMU"的指标损失——这是真机风险的第一手数据。
- **验收标准**：AMCL 不再直接触碰真位姿差分；含噪仿真回归仍零碰撞（avg_err 允许上升，需记录差值写入报告）。
- **涉及文件**: `cpp_src/sim/nav_bridge.h`、`cpp_src/puppy_nav_core/src/amcl.cpp`（融合输入处）
- **预估工作量**: 2~3 天

### 任务 4.2 ROS2 Lifecycle 完整实现

- **背景/问题**：HANDOFF 中期待办；`nav_core_lifecycle_main.cpp` 目前仅 25 行骨架。
- **工作内容**：实现 `on_configure / on_activate / on_deactivate / on_cleanup / on_shutdown / on_error` 完整状态机；未激活状态下不订阅、不发布、不规划；状态转换失败有明确错误码与日志。
- **验收标准**：用 `ros2 lifecycle` 命令走完全部状态迁移无异常；异常注入（配置加载失败）进入 error processing 并能 recover。
- **涉及文件**: `cpp_src/puppy_nav_core/src/nav_core_lifecycle_main.cpp`
- **预估工作量**: 2 天

### 任务 4.3 TEB Local Planner 集成评估（先评估，后集成）

- **背景/问题**：HANDOFF 中期待办。当前"路径跟随 + flee + RVO"是反应式局部规划，真机动态场景下 TEB 的时序优化更优，但引入 Nav2 依赖是大决策。
- **工作内容**：
  1. **先做评估文档**（≤ 2 页）：对比"继续打磨现有 RVO 栈"vs"接入 Nav2 TEB"在真机算力（PuppyPi 树莓派）、动态行人密度、维护成本上的优劣，给明确结论。
  2. 仅当结论为"接入"时才实施集成，并保留现有栈作为 fallback。
- **验收标准**：产出评估结论并评审通过；若集成，仿真对比实验显示动态场景平均任务耗时或近距离会车安全性有定量改善。
- **预估工作量**: 评估 1 天；集成另立项（3~5 天）

### 任务 4.4 真机部署前检查清单

- **背景/问题**：长期目标"真机部署与 100+ 小时验证"需要前置条件收敛，避免到真机才发现仿真假设不成立。
- **工作内容**：维护一份 `docs/real_robot_checklist.md`，逐项核对：里程计接口（4.1）、噪声容限回归数据（4.1）、lifecycle（4.2）、紧急停止链路、电池监控接口统一（IMPROVEMENT_ROADMAP.md 的 P0-1，ROS2 侧遗留）、传感器驱动实测帧率。每完成一项打勾并附验证证据链接。
- **验收标准**：清单全绿才允许真机上电自主导航。
- **预估工作量**: 持续维护

---

## 5. P4 — 仓库治理（第 1 周启动，穿插进行）

### 任务 5.1 根目录临时脚本清理（177 个 `_` 开头文件）

- **背景/问题**：根目录存在 **177 个** `_check_*.sh / _check_*.py / _auto_*.sh` 等开发脚手架脚本，是历次调试的遗留，严重干扰工程结构认知，新成员无法分辨哪些是正式工具。
- **工作内容**：
  1. 逐个过一遍，分三类：仍在用的 → 移入 `tools/` 并去掉下划线前缀、补注释；一次性调试用的 → 删除；不确定的 → 移入 `tools/legacy/` 观察一个月。
  2. **删除前必须先 `git add -A && git commit` 留档**，或整体打包备份到 `archive/scaffold_20260802.zip`。
  3. 清理后根目录只保留：README、构建入口、配置、源码目录、文档目录。
- **验收标准**：根目录非目录文件 ≤ 20 个；正式工具全部在 `tools/` 且有说明。
- **预估工作量**: 0.5~1 天
- ⚠️ **注意**: 这些是个人工程目录下的批量删除操作，执行前务必完成备份并由负责人现场确认清单，严禁脚本化无差别删除。

### 任务 5.2 文档收敛

- **背景/问题**：根目录并存 HANDOFF.md、HANDOFF_CXX_NAV_CORE.md、HANDOFF_FINAL.md、HANDOVER.md、交接5.md、jihua1/2/3/5.md、gaijin1/2.md、tigao1.md、IMPROVEMENT_ROADMAP.md 等 15+ 份文档，新人不知道信哪份。
- **工作内容**：
  1. 确立 **HANDOFF_FINAL.md 为唯一权威交接文档**，其余移入 `docs/archive/`，在各旧文档头部加一行"已归档，请以 HANDOFF_FINAL.md 为准"。
  2. 计划类文档（jihua/gaijin/tigao）中仍未执行的有效条目，合并进本任务书对应章节后归档原文。
  3. 建立文档纪律：每个版本只更新权威文档，不再新开并列文件。
- **验收标准**：根目录活跃文档 ≤ 5 份（README、HANDOFF_FINAL、本任务书、STATE_MACHINE、INTERFACES）。
- **预估工作量**: 0.5 天

### 任务 5.3 旧 Python 代码处置（nav_core/、src/）

- **背景/问题**：HANDOFF 已标注 `src/`（ROS2 Python 包）与 `nav_core/`（Python 导航核心）为旧版，当前主开发在 C++，但旧代码仍占仓库且部分 launch 文件可能仍引用。
- **工作内容**：
  1. 确认 ROS2（WSL2）侧是否仍在使用 `src/` 下各包：在用的保留并明确维护责任人；不在用的移入 `archive/python_legacy/`。
  2. `nav_core/`（纯 Python 旧导航）与 C++ `puppy_nav_core` 功能重叠，建议归档，算法细节已在 C++ 侧重写。
  3. 更新 README 的工程结构图，移除/标注已归档目录。
- **验收标准**：`colcon build` 在 WSL2 侧通过；仓库中不存在"看似可用实则废弃"的代码。
- **预估工作量**: 0.5~1 天

---

## 6. 里程碑建议

| 迭代 | 时间盒 | 内容 | 出口标准 |
|------|--------|------|----------|
| **M1 工程固化** | 第 1~2 周 | 任务 1.1~1.4 + 5.1~5.3 | 一键回归流水线全绿；仓库整洁；24h 压测通过 |
| **M2 指标收尾** | 第 2~4 周 | 任务 2.1~2.4 | 全部种子 A* ≥ 99%；尖峰降 50%；配置化完成 |
| **M3 代码重构** | 第 3~5 周 | 任务 3.1~3.4 | 拆分完成且回归无退化；单测覆盖率 ≥ 60% |
| **M4 真机就绪** | 第 4~8 周 | 任务 4.1~4.4 | 含噪仿真回归通过；lifecycle 完成；真机检查清单全绿 |

M1 是后续一切的前提，不建议并行启动 M3 的重构（重构依赖回归流水线兜底）。

---

## 7. 总验收对照表（完成时逐项填写）

| # | 任务 | 优先级 | 验收关键点 | 状态 | 实际结果 |
|---|------|--------|-----------|------|---------|
| 1.1 | CMake 构建修复 | P0 | 换机可构建，基线一致 | ☐ | |
| 1.2 | 24h 压测+内存 | P0 | 10 种子零碰撞、内存平稳 | ☐ | |
| 1.3 | 单元测试体系 | P0 | ctest 全绿、覆盖率 ≥60% | ☐ | |
| 1.4 | 回归流水线 | P0 | 一键验证+基线 diff | ☐ | |
| 2.1 | max_err 尖峰治理 | P1 | 尖峰次数降 ≥50% | ☐ | |
| 2.2 | seed 6 A* 修复 | P1 | seed6 ≥ 99%，他种不退化 | ☐ | |
| 2.3 | ICP 去留决策 | P1 | 修复启用或移除，二选一落地 | ☐ | |
| 2.4 | 参数配置化 | P1 | YAML 驱动、无魔法数字 | ☐ | |
| 3.1 | simulation.h 拆分 | P2 | 单文件 <500 行、回归一致 | ☐ | |
| 3.2 | amcl.cpp 拆分 | P2 | 同上、头文件收窄 | ☐ | |
| 3.3 | 死代码清理 | P2 | 主路径无禁用代码 | ☐ | |
| 3.4 | 日志规范化 | P2 | 分级+结构化事件 | ☐ | |
| 4.1 | 里程计抽象 | P3 | AMCL 脱离真值差分、含噪回归通过 | ☐ | |
| 4.2 | ROS2 Lifecycle | P3 | 状态机全迁移无异常 | ☐ | |
| 4.3 | TEB 评估 | P3 | 评估结论评审通过 | ☐ | |
| 4.4 | 真机检查清单 | P3 | 清单全绿 | ☐ | |
| 5.1 | 临时脚本清理 | P4 | 根目录 ≤20 文件、先备份 | ☐ | |
| 5.2 | 文档收敛 | P4 | 活跃文档 ≤5 份 | ☐ | |
| 5.3 | Python 旧码处置 | P4 | colcon build 通过、无僵尸代码 | ☐ | |

---

## 8. 执行纪律（请开发者遵守）

1. **任何算法改动必须先过回归流水线**（任务 1.4 建立后），指标退化即打回，不许"先合再看"。
2. **一次只改一件事**：v3.2.7→v3.2.18i 的教训（sigma_obs 单参数改动导致 A* 暴跌 48 个点）表明，本系统参数耦合极敏感，批量改动无法归因。
3. **改动记录追加到 HANDOFF_FINAL.md 第 7 节版本表**，保持优化历程连续。
4. 本任务书与 HANDOFF_FINAL.md 冲突时，以**更新的实测数据**为准，并回写修订本文档。

---

*本文档基于 HANDOFF_FINAL.md（v3.2.18i）编写，结合对 cpp_src/ 源码、构建脚本与仓库结构的实际核查（CMakeLists.txt 不完整、根目录 177 个临时脚本、simulation.h 1403 行 / amcl.cpp 1804 行、YAML 配置与环境变量双轨并存等）。*
