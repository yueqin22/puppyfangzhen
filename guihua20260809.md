# 工程数据分析与完善规划

**文档日期：** 2026-08-09  
**工程目录：** `E:\puppyfangzhen`  
**工程方向：** 基于 ROS2、CoppeliaSim 与自研 C++ 仿真器的四足机器人自主导航、建图、定位、规划与动态避障系统

## 1. 总体判断

当前工程已经具备较完整的导航系统雏形，包含：

- ROS2 启动与机器人描述；
- C++ 导航核心与独立仿真器；
- Python 实验、消融分析和结果可视化；
- AMCL 定位、A* 全局规划、局部避障、Costmap、恢复行为；
- 动态障碍物、巡航和长时间压力测试。

但当前主要问题不在于缺少更多算法，而在于实验数据可信度、指标采集、工程结构和性能边界仍不够稳定。下一阶段应优先完善实验基础设施，再继续优化算法。

## 2. 已有数据分析

### 2.1 消融实验的可信度不足

`experiment_results/ablation/ablation_summary.json` 标记为 `mode: demo`，每个配置只有 5 次试验。因此当前结果适合验证分析流程，不适合直接作为正式论文结论。

结果中还存在反常现象：

- 去掉动态障碍模块后，覆盖率从约 `93.78%` 上升到 `94.59%`；
- 去掉 AMCL 后，覆盖率上升到约 `96.46%`；
- 去掉 AMCL 后定位误差反而下降到约 `0.022m`；
- 去掉动态障碍模块后达到 80% 覆盖率的时间变短。

这些现象可能由以下因素造成：

1. Ground Truth 定位替代 AMCL 后，指标天然更优；
2. 动态障碍模块的保守策略降低了探索效率；
3. 实验场景没有覆盖对应模块的目标工况；
4. 不同配置没有严格使用相同的初始状态和障碍物轨迹；
5. 样本数过少，无法稳定估计方差。

因此应明确区分：

- `demo_results`：只用于展示流程；
- `official_results`：用于论文、报告和算法结论。

### 2.2 长时间避障结果

`long_test_results.json` 中不同算法表现差异明显：

| 算法 | 碰撞数 | 碰撞/1000帧 | 平均速度 | 运行耗时 |
|---|---:|---:|---:|---:|
| 基线 | 3097 | 28.68 | 5.83m/s | 21秒 |
| STVOC | 2 | 0.019 | 3.83m/s | 3584秒 |
| ORCA | 2286 | 21.17 | 5.22m/s | 50秒 |
| APF | 318 | 2.94 | 2.39m/s | 24秒 |
| VO | 3 | 0.028 | 5.84m/s | 338秒 |
| RVO | 498 | 4.61 | 5.76m/s | 205秒 |
| DWA | 3467 | 32.10 | 0.60m/s | 1672秒 |

初步结论：

- `VO/STVOC` 的碰撞控制最好，但计算成本过高；
- `APF` 是当前安全性和计算量之间较好的折中；
- `DWA` 当前表现最差，应优先排查参数、碰撞判定和局部规划逻辑；
- `ORCA` 当前结果没有体现出预期优势，需要核查邻居建模、机器人半径和时间步长。

但这组结果仍需谨慎解释，原因是碰撞统计方式、速度单位、到达率和路径进展等定义还不够统一。尤其需要确认碰撞是按事件计数还是按碰撞帧计数。

### 2.3 无效对比结果

`algo_comparison.json` 中存在以下无效值：

```json
{
  "dyn_collisions": -1,
  "wall_hits": -1,
  "arrived": -1,
  "total_points": -1,
  "arrival_rate": 0,
  "collisions_per_round": Infinity
}
```

这类结果代表日志解析失败或实验未正常完成，不应作为算法性能数据参与统计。目前 `compare_algorithms.py` 在正则匹配失败后使用默认值，容易把“数据管线失败”误判成“算法性能差”。

应改为：

```json
{
  "status": "invalid",
  "failure_reason": "metric_parser_failed",
  "missing_fields": ["arrived", "total_points"]
}
```

并禁止 `-1`、`None`、`NaN`、`Infinity` 进入均值、排序和显著性检验。

## 3. 性能分析

`performance_results.json` 显示：

- 单帧平均耗时约 `70.36ms`；
- 理论处理频率约 `14.2FPS`；
- 主要耗时来自 AMCL 和 OccupancyGrid。

| 模块 | 平均耗时 | P95 | 最大耗时 |
|---|---:|---:|---:|
| AMCL | 41.66ms | 205.53ms | 374.90ms |
| OccupancyGrid | 25.26ms | 56.32ms | 155.52ms |
| Costmap | 3.15ms | 7.53ms | 55.44ms |
| A* | 0.008ms | 0.012ms | 0.012ms |
| DWA | 0.075ms | 0.038ms | 3.48ms |

### 3.1 AMCL 优化方向

优先检查：

- 激光束数量是否过多；
- 每个粒子是否重复计算地图距离；
- 距离场是否可以缓存；
- 粒子更新是否可以批量向量化；
- 重采样是否过于频繁；
- KLD-Sampling 是否导致粒子数量突增；
- AMCL 是否需要每个控制周期都运行。

建议采用分频架构：

```text
控制周期：30Hz
局部避障：20~30Hz
Costmap：10~15Hz
AMCL：5~10Hz
全局规划：事件触发或1~2Hz
```

### 3.2 OccupancyGrid 优化方向

建议：

- 采用增量更新，不要每帧重建整张地图；
- 只更新激光影响范围；
- 使用局部地图窗口；
- 批量进行世界坐标到栅格坐标转换；
- 减少 Python 与 C++ 之间的数据拷贝；
- 将静态地图与动态障碍地图分层缓存。

### 3.3 性能采样缺陷

当前结果中：

```json
"cpu_avg": 0.0,
"cpu_max": 0.0
```

说明 CPU 采样没有真正生效。后续必须补充：

- 进程 CPU 平均值和峰值；
- 线程 CPU 占用；
- 内存峰值与长期增长趋势；
- P50/P95/P99 延迟；
- 仿真时间与真实时间比例；
- 单帧超时次数。

## 4. 已知失败模式与专项回归

`experiment_results/failure_analysis/failure_cases.json` 已记录以下失败模式：

### 4.1 AMCL 绑架问题

现象：粒子枯竭、定位误差持续上升、机器人失去定位。

建议：

- 使用 KLD-Sampling 自适应粒子数；
- 加入随机粒子注入；
- 检测定位误差突变后触发全局重定位；
- 融合 IMU 和里程计；
- 必要时使用多假设定位。

### 4.2 狭窄通道卡死

可能原因：

- Costmap 膨胀半径过大；
- 局部规划器振荡；
- 全局路径穿越实际不可通行区域；
- 机器人尺寸大于通道有效宽度。

建议：

- 通过机器人真实外形计算可通行宽度；
- 增加窄通道专用参数；
- 降低速度并提高规划迭代次数；
- 卡死超过阈值后自动后退并重规划；
- 在通道入口增加中间路径点。

### 4.3 动态障碍物碰撞

可能原因：

- 线性预测不能应对非线性运动；
- 局部规划周期过长；
- 动态障碍物安全距离过小；
- CBF 没有作为最终安全层；
- 动态障碍物历史轨迹清除过快。

建议：

- 缩短局部规划周期到约 `0.1s`；
- 增大动态障碍物安全距离；
- 采用“预测层 + 等待层 + 排斥层 + 制动层”；
- 对高速障碍物启用保守模式；
- 将 CBF 作为最后一道安全过滤器。

## 5. 统一指标定义

后续所有实验都应至少记录以下指标：

```text
collision_events       # 连续碰撞只计一次
collision_frames       # 处于碰撞状态的帧数
near_miss_events       # 一次接近事件只计一次
minimum_clearance      # 全程最小安全距离
path_length            # 实际路径长度
goal_success_rate      # 到达目标比例
timeout_rate           # 超时比例
coverage               # 地图覆盖率
localization_rmse      # 定位误差 RMSE
planning_time_p50      # 规划耗时 P50
planning_time_p95      # 规划耗时 P95
planning_time_p99      # 规划耗时 P99
progress_per_second    # 单位时间有效进展
cpu_time_per_frame     # 单帧计算成本
```

推荐每次实验生成统一 JSON：

```json
{
  "experiment_id": "20260809_scene01_seed003",
  "algorithm": "proposed",
  "scenario": "dynamic_obstacles",
  "seed": 3,
  "sim_dt": 0.0333,
  "frames": 108000,
  "success": true,
  "metrics": {
    "coverage": 0.0,
    "collision_events": 0,
    "near_miss_events": 0,
    "localization_error_rmse": 0.0,
    "planning_time_p95_ms": 0.0
  },
  "artifacts": {
    "trajectory": "trajectory.jsonl",
    "raw_log": "run.log",
    "config": "config.yaml"
  }
}
```

## 6. 工程结构完善建议

当前仓库包含大量临时脚本、截图、日志、多个版本实验结果和构建产物。建议逐步调整为：

```text
src/
    ros2_packages/

cpp_src/
    puppy_nav_core/
    sim/
    tests/

python/
    experiments/
    analysis/
    tools/

configs/
    scenes/
    algorithms/
    hardware/

data/
    raw/
    processed/

results/
    demo/
    official/

docs/
    architecture/
    experiments/
    operations/
```

同时：

- 将临时脚本移动到 `tools/legacy/`；
- 将截图移动到 `artifacts/screenshots/`；
- 将日志移动到 `artifacts/logs/`；
- 将实验输出加入 `.gitignore`；
- 只保留一套主版本的消融脚本；
- 明确 C++ 为运行时主实现，Python 负责实验与分析；
- 合并或废弃旧版 `nav_core`；
- 拆分过大的 `simulation.h` 和 `amcl.cpp`。

## 7. 测试体系

当前 Python 测试结果为：

```text
13 passed
```

但仍需增加以下测试层次：

### 7.1 单元测试

- OccupancyGrid 坐标转换；
- Costmap 膨胀；
- A* 不可达目标；
- AMCL 权重归一化；
- 粒子重采样；
- 速度限制；
- CBF 安全约束；
- 碰撞事件去重。

### 7.2 集成测试

- 激光 → 地图 → Costmap；
- AMCL → 全局规划；
- 全局路径 → 局部跟踪；
- 动态障碍 → 避障；
- 失败检测 → Recovery Manager。

### 7.3 场景回归测试

至少固定以下场景：

```text
open_field
small_room
office
narrow_passage
dynamic_crossing
kidnapped_robot
```

### 7.4 长时间稳定性测试

建议至少执行 1 小时、6 小时和 24 小时测试，检查：

- 内存是否持续增长；
- 线程和句柄是否泄漏；
- 日志是否失控；
- 定位误差是否漂移；
- 状态机是否卡死；
- 规划周期是否逐步变慢。

## 8. 分阶段实施路线

### P0：一周内完成

1. 修复 `compare_algorithms.py` 的日志解析失败处理；
2. 禁止 `-1`、`None`、`NaN`、`Infinity` 进入统计；
3. 保存每次 trial 的随机种子和完整配置；
4. 明确标注并隔离所有 `mode: demo` 结果；
5. 修复 CPU 监控；
6. 增加 `status`、`failure_reason` 和 `missing_fields`；
7. 固定场景、初始位姿、障碍物轨迹和仿真步长。

### P1：两周内完成

1. 优化 AMCL 粒子更新和距离场缓存；
2. 将 OccupancyGrid 改为增量更新；
3. 增加 P95/P99 延迟监控；
4. 建立 C++ `ctest` 自动执行流程；
5. 增加绑架机器人、窄通道和动态碰撞回归测试；
6. 统一 C++ 与 Python 的参数配置。

### P2：后续重构

1. 明确 C++ 为运行时主实现；
2. Python 只负责实验和数据分析；
3. 合并或废弃旧版 `nav_core`；
4. 建立统一实验数据 schema；
5. 增加 CI：构建、单测、场景回归和指标校验；
6. 清理根目录临时脚本和生成物。

## 9. 最终结论

工程当前最需要完善的不是继续堆叠 GNN、RL 或更多避障算法，而是先建立可信的实验和工程基础：

1. 确保每一次实验可复现；
2. 确保指标定义一致且解析失败可见；
3. 确保无效数据不会进入统计；
4. 优先优化 AMCL 和 OccupancyGrid 性能；
5. 针对已知失败模式建立回归测试；
6. 收敛 C++、Python、ROS2 三套代码的职责边界。

只有完成这些工作，后续的算法对比、消融实验和论文结论才具有稳定性和说服力。
