# PuppyPi 四足机器人导航 UE5 仿真交接文档

**文件名：** `jiaojie20260810.md`  
**工程目录：** `E:\puppyfangzhen`  
**UE 工程目录：** `D:\puppy_ue`  
**文档日期：** 2026-08-10

## 1. 当前工程状态

本项目目前包含三条主要验证链路：

1. ROS2 / CoppeliaSim 导航链路；
2. 自研 C++ 导航核心与独立仿真器；
3. UE5 场景 + `nav_ue_bridge.exe` TCP 锁步仿真链路。

当前重点是 UE5 闭环仿真。UE 侧通过 TCP 向 Bridge 发送 LiDAR、Ground Truth、行人和碰撞数据，Bridge 使用现有 C++ 导航核心计算速度指令，再返回 `CMD_VEL`、调试位姿、调试路径和 `STEP_ACK`。

## 2. UE5 闭环架构

```text
UE5 HomeMap
  └─ PuppyRobotPawn
      ├─ PuppyLiDARComponent
      ├─ PuppyTcpServer
      ├─ PuppyPi 风格可视模型
      └─ 30Hz 锁步移动
            │ TCP localhost:7777
            ▼
nav_ue_bridge.exe
  ├─ OccupancyGrid
  ├─ Costmap
  ├─ AMCL
  ├─ A*
  ├─ 动态行人避障
  └─ 巡航指标统计
```

## 3. 本轮已完成内容

### 3.1 锁步协议修复

修改文件：

- `cpp_src/bridge/nav_ue_bridge.cpp`
- `cpp_src/bridge/protocol.h`
- `D:\puppy_ue\Source\PuppyNav\PuppyRobotPawn.cpp`
- `D:\puppy_ue\Source\PuppyNav\PuppyTcpServer.cpp`

已完成的修复：

- Bridge 必须收齐 `LIDAR + GROUND_TRUTH + PED_STATE` 后才处理一帧；
- 修复之前 Bridge 比 UE 快约 1.5 倍的问题；
- TCP 发送增加 `send_all`，支持分片发送；
- UE 端 `SendFrame` 支持循环发送完整消息；
- 删除 watchdog 中重复发送同一传感器帧的逻辑；
- UE 每次收到 `STEP_ACK` 只推进一次；
- 锁步移动固定使用 `1/30s`，不再使用渲染 Tick 的实际 `DeltaTime`；
- A* 多级规划失败时增加短直达路径 fallback 和诊断日志。

### 3.2 PuppyPi 风格机器人外观

修改文件：

- `D:\puppy_ue\Source\PuppyNav\PuppyRobotPawn.cpp`

当前模型由 UE 内置基础几何组成，不依赖外部网格文件，包含：

- 白灰色机身；
- 圆润上壳；
- 黑色四条腿；
- 四个关节球；
- 四个脚掌；
- 蓝色前部传感器；
- 黑色摄像头镜头；
- 橙色背部识别条。

这套模型是 PuppyPi 风格近似模型，不是真实 PuppyPi 官方 FBX/GLB 网格。如果后续获得真实模型，可再替换成 Skeletal Mesh 或 Static Mesh，导航接口无需变化。

### 3.3 家庭场景布局

修改文件：

- `cpp_src/bridge/ue_auto_setup.py`
- `D:\puppy_ue\Content\Maps\HomeMap.umap`

场景重建脚本已生成：

- 地板；
- 家庭墙体；
- 客厅沙发、靠背和坐垫；
- 卧室床架、床垫和枕头；
- 书桌、餐桌和桌腿；
- 书柜、储物柜；
- 灯光、天空和雾效；
- 5 个动态行人；
- 巡航目标点。

场景视觉家具关闭了额外碰撞，导航碰撞区域仍由原有场景障碍盒体承担，因此不会改变原有导航栅格和指标定义。

场景生成日志确认：

```text
Created 42/42 collision regions with furniture visuals
Auto Setup Complete!
```

### 3.4 默认 GameMode 修复

修改文件：

- `D:\puppy_ue\Config\DefaultEngine.ini`
- `cpp_src/bridge/ue_auto_setup.py`

默认 GameMode 已固定为 C++ 类：

```ini
GlobalDefaultGameMode=/Script/PuppyNav.PuppyGameMode
```

这样不会再依赖已经删除或过期的 `BP_PuppyRobotPawn` 资产引用。

### 3.5 截图脚本修复

修改文件：

- `scripts/run_sim_ue.ps1`

当 Bridge 日志为空或尚未创建时，脚本现在会输出提示，不会因为 `[regex]::Matches()` 收到空值而报错。

## 4. 已验证结果

### 4.1 C++ / UE 构建

Bridge 构建命令：

```powershell
$cmake = "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe"
& $cmake --build cpp_src/bridge/build --config Release --target nav_ue_bridge -- /m:2
```

UE 构建命令：

```powershell
& "C:\Program Files\Epic Games\UE_5.3\Engine\Binaries\DotNET\UnrealBuildTool\UnrealBuildTool.exe" `
  puppy_ueEditor Win64 Development `
  -Project="D:\puppy_ue\puppy_ue.uproject" `
  -WaitMutex -NoHotReloadFromIDE
```

最近构建均成功。

### 4.2 指标测试

```text
33 passed
```

对应测试：

```powershell
pytest -q test_metrics_schema.py --disable-warnings --maxfail=1
```

### 4.3 UE 1800 帧闭环

最终 1800 帧回归结果：

```text
frames=1800
rounds=3
targets=21
rooms=5
gt_distance=52.31m
motion_frames=1650
stuck_ratio=8.3%
collisions=0
ue_collision_events=0
astar_rate=100.0%
avg_err=0.201m
max_err=0.421m
```

另一次新版场景抓图运行结果：

```text
frames=1800
rounds=3
targets=21
rooms=5
gt_distance=52.82m
collisions=0
astar_rate=100.0%
avg_err=0.202m
max_err=0.422m
```

说明：UE 与 Bridge 已经能够持续锁步运行，不再出现原先 Frame 400 附近断链或重复推进问题。

## 5. 截图位置

新版 PuppyPi + 家庭布局截图目录：

```text
screenshots/ue_runs/
```

当前截图：

```text
screenshots/ue_runs/R_PUPPYPI_HOME_20260811_shot_1_053534.png
screenshots/ue_runs/R_PUPPYPI_HOME_20260811_shot_2_053543.png
screenshots/ue_runs/R_PUPPYPI_HOME_20260811_shot_3_053551.png
```

运行脚本：

```powershell
& powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run_sim_ue.ps1 `
  -Frames 1800 `
  -Shots 4 `
  -ShotInterval 8 `
  -RunId R_NEXT `
  -ResX 1280 `
  -ResY 720
```

脚本会先把截图保存到 `screenshots/`，运行结束后可移动到 `screenshots/ue_runs/` 归档。

## 6. 手动启动方式

### 6.1 启动 Bridge

```powershell
$bridge = "E:\puppyfangzhen\cpp_src\bridge\build\Release\Release\nav_ue_bridge.exe"
$scene = "E:\puppyfangzhen\config\scene_home.json"
& $bridge --port 7777 --frames 1800 --scene $scene
```

### 6.2 启动 UE5

```powershell
& "C:\Program Files\Epic Games\UE_5.3\Engine\Binaries\Win64\UnrealEditor.exe" `
  "D:\puppy_ue\puppy_ue.uproject" `
  "/Game/Maps/HomeMap" `
  -game -windowed -ResX=1280 -ResY=720 -log
```

必须先启动 Bridge，再启动 UE5。UE 默认连接：

```text
127.0.0.1:7777
```

## 7. 当前已知问题

1. PuppyPi 当前是 UE 基础几何拼装模型，不是真实官方网格。
2. 部分复杂目标会触发 `PLAN-FALLBACK`，但当前不会导致停滞，巡航仍可继续。
3. `scene_home.json` 中部分目标因安全边界会被 Bridge 过滤，实际巡航目标数可能少于 JSON 原始目标数。
4. 场景重建脚本使用了部分 UE5 已标记 deprecated 的 `EditorLevelLibrary` API，但当前 UE5.3 仍可正常执行。
5. 场景重建时 `PlayerStart` 的旧 API `EditorLevelLibrary.move_actor` 在当前环境不可用；机器人 `BeginPlay` 会通过 `FIX-SNAP` 自动回到 `scene_home.json` 初始位姿，因此不影响当前仿真。
6. 真实 PuppyPi 模型如果后续导入，需要重新检查模型原点、比例、碰撞体和摄像头/LiDAR 相对位置。

## 8. 后续建议

按以下顺序继续：

1. 获取真实 PuppyPi `.fbx`、`.glb` 或 `.uasset` 模型并导入 `D:\puppy_ue\Content\PuppyPi\`；
2. 保留当前 `PuppyRobotPawn` 的碰撞胶囊和传感器组件，只替换视觉组件；
3. 用真实模型的包围盒重新校准机器人半径和高度；
4. 将家庭家具进一步替换成真实沙发、床、桌椅和柜体资源；
5. 增加 UE 画面中导航路径、AMCL 位姿和动态行人标识；
6. 做 3600 帧、10800 帧和 1 小时长时间回归；
7. 将最终结果复制到 `official_results`，与 demo 结果严格分离。

## 9. 交接检查清单

- [x] Bridge 可以编译；
- [x] UE `PuppyNav` 模块可以编译；
- [x] UE/Bridge 30Hz 锁步已修复；
- [x] A* 成功率统计正常；
- [x] UE 1800 帧巡航通过；
- [x] 碰撞数为 0；
- [x] PuppyPi 风格外观已加入；
- [x] 家庭布局已重新生成；
- [x] 截图已归档到 `screenshots/ue_runs/`；
- [x] `test_metrics_schema.py` 33 项测试通过；
- [ ] 真实 PuppyPi 网格模型尚未导入；
- [ ] 家具真实资产尚未导入；
- [ ] 1 小时 UE 长时间回归尚未重新执行。
