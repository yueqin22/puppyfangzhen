# Puppy 机器狗自主导航系统 — 虚幻引擎（UE5）仿真项目规划书

**文档编号**: UE-PLAN-20260803
**编写日期**: 2026-08-03
**依据文档**: HANDOFF_FINAL.md（v3.2.18i）、wanshan2026080201.md（完善任务书）
**面向对象**: 承接本项目的 UE / C++ 开发工程师
**文档性质**: 项目规划书 —— 含环境核查结论、架构决策、里程碑、验收标准与风险登记册，可直接据以开工。

---

## 1. 项目背景与目标

### 1.1 现状

- 导航系统（AMCL 定位 + A* 规划 + RVO/flee 避障）已在自研 C++ 仿真器上完成验证：**10 种子 1 小时零碰撞**，核心指标全部达标（详见 HANDOFF_FINAL.md 第 6 节）。
- 现有 C++ 仿真器是**纯数值仿真**：无渲染画面，场景为代码内定义的 BBox 集合，行人做运动学弹墙运动，LiDAR 为 72 射线几何求交。
- ROS2 全链路在 WSL2 侧，属旧版骨架（见 wanshan2026080201.md 任务 5.3）。

### 1.2 为什么引入虚幻引擎

1. **可视化调试**：粒子云、规划路径、代价地图、避障过程在 3D 场景中实时可见，定位"为什么撞/为什么飘"的效率远高于读日志。
2. **演示与汇报**：照片级室内场景中的机器狗自主导航，是真机立项、验收、展示的最直观材料。
3. **传感器真实性**：UE 的射线检测在真实网格几何上进行，比 BBox 几何求交更接近真实 LiDAR 表现（多径、边缘、家具腿等细碎遮挡）。
4. **数字孪生预备**：与真机部署（HANDOFF 长期目标）共用"场景—传感器—导航"接口，将来可平滑切换真机数据源。

### 1.3 项目目标（验收导向）

- **G1**：在 UE 场景中复刻 10m×8m 家居环境（7 房间 + 家具 + 5 动态行人），与 C++ 仿真器**几何等价**（LiDAR 扫描级校验）。
- **G2**：现有 C++ 导航核**不改算法**接入 UE 仿真闭环，机器人在 UE 画面中按导航自主巡逻。
- **G3**：UE 平台 10 种子回归结果与 C++ 基线对齐：零碰撞、A* 成功率 ≥97%、定位误差差值在可解释范围内。
- **G4**：全程在**本机**（配置见第 2 节）可流畅运行，实验模式 ≥30 FPS。

### 1.4 明确非目标（防止范围蔓延）

- ❌ 不替代现有 C++ 仿真器的**精度基准与批量回归**地位（UE 物理跨机器不确定，见 3.4 确定性策略）。
- ❌ 不做四足步态物理仿真（腿部动力学对导航结论无影响，仅第二阶段做表现层动画）。
- ❌ 不在本期接入真机、不做 ROS2 全链路迁移（属 wanshan2026080201.md 的 P3 范畴）。
- ❌ 不升级 UE 版本（锁定已安装的 5.3）。

---

## 2. 本机环境核查结论（2026-08-03 实测）

| 项目 | 实测值 | UE5.3 需求评估 |
|------|--------|----------------|
| 引擎 | **UE 5.3 已安装** `C:\Program Files\Epic Games\UE_5.3`（Epic Launcher 在位） | ✅ 无需下载安装 |
| CPU | Intel i5-3450，4 核 4 线程 @3.10GHz | ⚠️ 低于推荐配置：编辑器可用，首次着色器编译很慢（数小时级），需预留时间 |
| 内存 | 12 GB | ⚠️ 偏紧：编辑器约占用 6~8GB，运行时须关闭浏览器等大户 |
| GPU | NVIDIA GTX 1650 SUPER，4GB 显存，驱动 32.0.15.9186 | ⚠️ 入门可用：**必须关闭 Lumen/Nanite**，画质中低，纹理预算受 4GB 限制 |
| 磁盘 | C: 余 82GB / D: 余 95GB / E: 余 29GB | ⚠️ UE 工程放 **D:**（E: 余量不足，DerivedDataCache 会持续增长） |
| 编译器 | VS2022（MSVC 14.44）已装 | ✅ 需确认含"使用 C++ 的游戏开发"工作负载（M0 核查项） |
| 显示 | 存在向日葵/ToDesk/GameViewer 三个**虚拟显示适配器** | ⚠️ 本机常被远程访问：须在 Windows 图形设置中为 UnrealEditor 指定 **GTX 1650 高性能**，避免渲染落到虚拟适配器上黑屏/软渲染 |

**结论：本机可承担该项目，但属于"够用偏紧"，性能预算必须写进工程设置（见附录 A），不要按默认画质跑。**

---

## 3. 总体技术方案

### 3.1 候选架构对比

| 方案 | 描述 | 优点 | 缺点 | 结论 |
|------|------|------|------|------|
| **A. UE 前端 + 导航核外置** | UE 负责场景/物理渲染/LiDAR/真值输出；现有 C++ 导航核（AMCL+A*+RVO）作为独立进程，经本地桥接通信 | 保住 v3.2.18i 全部回归资产；导航算法零改动；与现有数据流同构；两边可独立调试 | 需开发桥接层；跨进程时序要设计好 | ✅ **采用** |
| B. ROS2 全链路 | UE + ROSIntegration/rclUE 插件，对接 WSL2 的 ROS2 Humble | 贴近真机链路 | ROS2 侧本身是旧版待治理；WSL2↔Windows 网络与插件在 Windows 下坑多；偏离当前主验证平台 | ❌ 否决（并入 P3 长期项） |
| C. 导航核移植进 UE 插件 | 把 puppy_nav_core 编为 UE 模块，进程内调用 | 无 IPC 开销；单进程 | 丧失独立批量回归能力（UE 启动重）；构建耦合；违背"先固成果"原则 | ❌ 否决 |

**决策：方案 A。** 理由：本项目最宝贵的资产是经过 10 种子验证的导航核与其回归方法，任何架构都不应牺牲它。方案 A 与现有仿真数据流（HANDOFF 第 3.2 节）完全同构——只是把"simulation.h 的场景/物理/LiDAR"换成"UE 世界"，导航栈上下游接口不变。

### 3.2 系统数据流

```
┌──────────────────────────── UE 5.3（D:\puppy_ue）──────────────────────────┐
│ 场景（由 scene_home.json 生成）  行人脚本运动   Chaos 碰撞查询               │
│      │                                                                     │
│  LiDAR 组件（72 射线 LineTrace）   真值位姿   碰撞事件   行人状态            │
│      └──────────────┬──────────────────────────────────────────┘          │
└─────────────────────┼──────────────────────────────────────────────────────┘
                      │  Bridge（localhost TCP，30Hz，二进制小端协议）
┌─────────────────────┼──────────────────────────────────────────────────────┐
│              nav_ue_bridge（新增，E:\puppyfangzhen\cpp_src\bridge）          │
│   解码 LiDAR/真值 → OccupancyGrid → Costmap → A* → 路径跟随 → flee/RVO      │
│   （即现有 nav_bridge.h 的传输层替换：数据源从 simulation.h 换成 socket）    │
│      └──────────────┬──────────────────────────────────────────┘          │
└─────────────────────┼──────────────────────────────────────────────────────┘
                      │ 速度指令 (vx,vy,wz) / 调试数据（路径、粒子云、估计位姿）
                      ▼
              UE 机器人运动学执行 + 调试可视化（DebugDraw）
```

### 3.3 桥接协议设计（bridge_protocol v1）

- **传输**：localhost TCP，长度前缀帧（4B LE length + payload），二进制小端；控制通道与数据通道同连接复用。
- **频率**：仿真步进 30Hz 与现有仿真器一致；UE 侧用**固定步长**（`FixedFrameRate=30`）驱动，导航进程逐帧同步（UE 发一帧传感数据 → 等导航回一帧指令 → 推进一帧），保证 lockstep 可复现。
- **消息集**：
  | 方向 | 消息 | 载荷要点 |
  |------|------|----------|
  | 控制 | `RESET` | seed、场景名、初始位姿 |
  | 控制 | `STEP_ACK / PAUSE / SET_SPEED` | 帧同步与倍速 |
  | UE→Nav | `LIDAR` | 72×float 距离 + max_range 标记规则（与现有"跳过 max_range 光束"约定一致） |
  | UE→Nav | `GROUND_TRUTH` | x, y, yaw（供消融与误差评估，AMCL 正常流程不消费） |
  | UE→Nav | `PED_STATE` | 5×(x,y,vx,vy)（供 RVO/flee，同现有接口） |
  | UE→Nav | `COLLISION` | 碰撞事件（判定口径与现有仿真器一致：机器人圆盘与障碍相交） |
  | Nav→UE | `CMD_VEL` | vx, vy, wz |
  | Nav→UE | `DEBUG_POSE` | AMCL 估计位姿 + 置信度 |
  | Nav→UE | `DEBUG_PATH` | A* 路径折线 + 粒子云抽样（≤200 个粒子） |
- **兼容性**：消息带版本号；协议文档化于 `cpp_src/bridge/protocol.md`。

### 3.4 确定性与可复现策略（关键设计）

UE 的 Chaos 物理**不保证跨机器/跨次运行确定性**，而本项目一切结论建立在"固定种子可复现"之上，因此：

1. **行人与机器人运动不走物理模拟**：行人沿用现有运动学弹墙模型（simulation.h `update_pedestrian` 的 UE 移植版），机器人按 CMD_VEL 运动学积分，二者由同一种子驱动、固定步长推进。
2. **物理引擎只用于查询**：碰撞检测与 LiDAR 用 UE 的射线/重叠查询（确定性足够，几何固定），不开启刚体动力学。
3. **精度基准不搬家**：v3.2.18i 的 10 种子回归仍以 C++ 仿真器为准；UE 平台做**等价性回归**（M4），允许数值微差（几何求交实现不同），但结论性指标（碰撞数、成功率档位）必须一致。

### 3.5 场景单一事实来源

当前场景定义硬编码在 simulation.h（BBox 墙体/家具、房间坐标区间、巡逻点、行人出生参数）。**手工在 UE 里重搭场景必然漂移**，因此：

1. 从 simulation.h 抽取场景定义为 **`config/scene_home.json`**：墙体/家具 BBox 列表、房间多边形、巡逻目标点（PatrolTarget 8+ 个）、行人出生表、LiDAR 参数（72 线、max_range、安装高度）、机器人半径 0.35m、网格 100×80@0.1m。
2. C++ 仿真器改为**读 JSON**（与 wanshan2026080201.md 任务 2.4 配置化合并实施）；UE 侧用编辑器 Python 脚本（`ue_scene_builder.py`）从同一 JSON **自动生成**场景几何（BSP/静态网格实例 + 导航所需的碰撞体）。
3. **几何等价性自动校验**：在双方仿真器中于同一组采样位姿（≥500 个随机点+朝向）做 LiDAR 扫描对比，72 射线距离差均值 < 5cm、单线最大差 < 20cm 为合格；该校验脚本固化为回归项。

### 3.6 机器人表现层

- **第一阶段（功能）**：胶囊体代理（半径 0.35m、高度 0.4m）+ 朝向箭头。导航结论只依赖足迹，与外形无关。
- **第二阶段（演示）**：四足骨骼网格（可用 puppy_description 的 URDF 尺寸导模或用 Marketplace 四足模型）+ 简单程序化步行动画，仅表现层，不参与碰撞与传感。

---

## 4. 里程碑计划

> 总工期约 **6~8 周（单人全情投入）**；与 wanshan2026080201.md 的依赖：建议**先完成任务 1.1（CMake）与 1.4（回归流水线）**再开工 M2，否则 UE 侧改动无回归兜底。

### M0 环境验证与 Spike（第 1 周）

| # | 任务 | 验收标准 | 工时 |
|---|------|---------|------|
| 0.1 | 核查 VS2022 含"使用 C++ 的游戏开发"工作负载 + Win10/11 SDK | UE 可创建 C++ 工程并编译通过 | 0.5d |
| 0.2 | 在 D: 创建 `puppy_ue` 空白 C++ 工程，应用附录 A 全部性能设置 | 空场景编辑器稳定运行，Standalone 模式 ≥60FPS | 0.5d |
| 0.3 | Windows 图形设置指定 UnrealEditor/puppy_ue 使用 GTX 1650；验证远程桌面（向日葵/ToDesk）下渲染正常 | 远程会话中视口有画面、帧率达标 | 0.5d |
| 0.4 | 桥接 hello-world：UE 插件内 TCP server + C++ 控制台客户端，30Hz lockstep 回环 1000 帧 | 无丢帧、无超时，单向延迟 < 2ms | 2d |
| 0.5 | 首次全量着色器编译（留足机器时间，夜间执行） | 编译完成无 OOM；若 12GB 内存不足，分模块编译并加虚拟内存 | 机器时间 |

**M0 出口决策**：若 0.2/0.3 实测帧率不达标（Standalone < 30FPS），降级方案——降低分辨率至 540p、关阴影后再评；仍不达标则缩小演示目标（仅功能验证不做照片级演示），并在风险登记册记录。

### M1 场景数据化与复刻（第 1~2 周）

| # | 任务 | 验收标准 | 工时 |
|---|------|---------|------|
| 1.1 | 抽取 `config/scene_home.json`（含全部 BBox、房间、巡逻点、行人、传感器参数） | JSON 覆盖 simulation.h 现有场景全部要素，经 diff 核对无遗漏 | 1.5d |
| 1.2 | C++ 仿真器改为读 JSON（与任务书 2.4 合并） | 10 种子回归与 v3.2.18i 基线逐指标一致 | 1d |
| 1.3 | UE 编辑器 Python 脚本从 JSON 自动生成场景（墙体/家具/地面/碰撞体） | 一键生成；尺寸单位 cm 换算正确 | 2d |
| 1.4 | UE LiDAR 组件（72 射线 LineTrace，max_range 规则一致） | 单帧 72 线查询耗时 < 2ms | 1d |
| 1.5 | 几何等价性校验（3.5 节方法）并固化脚本 | 500 采样点：均值差 < 5cm，最大差 < 20cm | 1d |

### M2 导航闭环（第 3~4 周）

| # | 任务 | 验收标准 | 工时 |
|---|------|---------|------|
| 2.1 | 实现 `nav_ue_bridge`：协议 v1 全消息 + 与 nav_bridge.h 对接（数据源替换） | LIDAR/GROUND_TRUTH/CMD_VEL 全通 | 3d |
| 2.2 | UE 机器人运动学执行器（CMD_VEL 积分 + 碰撞查询 + 圆盘相交判定） | 与 C++ 仿真器运动模型一致 | 1.5d |
| 2.3 | 单种子（seed=8）1 小时等效闭环 | **零碰撞**，A* 成功率 ≥97%，avg_err 与基线差 < 30% | 2d |
| 2.4 | UE 侧调试可视化第一版：估计位姿 vs 真值轨迹、A* 路径 DebugDraw | 画面中可直观分辨定位漂移与重规划 | 1.5d |

### M3 动态行人与种子复现（第 5 周）

| # | 任务 | 验收标准 | 工时 |
|---|------|---------|------|
| 3.1 | 5 行人脚本化移植（弹墙/滑动逻辑、`create_pedestrians` 出生表、种子驱动） | 同种子下 UE 与 C++ 行人轨迹逐帧一致（容差 1mm） | 2d |
| 3.2 | flee/RVO 全链路在 UE 闭环中启用 | 多行人场景单种子零碰撞 | 1.5d |
| 3.3 | 种子复现测试：同 seed 跑 3 次，指标完全一致 | 3 次结果逐字段相同 | 0.5d |

### M4 等价性回归（第 6 周）

| # | 任务 | 验收标准 | 工时 |
|---|------|---------|------|
| 4.1 | UE 平台 10 种子 × 1 小时等效批量测试（脚本化无人值守） | **10/10 零碰撞**；A* 成功率 ≥97%；房间覆盖 ≥8 | 机器时间+1d |
| 4.2 | 与 v3.2.18i 基线的指标 diff 报告（碰撞/A*/avg_err/置信度/覆盖率） | 报告落盘 `docs/ue_equivalence_report.md`；差值均有解释 | 1d |
| 4.3 | 等价性校验纳入一键流水线（扩展现有 batch 回归） | 单命令同时跑 C++ 基线与 UE 回归并出对比表 | 1d |

### M5 可视化增强与演示（第 7~8 周，可按需裁剪）

| # | 任务 | 验收标准 | 工时 |
|---|------|---------|------|
| 5.1 | 粒子云、代价地图热区、行人意图（flee 方向）可视化 | 调试视图可开关 | 2d |
| 5.2 | 巡检视角/第三人称跟随/顶视图三机位 + 截图/录像脚本 | 输出 1080p 演示视频 1 段（≥2 分钟无碰撞巡逻） | 1.5d |
| 5.3 | （可选）四足外观模型 + 程序化步态，替换胶囊体 | 视觉替换，回归指标不变 | 2d |
| 5.4 | 演示场景打磨：灯光、材质（受 4GB 显存预算约束，1024 纹理为主） | Standalone 30FPS 以上 | 1d |

---

## 5. 工程规范

### 5.1 目录与仓库

```
E:\puppyfangzhen\            # 导航核与基准（现有，保持不动）
├── cpp_src\
│   ├── puppy_nav_core\      # 导航核心库（算法零改动）
│   ├── sim\                 # C++ 数值仿真器（保留为精度基准）
│   └── bridge\              # ★ 新增：nav_ue_bridge（协议+传输+main）
├── config\scene_home.json   # ★ 新增：场景单一事实来源
└── docs\ue_equivalence_report.md

D:\puppy_ue\                 # ★ 新增：UE 5.3 工程（勿放 E:，余量不足）
├── Content\Maps\Home.umap
├── Content\Generated\       # Python 脚本生成的场景资产
├── Source\puppy_ue\         # UE C++ 模块：LiDAR 组件/行人脚本/TCP server/运动执行器
├── Python\ue_scene_builder.py
└── Config\                  # 附录 A 的引擎设置
```

- UE 工程纳入 Git：配好 `.gitignore`（Binaries/Intermediate/DerivedDataCache/Saved），大资产用 Git LFS。
- 提交纪律：**UE 侧任何改动过等价性回归（M4 流水线）才可合入**；导航核改动仍过 C++ 基线回归，两边门禁独立。

### 5.2 与既有文档的关系

- 本规划书与 wanshan2026080201.md 并行执行：任务 1.1/1.4 是 M2 的前置；任务 2.4（配置化）与 M1.2 合并实施；场景 JSON 化后 HANDOFF_FINAL.md 第 2 节工程结构需回写更新。
- 若工期冲突，**优先级：C++ 基线稳固（P0） > UE M0~M2 > UE M3~M4 > UE M5**。

---

## 6. 风险登记册

| # | 风险 | 概率 | 影响 | 缓解措施 |
|---|------|------|------|---------|
| R1 | 本机性能不足（i5-3450/12GB/4GB 显存），编辑器卡顿或实验不达 30FPS | 中 | 高 | M0 先做帧率验证并设出口决策；附录 A 降画质；实验用 Standalone 而非 PIE；必要时锁 540p |
| R2 | 首次着色器全量编译在 4 线程 CPU 上耗时过长（可能 3~6 小时） | 高 | 中 | 安排在夜间；期间不中断；加内存分页文件防 OOM |
| R3 | 远程桌面会话渲染落到虚拟显示适配器导致黑屏/软渲染 | 中 | 中 | M0.3 强制指定独显；保留物理显示器会话方案 |
| R4 | UE 场景与 C++ 场景几何漂移，指标差异无法归因 | 中 | 高 | 3.5 节单一事实来源 + 500 点 LiDAR 等价校验作为硬门禁 |
| R5 | 跨进程时序/丢帧导致不可复现 | 中 | 高 | lockstep 帧同步协议；M3.3 种子复现测试三连跑 |
| R6 | 把 UE 当成新基准，悄悄架空 C++ 回归 | 低 | 高 | 制度层面明确：C++ 基线为唯一精度基准，UE 只做等价性对齐 |
| R7 | E: 盘空间不足（余 29GB）拖累构建与缓存 | 中 | 中 | UE 工程与 DDC 放 D:；E: 只留源码与文档 |
| R8 | 范围蔓延（步态物理、ROS2、真机被提前卷入） | 中 | 中 | 1.4 非目标清单；超范围需求一律另立项 |

---

## 7. 总验收对照表

| 目标 | 验收关键点 | 状态 | 实际结果 |
|------|-----------|------|---------|
| G1 场景等价 | 500 点 LiDAR 校验：均值差 <5cm、最大差 <20cm | ☐ | |
| G2 导航闭环 | seed=8 一小时零碰撞，画面可见自主巡逻 | ☐ | |
| G3 等价回归 | UE 10 种子零碰撞、A* ≥97%、覆盖率 ≥8，diff 报告落盘 | ☐ | |
| G4 本机可跑 | 实验模式 ≥30FPS（Standalone，附录 A 设置下） | ☐ | |
| M0~M5 | 各里程碑出口标准逐项打勾 | ☐ | |

---

## 附录 A：本机 UE 5.3 工程性能设置清单（M0 强制执行）

**项目设置（Project Settings）**
- Rendering → Dynamic Global Illumination: **None**（关 Lumen）
- Rendering → Reflection Method: **None / Screen Space**
- Rendering → Nanite: **关闭**（4GB 显存 + 简单几何无收益）
- Anti-Aliasing: FXAA（TAA/TSR 更耗）
- Frame Rate: `t.MaxFPS=30`（实验模式锁定，与仿真步长一致）

**DefaultEngine.ini 建议项**
```
[/Script/Engine.RendererSettings]
r.DynamicGlobalIlluminationMethod=0
r.ReflectionMethod=1
r.Lumen.TranslucencyReflections.FrontLayer.Enable=0
r.Shadow.Virtual.Enable=0
r.TextureStreaming=1
r.Streaming.PoolSize=2000
[/Script/WindowsTargetPlatform.WindowsTargetSettings]
DefaultGraphicsRHI=DefaultGraphicsRHI_DX12
```
（若 DX12 在 1650S 上不稳，回退 `DefaultGraphicsRHI_DX11`。）

**Scalability（实验运行时）**：Overall **Medium-Low**；阴影 Low；后处理 Low；视口/独立进程分辨率 **1280×720**。

**系统层**
- Windows 图形设置：`UnrealEditor.exe` 与打包后的 `puppy_ue.exe` → 高性能（GTX 1650）。
- 运行期间关闭浏览器/IM；虚拟内存分页文件 ≥ 24GB（防着色器编译 OOM）。
- DerivedDataCache 指向 D: 盘。

---

*本规划书基于 2026-08-03 对本机的实测核查（UE 5.3 已装、i5-3450/12GB/GTX 1650S 4GB、C:82G/D:95G/E:29G 余量、存在远程桌面虚拟显示适配器）与 HANDOFF_FINAL.md（v3.2.18i）编写。架构核心决策：UE 作传感器与可视化前端、导航核外置经桥接闭环，C++ 数值仿真器保留为唯一精度基准。*
