# MiniCPM-RobotTrack 应用到 Puppy 项目实施计划

> 编制日期：2026-08-28  
> 上游项目：OpenBMB/MiniCPM-Robot  
> 本地源码：`third_party/MiniCPM-Robot`  
> 当前下载提交：`f666593066641d2c0cef06bc17bfb1e954547674`

## 1. 目标与范围

将 MiniCPM-Robot 中的端侧视觉跟踪与导航模型 MiniCPM-RobotTrack，逐步接入当前 Puppy 四足机器人仿真/ROS2 系统，实现以下能力：

1. 接收自然语言任务，例如“巡视后院并检查是否有可疑物品”。
2. 使用机器人前置 RGB 摄像头获取连续画面。
3. 由 MiniCPM-RobotTrack 根据指令和视觉观测生成跟踪/导航意图及速度建议。
4. 通过安全适配层转换为当前系统的 `/cmd_vel_safe` 或 `/cmd_vel`，复用已有运动控制、限速、超时停车和故障处理机制。
5. 在仿真中先验证“视觉任务—导航任务—运动控制”的闭环，再部署到 Unitree Go2 等实机。

本阶段不直接把大模型接入电机控制器，也不绕过现有 `safety_manager`、`motion_adapter`、Nav2 和急停链路。

## 2. 已确认的上游能力与约束

- 上游跟踪模块位于 `third_party/MiniCPM-Robot/MiniCPM-RobotTrack`。
- 上游提供 `go2_runtime.py`、`go2_runtime.yaml` 和 Go2 部署文档，可作为端侧运行参考。
- 上游 Go2 部署文档的验证组合为 Go2 EDU、Jetson Orin NX 16GB、D435i、ROS 2 Humble；Go2 Air/Pro、Orin Nano、Orin NX 8GB 及其他 RealSense 型号不应直接宣称兼容。
- 上游默认运行模式为 `dry-run`，实控模式需要显式确认；默认速度上限为 `vx=0.15 m/s`、`wz=0.30 rad/s`，应保留这一安全策略并由现场测试逐步放宽。
- 上游运行时默认读取 Go2 前置相机 RGB，不依赖 depth 或 IMU；当前 Puppy 仿真主要已有 LiDAR `/scan`，因此需要补充相机图像链路。
- 模型权重、ONNX 文件和 TensorRT engine 不随源码提交，应通过独立资产下载/部署流程管理。

## 3. 当前项目对接点

### 3.1 可复用的现有接口

| 能力 | 当前接口/位置 | 接入策略 |
|---|---|---|
| 速度控制 | `/cmd_vel`、`/cmd_vel_safe` | 模型输出先进入安全适配节点，不直接控制硬件 |
| 仿真运动 | `src/puppy_bringup/scripts/coppelia_bridge.py` | 复用其 `/cmd_vel` 输入及超时停车 |
| 里程计 | `/odom` | 用于速度执行结果、轨迹和延迟评估 |
| 激光雷达 | `/scan` | 继续作为独立安全感知/避障来源，不由视觉模型替代 |
| 点到点导航 | `navigate_to_pose` Action | 由任务编排层在需要时调用 |
| 巡逻任务 | `/mission/command`、`patrol_route` | 将自然语言任务解析为巡逻/检查任务 |
| 运动适配 | `src/puppypi_adapter/puppypi_adapter/motion_adapter_node.py` | 作为最终速度限幅、斜坡、超时和平台适配层 |
| 机器人状态 | `puppypi_adapter` 健康状态接口 | 作为模型运行前置条件和失联降级依据 |

### 3.2 需要新增的组件

建议新增独立 ROS2 包 `puppy_minicpm_robot`，避免修改上游源码和现有导航核心：

- `minicpm_track_node`：管理模型进程/推理客户端、接收指令和图像、发布推理状态。
- `vision_bridge_node`：统一仿真相机、USB/RealSense 相机和 Go2 VideoClient 的图像格式、时间戳及 QoS。
- `mission_grounder_node`：将自然语言任务转换为结构化任务，如巡逻区域、目标类别、检查时长和返回点。
- `track_cmd_adapter_node`：将上游 waypoint/速度意图转换为 `geometry_msgs/Twist`，并执行限速、死区、平滑、失联停车。
- `minicpm_robot_msgs`：如现有接口不足，新增最小化的任务、推理状态、目标置信度和安全状态消息；优先复用标准 ROS2 消息。
- 配置文件：模型路径、推理频率、图像尺寸、指令、速度上限、失联超时、仿真/实机模式和日志目录。

## 4. 分阶段实施步骤

### 阶段 A：源码与环境基线

1. 固定上游源码版本，保留 `third_party/MiniCPM-Robot` 作为只读第三方目录；后续本地修改放在新 ROS2 包或补丁目录。
2. 阅读并执行上游 `MiniCPM-RobotTrack` 的依赖检查、资产下载和模型加载自检。
3. 在独立 Python 环境验证 CPU/GPU 推理、模型输入尺寸、输出字段、推理耗时和显存占用。
4. 不把模型权重、ONNX engine、视频和运行日志纳入 Git；在项目文档中记录下载来源、校验值和目录约定。

**阶段出口：** 能在本机用一帧图片和一条指令完成一次离线推理，并保存结构化输出。

### 阶段 B：补齐 Puppy 相机链路

1. 检查当前 URDF、Gazebo/CoppeliaSim 是否已有 RGB 相机；若没有，新增 `camera_link`、相机传感器和 ROS2 图像发布。
2. 统一输出建议为 `/camera/color/image_raw` 和 `/camera/color/camera_info`，编码优先 `sensor_msgs/Image` + `cv_bridge`。
3. 为仿真图像增加可重复场景、固定帧率、时间戳和录包能力；为实机保留 Go2 VideoClient/RealSense RGB 适配入口。
4. 在不启动模型的情况下验证图像频率、分辨率、QoS、丢帧、时间延迟和相机坐标系。

**阶段出口：** `ros2 topic hz /camera/color/image_raw` 稳定，图像能在 RViz/录包中正确显示，仿真与实机使用同一 ROS2 图像接口。

### 阶段 C：封装 MiniCPM-RobotTrack 推理节点

1. 在 `puppy_minicpm_robot` 中通过进程内 Python API 或受控本地 HTTP 服务调用上游模型；第一版优先选择易调试的独立推理进程。
2. 输入包括图像、自然语言指令、当前时间戳和可选机器人状态；输出包括 waypoint/方向意图、线速度、角速度、置信度、推理耗时和错误码。
3. 增加模型热启动、单帧超时、连续错误计数、模型不可用状态和优雅退出；不在回调线程内阻塞 ROS2 主执行器。
4. 对输入图像执行统一裁剪/缩放，默认参考上游 `384` 输入尺寸；保留配置化选项，不把尺寸写死在节点中。
5. 输出只进入 `track_cmd_adapter_node`，不直接发布到硬件接口。

**阶段出口：** 在仿真回放图像上稳定输出结果，节点重启、模型超时和无图像时均能安全停车。

### 阶段 D：任务编排与导航融合

1. 将自然语言任务拆成“任务理解—区域/目标确认—导航/跟踪—检查—返回/结束”状态机。
2. 对“巡视后院”这类任务，第一版不要求模型直接理解整个地图；采用预先配置的 named goal 或 patrol route，模型负责视觉跟随、目标确认和局部意图修正。
3. 需要长距离移动时，由 `navigate_to_pose` 或现有 `patrol_route` 负责全局路径；MiniCPM-RobotTrack 作为视觉局部策略或目标跟踪器运行。
4. 将 `/scan`、代价地图、碰撞检测和 Nav2 恢复机制置于视觉速度之上；激光安全层检测到危险时强制覆盖模型速度。
5. 为任务增加取消、暂停、返航、低电量、通信丢失和人工接管状态。

**阶段出口：** 可在现有 home/small_room 场景中执行固定巡逻路线，并在图像中模拟目标发现后完成停止、记录和返回。

### 阶段 E：仿真验收与性能调优

建立至少四类场景：开阔区域、窄通道、动态行人/障碍物、目标遮挡或无目标。每类场景使用多组随机初始位姿和光照条件。

记录以下指标：

- 指令解析成功率、任务完成率、目标发现率和误报率。
- 图像端到端延迟、模型推理 P50/P95、控制发布频率和丢帧率。
- 路径长度、到达误差、碰撞/急停次数、最小障碍距离和停车响应时间。
- 模型不可用、相机断流、导航失败和人工接管时的恢复结果。

验收门槛建议：dry-run 全部通过；安全停车 100% 生效；模型异常不能导致持续运动；再根据实测延迟确定控制频率和速度上限。

### 阶段 F：Go2 实机部署

1. 首先确认硬件是否属于上游文档支持组合；若不是，标记为“待适配”，不得直接进入 live 控制。
2. 按上游文档准备 JetPack、CUDA/TensorRT、Jetson PyTorch、CycloneDDS、Unitree SDK2、相机和网络接口；确认版本兼容性。
3. 先执行相机预检、模型预热、`dry-run` 和静态站立测试，再执行低速、封闭场地、有人值守的 live 测试。
4. 实控必须具备实体/遥控急停、操作员确认、速度上限、命令超时停车和独立安全覆盖；严禁无人值守后台启动。
5. 通过 rosbag、运行日志和现场视频保留每次测试证据，逐步扩大速度、场景和任务复杂度。

**实机出口：** 在封闭测试区域连续完成预定巡逻任务，所有异常路径可停车或返航，且性能达到预设指标。

## 5. 推荐目录结构

```text
third_party/MiniCPM-Robot/          # 上游源码，只读
src/puppy_minicpm_robot/             # ROS2 适配包
  puppy_minicpm_robot/
    minicpm_track_node.py
    vision_bridge_node.py
    mission_grounder_node.py
    track_cmd_adapter_node.py
  config/minicpm_robot.yaml
  launch/minicpm_robot_sim.launch.py
  launch/minicpm_robot_go2.launch.py
  test/
models/minicpm_robot_track/          # 本地模型资产，不提交大文件
logs/minicpm_robot/                  # 推理/延迟/安全日志
```

## 6. 主要风险与应对

| 风险 | 应对措施 |
|---|---|
| x86 仿真环境无法复现 Jetson 性能 | 仿真只验证接口和行为；端侧延迟、显存和 TensorRT 单独在 Jetson 测试 |
| 上游模型输出与 Puppy 控制语义不一致 | 使用显式适配层、方向标定、速度比例和限幅测试，禁止直接透传 |
| 视觉误判导致持续运动 | 置信度阈值、无新帧停车、指令超时停车、LiDAR 安全覆盖和人工急停 |
| 相机视角/坐标系不一致 | 固定 `camera_link` 外参和图像裁剪规则，增加标定与回放测试 |
| 模型权重和依赖过大 | 使用外部资产目录、版本锁定、校验和及部署脚本，不提交二进制大文件 |
| 自然语言任务不可直接落地 | 第一版限制为结构化任务模板和预配置区域，后续再扩展开放词汇任务 |
| Go2 SDK、CycloneDDS、TensorRT 版本冲突 | 按上游支持矩阵部署，隔离 Python 环境，部署前执行 preflight，不混用系统库 |

## 7. 近期执行顺序

1. 为当前仿真增加/确认 RGB 相机话题。
2. 新建 `puppy_minicpm_robot` ROS2 包和最小配置文件。
3. 先实现离线图片推理适配，再实现 ROS2 图像订阅。
4. 实现 dry-run 速度输出、超时停车和安全层单元测试。
5. 接入现有巡逻状态机，完成固定路线仿真回放。
6. 在 Jetson/Go2 目标硬件上完成环境预检，再申请 live 实测。

## 8. 暂不实施事项

- 暂不修改 `third_party/MiniCPM-Robot` 上游源码。
- 暂不把模型权重、TensorRT engine 或外部 vendor wheel 下载进 Git 仓库。
- 暂不让自然语言模型直接替代 Nav2 全局规划、LiDAR 避障或电机安全控制。
- 暂不在未完成 dry-run、急停和人工接管验收前进行开放场地实控。
