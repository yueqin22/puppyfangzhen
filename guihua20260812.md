# PuppyPi 商用级自主导航与仿真平台建设规划

> **规划名称：** guihua20260812.md  
> **文档版本：** V1.0  
> **规划日期：** 2026-08-12  
> **适用项目：** `E:\puppyfangzhen`  及其对应的 UE5 工程 `D:\puppy_ue`  
> **文档定位：** 面向后续编程实施、系统重构、测试验收和产品化交付的总实施基线

---

## 0. 执行摘要

本规划的目标不是继续堆叠单个算法，而是把当前已经具备算法、仿真、ROS2、UE5、C++ 导航核心和 Python 分析工具的研究型工程，系统性建设为：

1. **可重复构建**：任何开发机、测试机和交付机都能按固定步骤构建；
2. **可验证运行**：每次提交都能自动执行单元测试、集成测试、2D 仿真和 UE5 3D 烟测；
3. **可观测诊断**：系统能回答“机器人为什么停、为什么撞、定位为什么漂、哪一帧开始异常”；
4. **可维护扩展**：场景、地图、目标点、障碍物、机器人参数和算法参数不再散落硬编码；
5. **可安全交付**：具备急停、通信丢失保护、限速、边界保护、故障降级和可审计日志；
6. **可商业化演进**：能够从仿真验证自然过渡到真实 PuppyPi 或同类四足平台，不依赖某一次演示成功。

当前项目已验证的基础能力包括：

- C++ AMCL、A*、Costmap、局部避障和恢复行为；
- Python 2D 仿真、自动巡航、结果验证和可视化；
- UE5 3D 场景、LiDAR、行人、TCP 桥接和 30Hz lockstep；
- 场景配置从 `config/scene_home.json` 加载；
- R28/R29/R30 验证表明，系统已经可以完成多目标巡航，A* 成功率达到 100%，定位误差进入可接受的仿真验证范围；
- 已补充碰撞事件去重和 `SIM_END` 仿真结束协议。

但距离商用标准仍有明显差距，主要集中在：

- 两套 UE 源码及工程副本存在分叉风险；
- 场景几何、目标点、初始位姿仍有部分镜像硬编码；
- 协议缺少版本、序列号、心跳、错误码和超时状态机；
- 测试以脚本和人工观察为主，缺少正式 CI、质量门禁和回归基线；
- 安全策略、故障降级、日志审计、参数版本和发布流程尚未产品化；
- 性能、资源、长时间稳定性和跨设备兼容性仍需系统验证。

本规划建议采用 **“先工程化、再稳定化、后产品化”** 的顺序，禁止在基础协议、配置源和测试体系不稳定时继续无序调参。

---

## 1. 项目定位与商业目标

### 1.1 产品定位

建议将本项目定义为：

> **面向室内四足机器人的自主导航仿真、算法验证与运行时控制平台。**

平台由以下产品能力组成：

- **算法运行时**：定位、地图、全局规划、局部避障、恢复行为；
- **仿真验证系统**：2D 快速仿真、UE5 物理/视觉仿真、传感器闭环；
- **场景与参数系统**：统一地图、障碍物、目标点、机器人和传感器配置；
- **诊断与测试系统**：自动运行、指标采集、截图、日志、报告和失败回放；
- **硬件适配层**：ROS2、PuppyPi SDK、Mock、Gazebo 与 UE5 接口；
- **开发者工具链**：配置校验、场景预览、轨迹可视化、回归比较和问题定位。

### 1.2 商业化目标

最终交付应达到以下目标：

| 目标类别 | 商用要求 |
|---|---|
| 可用性 | 安装后能够按文档完成构建、启动、验证，不依赖作者口头指导 |
| 稳定性 | 连续运行 8 小时无崩溃、无内存持续增长、无通信死锁 |
| 安全性 | 通信中断、定位失效、传感器异常、越界和碰撞风险时可安全停车或降级 |
| 可测试性 | 每次代码变更可自动给出通过/失败结论和失败证据 |
| 可追溯性 | 每个结果能追溯到源码版本、配置版本、场景版本、随机种子和运行环境 |
| 可扩展性 | 新增场景、机器人、传感器或规划器不需要复制大段代码 |
| 可维护性 | 模块边界、接口、错误处理和配置责任清晰 |
| 可交付性 | 可生成开发版、测试版、演示版和发布版，且版本内容可复现 |

---

## 2. 当前系统基线

### 2.1 目录与职责基线

| 目录/文件 | 当前职责 | 商用化要求 |
|---|---|---|
| `src/` | ROS2 工作空间及机器人相关包 | 作为机器人运行时和 ROS2 接口权威源码 |
| `cpp_src/puppy_nav_core/` | C++ 导航核心 | 作为定位、规划、Costmap、地图核心权威实现 |
| `cpp_src/bridge/` | UE5 TCP 桥接及 UE 模块参考源码 | 明确桥接协议和 UE 模块发布源 |
| `D:\puppy_ue\Source\PuppyNav` | 实际 UE5 工程源码 | 必须纳入版本管理或由脚本自动同步生成 |
| `config/` | 场景和参数配置 | 建成版本化、可校验、可迁移配置中心 |
| `scripts/` | 构建、运行、截图、验证脚本 | 统一为跨环境命令入口，禁止脚本互相隐式依赖 |
| `tests/` 或 `cpp_src/*/test/` | 单元及集成测试 | 建立分层测试目录和质量门禁 |
| `logs/`、`screenshots/` | 运行产物 | 明确临时产物、基线产物和发布证据的生命周期 |
| `docs/` | 技术文档、交接、规划和报告 | 建立文档分层、版本、审阅和归档规则 |

### 2.2 当前架构

```text
                    ┌─────────────────────────────────┐
                    │       开发/验证工具链             │
                    │ Python 分析、报告、可视化、脚本    │
                    └───────────────┬─────────────────┘
                                    │
┌────────────────────┐     ┌────────▼────────┐     ┌────────────────────┐
│ ROS2/Gazebo/硬件层  │◄───►│ C++ Navigation  │◄───►│ UE5 3D Simulation   │
│ 真机/Mock/机器人接口 │     │ AMCL/A*/Costmap │     │ LiDAR/Ped/Lockstep │
└────────────────────┘     └────────┬────────┘     └────────────────────┘
                                     │
                              ┌──────▼──────┐
                              │ Config/Data │
                              │ Scene/Robot │
                              │ Params/Schema│
                              └─────────────┘
```

### 2.3 当前已知问题清单

#### P0：必须先解决

1. **源码权威性不清**：仓库 `cpp_src/bridge/ue_modules` 与实际工程 `D:\puppy_ue\Source\PuppyNav` 存在明显差异。
2. **场景数据双源**：UE 中仍存在 `HOME_OBSTACLES`、行人和初始位姿镜像硬编码，容易与 `config/scene_home.json` 分叉。
3. **协议缺少正式状态机**：当前有传感器帧、命令、ACK，但缺少连接握手、协议版本、序列号、心跳、错误和结束确认。
4. **测试证据不统一**：2D、UE5、ROS2、硬件适配层的测试结果格式不同，无法形成统一发布门禁。
5. **安全行为未统一**：通信超时、传感器异常、定位失效、规划失败和碰撞风险的处理分散在不同模块。
6. **中文编码污染**：部分 C++ 和日志存在乱码，影响长期维护、检索和问题诊断。

#### P1：必须在产品化前解决

1. 参数没有统一 schema、默认值、范围、单位和版本迁移机制。
2. 日志缺少统一字段、运行 ID、帧号、场景版本和配置哈希。
3. 规划器、定位器、避障器和恢复器的接口尚未全部抽象化。
4. 缺少长时间运行、断线重连、异常传感器和随机场景压力测试。
5. 缺少性能预算、内存预算、CPU/GPU 预算和目标硬件矩阵。
6. 发布包、安装脚本、版本信息和回滚机制尚未定型。

#### P2：持续优化项

1. 进一步降低 AMCL 和 Costmap 的 P95 延迟。
2. 提高复杂家具、窄门、动态行人和视觉遮挡环境下的规划质量。
3. 增加实验回放、轨迹重演、失败帧自动截图和对比分析。
4. 建立可视化控制台，显示机器人状态、规划路径、定位粒子、风险和通信状态。
5. 建立算法配置在线切换和 A/B 对比能力。

---

## 3. 总体技术原则

### 3.1 单一事实源

每类数据必须只有一个权威来源：

- 场景障碍物、目标点、房间和初始位姿：`config/scenes/*.json`；
- 机器人尺寸、传感器外参和速度限制：`config/robots/*.yaml` 或 JSON；
- 算法参数：`config/profiles/*.yaml`；
- 协议消息定义：`cpp_src/bridge/protocol.h` 加协议文档和版本号；
- 运行状态模型：统一 C++ 状态机；
- 测试指标定义：`docs/metrics.md` 或 schema 文件。

UE、Python、ROS2 和 C++ 不允许再分别维护同一份障碍物列表。

### 3.2 配置优先于硬编码

代码只保留：

- 数据结构；
- 解析器；
- 校验器；
- 默认安全策略；
- 必要的程序常量。

禁止在运行逻辑中继续新增：

- 家具坐标；
- 巡航点坐标；
- 房间边界；
- 初始位置；
- 机器人半径；
- 传感器量程；
- 速度和恢复阈值。

### 3.3 安全优先于性能，性能优先于功能堆叠

任何优化不得牺牲：

1. 失联停车；
2. 速度限制；
3. 地图边界保护；
4. 碰撞检测；
5. 状态和日志可追溯性；
6. 失败时可恢复或安全退出。

### 3.4 仿真与真机接口一致

仿真模式可以使用 Ground Truth 做评估和恢复辅助，但不能改变导航接口的基本语义。必须明确区分：

- **控制输入**：里程计、IMU、LiDAR、视觉或其他传感器；
- **评估输入**：Ground Truth；
- **调试辅助**：仿真专用恢复或可视化；
- **真实部署输入**：严禁依赖 Ground Truth。

---

## 4. 目标架构设计

### 4.1 分层架构

```text
┌────────────────────────────────────────────────────────┐
│  Application Layer                                    │
│  Patrol / Mission / Waypoint / Fleet API              │
├────────────────────────────────────────────────────────┤
│  Safety Supervisor                                    │
│  E-stop / Watchdog / Fault / Speed Limit / Recovery   │
├────────────────────────────────────────────────────────┤
│  Navigation Runtime                                   │
│  Localization / Global Planner / Local Planner        │
│  Costmap / Behavior Tree / Dynamic Obstacle           │
├────────────────────────────────────────────────────────┤
│  Robot Abstraction                                    │
│  Pose / Twist / Sensors / Actuator / Battery          │
├────────────────────────────────────────────────────────┤
│  Transport & Integration                              │
│  ROS2 / UE5 TCP / Gazebo / Mock / Hardware SDK        │
├────────────────────────────────────────────────────────┤
│  Data & Configuration                                 │
│  Scene / Robot / Params / Schema / Version / Hash     │
└────────────────────────────────────────────────────────┘
```

### 4.2 核心模块边界

#### `NavigationRuntime`

职责：

- 接收标准化传感器和机器人状态；
- 输出速度指令、导航状态和诊断；
- 不直接读 UE、Gazebo 或硬件 SDK；
- 不直接读取散落路径；
- 不负责 UI。

#### `Localization`

接口建议：

```cpp
struct PoseEstimate {
    double x;
    double y;
    double yaw;
    double confidence;
    uint64_t timestamp_ns;
    uint32_t source_frame;
};

class ILocalizer {
public:
    virtual ~ILocalizer() = default;
    virtual bool initialize(const Pose2D& initial_pose) = 0;
    virtual PoseEstimate update(const Odometry& odom,
                                const LaserScan& scan) = 0;
    virtual void recover(const RecoveryRequest& request) = 0;
};
```

#### `GlobalPlanner`

必须提供：

- 正常规划；
- 放宽代价规划；
- 纯静态层规划；
- 失败原因；
- 起点/终点修正距离；
- 路径长度、节点数和耗时。

#### `LocalPlanner`

必须提供：

- 速度候选生成；
- 动态障碍预测；
- 安全距离评估；
- 速度限幅；
- 不能前进时的明确原因；
- 与恢复行为的状态接口。

#### `SafetySupervisor`

必须独立于规划器运行，至少实现：

- 急停；
- 通信超时停车；
- 传感器超时停车；
- 定位置信度过低降速；
- 路径无效停车；
- 边界风险停车；
- 连续碰撞降速或停车；
- 仿真结束停车。

### 4.3 运行状态机

```text
BOOT
  ↓
WAIT_CONFIG
  ↓
WAIT_CONNECTION
  ↓
SENSOR_INIT
  ↓
LOCALIZATION_INIT
  ↓
READY
  ↓
RUNNING ───────────────┐
  │                    │
  ├─ PAUSED            │
  ├─ DEGRADED          │
  ├─ RECOVERING        │
  ├─ EMERGENCY_STOP ───┤
  ├─ SIMULATION_END    │
  └─ FAULT             │
                       ↓
                   SHUTDOWN
```

每个状态必须定义：

- 允许接收的消息；
- 允许发送的消息；
- 允许的速度；
- 超时策略；
- 退出条件；
- 进入和退出日志；
- 可否自动恢复。

---

## 5. 配置与场景数据治理

### 5.1 推荐目录

```text
config/
├── schema/
│   ├── scene.schema.json
│   ├── robot.schema.json
│   └── navigation.schema.json
├── scenes/
│   ├── home_v1.json
│   ├── office_v1.json
│   └── warehouse_v1.json
├── robots/
│   ├── puppypi_v1.yaml
│   └── mock_robot_v1.yaml
├── profiles/
│   ├── safe.yaml
│   ├── balanced.yaml
│   └── performance.yaml
└── manifests/
    └── release_manifest.yaml
```

### 5.2 场景 schema 必填字段

```json
{
  "schema_version": "1.0",
  "scene_id": "home",
  "scene_version": "1.0.0",
  "units": "m",
  "coordinate_frame": "world_xy",
  "grid": {},
  "robot": {},
  "obstacles": [],
  "patrol_targets": [],
  "pedestrians": [],
  "rooms": []
}
```

校验规则至少包括：

- 障碍物 `xmin < xmax`、`ymin < ymax`；
- 目标点不得落入障碍膨胀区；
- 初始点不得落入障碍物或边界安全区；
- 房间和地图边界必须闭合；
- 所有坐标单位一致；
- 所有目标点可由规划器到达；
- 行人起点不与机器人或静态障碍冲突；
- 网格尺寸、分辨率和原点互相一致；
- UE、2D、ROS2 加载后的数量和坐标哈希一致。

### 5.3 场景一致性检查工具

新增：

```text
scripts/validate_scene.ps1
scripts/validate_scene.py
```

输出：

```text
SCENE VALIDATION
[PASS] schema
[PASS] grid dimensions
[PASS] obstacle geometry
[PASS] target safety
[PASS] reachability
[PASS] UE mirror check
[PASS] Python loader check
[PASS] hash: sha256:...
```

任何一项失败都应阻止启动正式仿真。

### 5.4 UE 数据源改造

必须将 UE 中的 `HOME_OBSTACLES`、行人初始状态和巡航目标替换为运行时加载 JSON。临时硬编码只允许作为明确标记的离线 fallback，并且：

- 默认关闭 fallback；
- fallback 启动时打印红色级别警告；
- 记录实际使用的数据源和哈希；
- 在测试中验证 JSON 与运行时加载结果一致。

---

## 6. 通信协议产品化

### 6.1 协议版本

在 `FrameHeader` 中增加：

```cpp
struct FrameHeader {
    uint32_t length;
    uint16_t type;
    uint16_t version;
    uint32_t sequence;
    uint32_t timestamp_ms;
};
```

如果必须保持旧协议兼容，应新增协议版本 `v2`，而不是直接修改 v1 的二进制布局。

### 6.2 消息分类

#### 建链类

- `HELLO`
- `HELLO_ACK`
- `RESET`
- `RESET_ACK`
- `CAPABILITIES`

#### 运行类

- `SENSOR_FRAME`
- `CMD_VEL`
- `STEP_ACK`
- `HEARTBEAT`
- `STATUS`

#### 控制类

- `PAUSE`
- `RESUME`
- `EMERGENCY_STOP`
- `SIM_END`
- `SHUTDOWN_ACK`

#### 诊断类

- `DEBUG_POSE`
- `DEBUG_PATH`
- `METRICS`
- `FAULT`

### 6.3 锁步协议要求

每个仿真帧必须具备：

```text
frame_id
sensor_timestamp
sensor_bundle_complete
command_frame_id
ack_frame_id
state_before
state_after
```

严格保证：

```text
一个 sensor frame → 一个 command → 一个 ACK → 一次物理推进
```

禁止：

- 旧命令覆盖新命令但没有日志；
- 重复 ACK 导致重复推进；
- 结束后继续等待 ACK；
- 断线后继续发送速度；
- 缺少序列号仍将数据当作当前帧处理。

### 6.4 超时策略

| 条件 | 动作 |
|---|---|
| 100ms 未收到传感数据 | 保持当前状态，不推进 |
| 300ms 未收到有效控制 | 速度降为 0 |
| 500ms 通信超时 | 进入 `DEGRADED`，停车并报警 |
| 2s 通信中断 | 进入 `FAULT`，释放锁步和连接资源 |
| 收到 `SIM_END` | 停车、停止锁步、发送 `SHUTDOWN_ACK` |
| 收到非法序列号 | 丢弃帧并记录协议错误 |

---

## 7. 安全体系

### 7.1 安全等级

#### S0：正常

- 定位可信；
- 传感器正常；
- 路径有效；
- 速度按任务允许范围运行。

#### S1：受限运行

触发条件：

- AMCL 置信度下降；
- 起点修正次数增加；
- 动态障碍密度升高；
- 局部规划频繁失败。

动作：

- 限速至安全档；
- 增加重规划频率；
- 禁止高风险穿越；
- 记录诊断事件。

#### S2：安全停车

触发条件：

- 通信超时；
- LiDAR 连续无效；
- 路径为空且恢复失败；
- 连续碰撞超过阈值；
- 越界风险；
- 定位误差超过上限。

动作：

- 速度立即归零；
- 保持原地或执行低风险退让；
- 等待人工或上层任务恢复。

#### S3：急停

触发条件：

- 外部急停；
- 硬件安全故障；
- 多传感器同时失效；
- 机器人姿态异常；
- 保护性看门狗触发。

动作：

- 清零速度和角速度；
- 禁止自动恢复；
- 发布故障码和审计日志。

### 7.2 安全指标

正式版本必须满足：

- 通信中断后 300ms 内停止速度输出；
- 急停命令处理延迟 P95 < 50ms；
- 机器人不得因单个异常模块继续以正常速度运行；
- 所有安全状态转换有唯一事件 ID；
- 所有安全故障可以在日志中重放。

---

## 8. 导航算法产品化计划

### 8.1 定位

#### 短期

- 固定 AMCL 更新频率为 5~10Hz；
- 控制、局部规划和定位解耦；
- 预计算距离场；
- 记录粒子数、置信度、重定位次数、P50/P95 延迟；
- 完善 kidnapping、初始位姿错误和地图不一致测试。

#### 中期

- 引入统一 `ILocalizer` 接口；
- 支持 AMCL、Ground Truth evaluator、Mock localizer；
- 支持地图版本和定位源切换；
- 建立真实传感器噪声模型。

#### 验收指标

- 静态场景平均位置误差 ≤ 0.10m；
- 动态场景平均位置误差 ≤ 0.25m；
- 95% 帧位置误差 ≤ 0.50m；
- 误定位恢复成功率 ≥ 95%；
- AMCL P95 单次更新延迟满足控制周期预算。

### 8.2 全局规划

- 保留 A* 作为稳定基线；
- 统一规划失败原因枚举；
- 禁止无日志的直线穿越 fallback；
- fallback 必须进行障碍和机器人半径检查；
- 路径必须经过平滑、可行性和安全距离验证；
- 起点/终点修正必须输出原坐标、修正坐标和距离。

验收指标：

- 静态可达目标规划成功率 ≥ 99%；
- 规划成功路径不穿越膨胀障碍；
- P95 规划延迟 ≤ 50ms；
- 不可达目标能在 2s 内明确报告。

### 8.3 局部避障

采用分层策略：

```text
静态安全检查
   ↓
动态障碍预测
   ↓
速度候选生成
   ↓
碰撞时间 TTC 检查
   ↓
速度限幅
   ↓
SafetySupervisor 最终裁决
```

必须同时记录：

- 目标速度；
- 安全裁剪前速度；
- 安全裁剪后速度；
- 被哪一个障碍物限制；
- 最小 clearance；
- TTC；
- 是否进入恢复。

### 8.4 恢复行为

恢复行为必须从规划器代码中独立出来，形成状态机：

```text
NORMAL
  → SLOW_DOWN
  → REPLAN
  → ESCAPE_BURST
  → BACKTRACK
  → LOCALIZATION_RECOVERY
  → TARGET_SKIP
  → SAFE_STOP
```

每次目标跳过必须记录：

- 目标 ID；
- 尝试次数；
- 失败原因；
- 最近位置；
- A* 结果；
- 是否可能为场景错误。

不得以“连续卡住就自动跳过”掩盖场景、地图或控制错误。正式验收时应区分：

- 合法跳过：目标确实不可达；
- 非法跳过：算法没有正确规划或状态同步失败。

---

## 9. 性能与资源预算

### 9.1 运行频率预算

| 模块 | 目标频率 | 预算 |
|---|---:|---:|
| 控制输出 | 30Hz | P95 < 20ms |
| 局部避障 | 20~30Hz | P95 < 30ms |
| Costmap 动态更新 | 10~15Hz | P95 < 40ms |
| AMCL | 5~10Hz | P95 < 80ms |
| 全局规划 | 事件触发/1~2Hz | P95 < 100ms |
| 诊断和日志 | 异步 | 不阻塞控制线程 |
| 截图/回放 | 异步 | 不影响仿真帧推进 |

### 9.2 优化顺序

1. 先测量，禁止凭感觉优化；
2. 优化 AMCL 重复计算；
3. 优化 OccupancyGrid 和 Costmap 增量更新；
4. 将日志写入改为异步队列；
5. 避免每帧分配大 vector；
6. 对 LiDAR 数据采用固定容量缓冲区；
7. 对路径和粒子可视化进行采样；
8. 最后再考虑 SIMD、线程池和 GPU。

### 9.3 资源门禁

正式测试至少记录：

- CPU 总占用及每模块占用；
- 内存起始、峰值和结束值；
- GPU 占用和显存；
- 线程数；
- TCP 缓冲区积压；
- 日志写入量；
- 30Hz deadline miss 次数。

---

## 10. 测试与质量体系

### 10.1 测试金字塔

```text
              ┌─────────────┐
              │ 发布验收测试 │  少量、完整、严格
              ├─────────────┤
              │ 系统集成测试 │  ROS2/UE5/桥接/硬件Mock
              ├─────────────┤
              │ 模块测试     │  AMCL/A*/Costmap/协议/状态机
              ├─────────────┤
              │ 单元测试     │  几何、坐标、解析、边界、数学
              └─────────────┘
```

### 10.2 必须新增的测试类别

#### 协议测试

- 半包接收；
- 粘包接收；
- 非法长度；
- 非法消息类型；
- 旧序列号；
- 重复 ACK；
- `SIM_END`；
- 断线重连；
- 发送失败；
- 超时停车。

#### 场景测试

- 目标在障碍物内；
- 起点在障碍膨胀区；
- 门宽不足；
- 地图原点错误；
- 单位错误；
- Y 轴反向；
- UE 与 JSON 数量不一致；
- 动态行人初始重叠。

#### 导航测试

- 直线可达；
- 多拐角路径；
- 窄通道；
- 障碍动态横穿；
- 定位跳变；
- A* 连续失败；
- 目标到达边界；
- 恢复后重新规划。

#### 长稳测试

- 30 分钟；
- 2 小时；
- 8 小时商用候选版本；
- 随机种子矩阵；
- 不同帧率；
- 窗口最小化/恢复；
- UE 断开桥接；
- 桥接异常退出；
- 日志目录不可写。

### 10.3 统一测试报告

每次测试生成：

```text
artifacts/<run_id>/
├── manifest.json
├── metrics.json
├── summary.md
├── bridge.log
├── ue.log
├── trajectory.csv
├── diagnostics.csv
├── screenshots/
├── config_snapshot/
└── environment.json
```

`manifest.json` 必须包含：

- Git commit 或源码快照哈希；
- 场景版本和 SHA256；
- 机器人配置版本；
- 算法 profile；
- 随机种子；
- 操作系统、编译器、UE、ROS2 版本；
- 启动命令；
- 结束原因；
- 测试结论。

### 10.4 发布质量门禁

候选发布必须满足：

- 编译无 error；
- P0/P1 缺陷为 0；
- 单元测试通过率 100%；
- 协议集成测试通过率 100%；
- 2D 回归通过；
- UE5 烟测通过；
- 8 小时稳定性测试通过；
- 关键指标不低于上一基线；
- 所有报告和日志可归档。

---

## 11. 可观测性与诊断平台

### 11.1 统一日志字段

所有模块日志必须尽量包含：

```text
run_id
component
level
monotonic_time
wall_time
frame_id
sequence
state
scene_id
scene_version
config_hash
robot_pose
estimate_pose
goal_id
fault_code
message
```

### 11.2 指标分类

#### 运行指标

- 控制帧率；
- deadline miss；
- 速度；
- 路径长度；
- 目标完成数；
- 目标跳过数；
- 房间覆盖数。

#### 安全指标

- 碰撞事件；
- 最小 clearance；
- TTC；
- 急停次数；
- 通信超时；
- 传感器异常；
- 越界风险。

#### 定位指标

- 平均误差；
- P95 误差；
- 最大误差；
- 置信度；
- 粒子数；
- 重定位次数。

#### 规划指标

- A* 成功率；
- 规划延迟；
- 起点修正次数；
- 终点修正次数；
- fallback 次数；
- 恢复次数。

### 11.3 自动诊断规则

系统应自动识别：

- 机器人连续 N 帧位移不足；
- 速度命令非零但实际位移为零；
- A* 成功但局部控制无进展；
- AMCL 误差突然跳变；
- LiDAR 与地图系统性不一致；
- UE 与桥接帧号不一致；
- 碰撞事件持续增加；
- 目标在安全半径外反复切换；
- 结束后仍存在未清理资源。

---

## 12. 代码质量与工程规范

### 12.1 编码规范

- 所有新文件统一 UTF-8；
- C++ 文件禁止新增乱码注释；
- 公共接口使用英文标识符；
- 中文说明写入 UTF-8 文档；
- 禁止一行多语句和隐式全局状态；
- 禁止函数静态变量承载跨运行状态；
- 禁止裸指针拥有资源；
- 禁止在控制线程执行大规模同步文件写入。

### 12.2 错误处理

错误必须分类：

```text
CONFIG_INVALID
PROTOCOL_INVALID
CONNECTION_TIMEOUT
SENSOR_TIMEOUT
LOCALIZATION_LOST
PLANNING_FAILED
COLLISION_RISK
SAFETY_STOP
SIMULATION_END
INTERNAL_ERROR
```

每个错误必须有：

- 严重级别；
- 是否可恢复；
- 恢复动作；
- 最大重试次数；
- 用户可读信息；
- 机器可读错误码。

### 12.3 依赖和构建

建立固定构建入口：

```text
scripts/bootstrap.ps1
scripts/build_all.ps1
scripts/test_all.ps1
scripts/run_2d_regression.ps1
scripts/run_ue_smoke.ps1
scripts/package_release.ps1
```

任何脚本必须：

- 检查前置依赖；
- 输出明确错误；
- 返回非零退出码；
- 记录命令和环境；
- 不依赖当前终端手工设置变量。

---

## 13. CI/CD 与发布流程

### 13.1 分支和版本策略

建议采用：

```text
main       可发布、受保护
 develop    日常集成
 feature/*  功能开发
 release/*  发布准备
 hotfix/*   紧急修复
```

版本号采用 SemVer：

```text
MAJOR.MINOR.PATCH
```

示例：

```text
1.0.0  首个商用候选版本
1.1.0  新增场景或算法能力
1.1.1  缺陷修复
```

### 13.2 CI 阶段

#### PR 快速门禁

- Python 编译检查；
- C++ 编译；
- 单元测试；
- 配置 schema 校验；
- clang-format/check；
- 静态分析；
- 协议测试。

#### 夜间回归

- 2D 20 目标巡航；
- 50 个随机种子；
- 复杂动态障碍；
- UE5 3D 烟测；
- 性能采样；
- 失败截图和报告。

#### 发布门禁

- 8 小时稳定性；
- 目标硬件矩阵；
- 安全故障注入；
- 断线、重启和回滚测试；
- 发布包完整性检查。

### 13.3 发布包

```text
PuppyNav-1.0.0-win64/
├── bin/
│   ├── nav_ue_bridge.exe
│   └── diagnostics.exe
├── config/
├── scenes/
├── scripts/
├── docs/
├── LICENSES/
├── VERSION
├── manifest.json
└── checksums.sha256
```

禁止把临时日志、实验脚本、旧备份和未验证二进制混入发布包。

---

## 14. 分阶段实施路线图

### 阶段 0：基线冻结与风险清理（1 周）

目标：让团队知道“当前什么是能工作的基线”。

任务：

- 冻结当前 R28/R30 结果；
- 建立源码、配置和二进制清单；
- 标记 `D:\puppy_ue` 与仓库源码差异；
- 统一运行 ID 和产物目录；
- 编码污染清理；
- 建立 P0/P1/P2 issue 清单。

出口标准：

- 能从干净目录构建桥接；
- 能启动 UE5；
- 能运行 2D 和 3D 短回归；
- 所有版本和配置可追溯。

### 阶段 1：配置与场景单一事实源（2 周）

任务：

- 建立 scene schema；
- 将 UE 障碍物、行人和目标改为 JSON 加载；
- 完成 `validate_scene.py`；
- 增加坐标、单位、原点和可达性检查；
- 输出场景哈希；
- 删除或隔离镜像硬编码。

出口标准：

- 2D、C++、UE5 加载数量一致；
- 场景哈希一致；
- 修改一个场景文件即可同步所有仿真层。

### 阶段 2：协议与状态机产品化（2 周）

任务：

- 协议 v2；
- HELLO/ACK/心跳/序列号；
- `SIM_END`、`SHUTDOWN_ACK`；
- 断线和超时状态机；
- 重复 ACK 和乱序帧测试；
- 统一错误码。

出口标准：

- 断线 300ms 内停车；
- 仿真结束无 watchdog；
- 所有帧可按序列号追踪；
- 协议测试 100% 通过。

### 阶段 3：导航运行时重构（3 周）

任务：

- 抽象 `ILocalizer`、`IGlobalPlanner`、`ILocalPlanner`；
- 独立 `SafetySupervisor`；
- 独立恢复状态机；
- 消除函数静态跨运行状态；
- 统一错误和诊断接口。

出口标准：

- 可替换 AMCL/A*/局部规划器；
- 所有失败都有原因；
- 安全模块可以独立测试。

### 阶段 4：性能与稳定性（3 周）

任务：

- 建立 profiling；
- AMCL、Grid、Costmap 增量化；
- 异步日志；
- 固定容量缓冲区；
- 30Hz deadline 监测；
- 8 小时稳定测试。

出口标准：

- 控制周期达标；
- 无内存持续增长；
- 8 小时无死锁和崩溃；
- 资源使用在预算内。

### 阶段 5：测试、CI 与发布（2 周）

任务：

- 建立 CI；
- 统一 artifacts；
- 自动生成报告；
- 发布包和校验值；
- 安装、升级和回滚脚本；
- 编写运维和故障排查手册。

出口标准：

- 清洁机器可安装；
- 版本可复现；
- 发布门禁自动化；
- 交付文档完整。

### 阶段 6：真实硬件过渡（持续）

任务：

- Mock → Gazebo → UE5 → 真机逐级验证；
- 加入真实 LiDAR、IMU、里程计噪声；
- 限速和急停联调；
- 真实房间扫描和地图版本管理；
- 建立现场回滚方案。

出口标准：

- 仿真与真机接口一致；
- 真机可安全停车；
- 现场问题可采集和回放；
- 不依赖 Ground Truth。

---

## 15. 具体任务分解

### P0-01 建立权威源码目录

**修改范围：** `D:\puppy_ue\Source\PuppyNav`、`cpp_src/bridge/ue_modules`、构建脚本。  
**要求：** 选择一个权威来源，另一个由同步脚本生成。  
**验收：** 修改 Pawn 后，单一命令能更新 UE 工程并完成构建。

### P0-02 场景运行时加载

**修改范围：** UE Pawn、场景 loader、`config/scene_home.json`。  
**要求：** 删除实际运行链路中的障碍物和行人镜像硬编码。  
**验收：** 修改 JSON 中一个障碍物，UE、2D、C++ 三端同时变化。

### P0-03 协议状态机

**修改范围：** `protocol.h`、`PuppyTcpServer.*`、`nav_ue_bridge.cpp`。  
**要求：** 增加协议版本、序列号、心跳、结束确认和断线停车。  
**验收：** 注入断线、重复 ACK、半包和错误帧均能安全处理。

### P0-04 统一安全监督器

**修改范围：** C++ 导航核心和 UE 控制入口。  
**要求：** 所有速度输出必须经过安全监督器。  
**验收：** 任意通信、传感器、定位或规划故障都不会继续正常高速运行。

### P0-05 统一测试报告

**修改范围：** `scripts/`、`logs/`、`screenshots/`、`docs/`。  
**要求：** 每次运行都产生 manifest、metrics、日志、轨迹和截图。  
**验收：** 仅凭 artifacts 可复盘一次完整运行。

### P1-01 运行时诊断面板

**内容：** 当前状态、帧率、目标、路径、AMCL、障碍、碰撞、错误码和网络状态。  
**验收：** 研发人员无需查看原始日志即可判断主要故障方向。

### P1-02 性能优化

**内容：** AMCL、Grid、Costmap 和日志管线性能优化。  
**验收：** 满足 30Hz 控制和资源预算。

### P1-03 故障注入测试

**内容：** 断开 TCP、冻结 LiDAR、制造定位跳变、删除地图、阻塞目标、制造动态障碍。  
**验收：** 每种故障都有预期状态和安全动作。

### P2-01 多场景基准集

**场景：** 家庭、办公室、仓库、窄走廊、动态人群、光照变化。  
**验收：** 算法改动不只在单一客厅场景上有效。

### P2-02 多机器人/多任务扩展

**内容：** 任务队列、优先级、机器人 ID、资源锁和多机通信隔离。  
**验收：** 不修改导航核心即可增加第二台机器人。

---

## 16. 商用验收指标建议

### 16.1 功能指标

- 目标完成率 ≥ 95%；
- 静态可达目标规划成功率 ≥ 99%；
- 仿真结束状态正确率 100%；
- 断线停车成功率 100%；
- 场景配置校验通过率 100%。

### 16.2 安全指标

- 重大碰撞：0；
- 急停响应 P95 < 50ms；
- 通信断开停车 ≤ 300ms；
- 越界事件：0；
- 无效传感器继续高速运行：0 次。

### 16.3 稳定性指标

- 8 小时连续运行无崩溃；
- 内存增长在预算内；
- 无锁步死锁；
- 无未关闭 socket；
- 无持续日志爆量；
- 重启后可恢复运行。

### 16.4 算法指标

- 静态场景平均定位误差 ≤ 0.10m；
- 动态场景平均定位误差 ≤ 0.25m；
- A* P95 延迟 ≤ 100ms；
- 卡住比例 ≤ 5% 为目标，≤ 10% 为候选版本上限；
- 碰撞事件率持续低于上一版本基线。

---

## 17. 风险清单与应对

| 风险 | 影响 | 概率 | 应对措施 |
|---|---|---:|---|
| UE 源码分叉 | 修复未进入实际运行工程 | 高 | 建立单一权威源和同步脚本 |
| 场景硬编码 | 仿真与算法不一致 | 高 | JSON schema + 运行时加载 |
| 协议粘包/丢帧 | 锁步死锁或轨迹失真 | 中 | 序列号、状态机、协议测试 |
| AMCL 漂移 | 路径错误和恢复频繁 | 中 | 地图一致性、噪声模型、重定位测试 |
| 过度安全限速 | 完成率下降 | 中 | 安全模式分级和指标分层 |
| 统计口径不一致 | 错误结论 | 高 | 统一 metrics schema 和事件定义 |
| 长稳不足 | 商用现场崩溃 | 中 | 8 小时 soak test 和资源监控 |
| 编码乱码 | 维护和排障困难 | 中 | UTF-8 规范和 CI 检查 |
| 临时 fallback 被误用 | 掩盖真实问题 | 中 | fallback 默认关闭并阻止发布 |
| 外部环境差异 | 清洁机器无法运行 | 中 | 安装器、环境检查和容器化 |

---

## 18. 编程实施规则

后续编程人员必须遵循以下规则：

1. 先读本规划、`README.md`、相关 `HANDOVER` 和模块文档，再修改代码；
2. 不得使用 `git reset --hard` 清理工作区；
3. 不得覆盖与当前任务无关的已有修改；
4. 每次改动必须说明影响文件、风险和验证命令；
5. 先写或补测试，再实现关键状态机和协议功能；
6. 修改 C++ 后必须重新编译对应目标；
7. 修改 UE 源码后必须重新编译 `PuppyNav` 模块；
8. 修改场景后必须运行场景校验、2D 回归和 UE 烟测；
9. 任何性能结论必须有采样数据，不接受主观判断；
10. 任何“修复成功”必须附带日志、指标或截图证据；
11. 不得用 Ground Truth 替代真实定位输入后再宣称算法性能提升；
12. 任何目标跳过、恢复、急停和降级都必须记录原因；
13. 新增配置必须带单位、范围、默认值和版本说明；
14. 新增协议消息必须同步更新 C++、UE、文档和测试；
15. 完成一个阶段后必须更新本规划的执行状态。

推荐每个任务采用以下工作流：

```text
需求确认
  → 影响面分析
  → 失败案例/测试设计
  → 最小实现
  → 编译与单测
  → 2D 回归
  → UE/Gazebo 集成验证
  → 产物归档
  → 文档更新
```

---

## 19. 交付物清单

最终商用候选版本必须至少包含：

- 源码；
- 构建脚本；
- 安装脚本；
- 协议文档；
- 场景 schema；
- 机器人 schema；
- 算法参数说明；
- API/Topic/消息说明；
- 单元测试和集成测试；
- 2D、UE5、Gazebo 验证报告；
- 故障注入报告；
- 8 小时稳定性报告；
- 发布 manifest 和 SHA256；
- 运维手册；
- 故障排查手册；
- 回滚和恢复手册；
- 变更记录；
- 已知限制说明。

---

## 20. 当前执行顺序

编程实施不得跳过以下顺序：

### 第一个里程碑

1. 建立权威源码源；
2. 同步 UE 工程；
3. 完成场景 schema 和校验；
4. 消除 UE 场景硬编码；
5. 建立统一 artifacts。

### 第二个里程碑

1. 协议 v2；
2. 连接状态机；
3. 断线停车；
4. `SIM_END`/`SHUTDOWN_ACK`；
5. 协议故障注入测试。

### 第三个里程碑

1. `SafetySupervisor`；
2. 定位、规划和恢复接口抽象；
3. 统一错误码和诊断；
4. 30Hz 性能预算；
5. 长时间稳定测试。

### 第四个里程碑

1. CI/CD；
2. 发布包；
3. 安装和回滚；
4. 多场景基准；
5. 商用候选版本验收。

---

## 21. 规划结论

当前项目已经具备继续产品化的技术基础，但必须从“单次仿真调通”转向“工程可复制、故障可诊断、系统可验证、版本可交付”。

最重要的不是继续增加更多规划算法，而是完成以下四件事：

1. **统一数据源**：所有仿真和运行模块读取同一份场景、机器人和参数配置；
2. **统一状态机**：通信、导航、安全、恢复和仿真结束都有明确状态和转移；
3. **统一质量证据**：所有性能、稳定性和安全结论都有自动化报告；
4. **统一交付流程**：任何版本都可以构建、测试、安装、运行、回滚和复盘。

完成本规划后，项目将具备从研究验证系统升级为商用候选平台的基本条件，并能继续扩展到真实机器人、多场景导航、多任务调度和多机器人协同。

---

## 附录 A：建议新增文件

```text
config/schema/scene.schema.json
config/schema/robot.schema.json
config/schema/navigation.schema.json
config/scenes/home_v1.json
config/robots/puppypi_v1.yaml
config/profiles/safe.yaml
config/profiles/balanced.yaml
scripts/validate_scene.py
scripts/build_all.ps1
scripts/test_all.ps1
scripts/run_2d_regression.ps1
scripts/run_ue_smoke.ps1
scripts/package_release.ps1
docs/metrics.md
docs/error_codes.md
docs/release_process.md
docs/fault_injection.md
tests/protocol/
tests/integration/
tests/system/
```

## 附录 B：首批建议 Issue

```text
P0-01 统一 UE 权威源码和同步机制
P0-02 移除 UE 场景几何硬编码
P0-03 完成场景 schema 与运行时校验
P0-04 协议增加版本、序列号和心跳
P0-05 断线 300ms 内安全停车
P0-06 建立统一运行状态机
P0-07 建立统一指标和 artifacts
P0-08 清理 C++/日志编码乱码
P1-01 抽象定位器接口
P1-02 抽象全局/局部规划器接口
P1-03 独立 SafetySupervisor
P1-04 建立故障注入测试
P1-05 建立 8 小时稳定性测试
P1-06 建立 CI/CD 和发布包
P2-01 多场景基准集
P2-02 运行时诊断面板
P2-03 轨迹回放与失败重演
P2-04 多机器人任务扩展
```

---

**文档状态：** Draft for implementation  
**下一步：** 由编程人员按“当前执行顺序”从 `P0-01` 开始实施，并在每个里程碑完成后更新本文档。
