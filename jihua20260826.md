# OOMWOO 扫地机器人项目算法优化计划

- 计划日期：2026-08-26
- 源码仓库：`makerspet/oomwoo`
- Clone 地址：`https://github.com/makerspet/oomwoo.git`
- 本地目录：`./robot_src`
- 分析基线：本地 clone 的 Git 提交 `3b579820911254f19d4d769e665c0546af9bd699`（2026-08-24）

## 1. 项目现状与判断

OOMWOO 是一个以 Raspberry Pi/CM4/CM5 + MCU 为核心、基于 ROS2 的开源扫地机器人项目。仓库当前以系统架构、软件接口和模块 RFC 为主，仍处于 early development 阶段，完整的清扫、定位、导航和硬件驱动算法包并未全部内置。因此本计划采取“先建立可复现实验基线，再逐项替换算法”的路线，避免在硬件和接口尚未稳定时过早优化局部实现。

源码中已经明确的算法边界如下：

1. 传感器侧以约 5 Hz 的 2D LiDAR、轮编码器/`/odom`、`/tf`、IMU 和碰撞/悬崖/轮离地信号为基础。
2. CPU 负责 ROS2、`slam_toolbox`、Nav2、LiDAR 处理和高层行为；MCU 负责电机、编码器、传感器采集、充电监督、看门狗和与运动安全相关的硬实时保护。
3. 首次清扫需要同时完成建图、探索和覆盖路径规划；已知地图运行需要定位、导航、重定位和断点续扫。
4. 需要处理动态障碍物，以及 LiDAR 扫描平面看不到但会触发 bumper 的静态障碍物。
5. 项目明确要求仿真优先、无云端依赖、可在 Gazebo 中复现，并重视 Raspberry Pi 4 GB 内存预算、LiDAR 不降频和恢复事件延迟。

## 2. 优化目标与总体原则

### 2.1 总体目标

在不破坏 ROS2 软件接口和 MCU 安全边界的前提下，提升以下能力：

- 地图质量：降低漂移、重影和闭环误差，支持断点续图。
- 导航可靠性：提高到点成功率，减少窄通道振荡、局部最优和无效重规划。
- 清扫完整性：提升可达区域覆盖率、边缘覆盖率，并减少重复路径和漏扫。
- 动态适应性：对人、宠物和临时物体保持稳定避障并能恢复清扫。
- 安全性：碰撞、悬崖、轮离地、急停和通信超时始终由 MCU 兜底，ROS2 算法只能提出运动请求。
- 资源效率：在保持 LiDAR 目标更新率约 5 Hz 的情况下，降低 CPU/RAM 占用和恢复延迟。

### 2.2 设计原则

- 先基线、后优化：每次改动必须有固定世界、固定初始位姿、固定随机种子和 Git SHA。
- 模块化替换：通过现有 topic/action/参数隔离 SLAM、定位、规划、覆盖和任务编排。
- 安全优先：所有速度命令设置有效期；陈旧命令、通信丢失或 MCU 报警立即进入安全停止。
- 仿真到实机一致：Gazebo 使用与实机相同的逻辑接口，硬件差异放入适配器。
- 可解释指标：不只看平均值，同时记录失败率、最差值、长尾延迟和恢复次数。

## 3. 分阶段算法优化路线

### 阶段 0：建立基线与可观测性（P0，1 周）

**目标：** 先证明当前系统能运行，并让所有后续优化可量化比较。

实施内容：

- 基于 `urdf-gazebo-sim` 建立单房间、多房间、窄通道、家具密集、混合地面和 LiDAR 不可见障碍物场景。
- 固定记录 `/scan`、`/odom`、`/tf`、bumper、`/cmd_vel`、Nav2 action 状态、costmap 和地图文件。
- 增加统一运行元数据：ROS2 发行版、RMW、硬件/RAM、LiDAR 频率、场景、随机种子和 Git SHA。
- 增加 rosbag2 录制/回放脚本、运行时诊断和故障事件时间戳。
- 建立 RSS/PSS、CPU、启动时间、LiDAR 更新率、恢复事件延迟的基准报表。

**验收：** 相同输入重复运行结果可复现；传感器数据无异常丢帧；所有失败都能定位到感知、定位、规划、控制或硬件安全层。

### 阶段 1：里程计与 SLAM 优化（P0，2 周）

**目标：** 为覆盖和导航提供稳定地图及可信位姿。

实施内容：

- 统一 `base_link`、`odom`、`map`、LiDAR 和 IMU frame，校准 LiDAR 外参、轮距、轮半径和编码器比例。
- 以 `slam_toolbox` 为第一基线，比较同步/异步模式、扫描匹配参数、运动阈值、地图分辨率和回环检测参数。
- 融合轮编码器与 IMU：优先使用 `robot_localization`/EKF 形成平滑的 `odom -> base_link`，再将 LiDAR SLAM 用于全局校正。
- 对低速、原地旋转和轮胎打滑分别建立标定数据集，避免单一平均参数掩盖系统误差。
- 支持序列化 SLAM 会话和断点续图；在地图质量回归中对比从头建图与续图结果。
- 对 scan 时间戳、TF 延迟和异常跳变增加监测，禁止用过期 TF 驱动规划。

**指标建议：** 记录轨迹漂移率、闭环位置误差、地图与真值的重叠率、重定位收敛时间、错误重定位率和 TF 延迟分位数。

### 阶段 2：已知地图定位与导航优化（P0，2 周）

**目标：** 提高任意可达目标的到点率和复杂环境鲁棒性。

实施内容：

- 先用 AMCL 或 `slam_toolbox` localization mode 建立已知地图定位基线，并覆盖未知初始位姿。
- 配置分层 costmap：静态地图层、LiDAR obstacle/voxel 层、机器人膨胀层、动态障碍物清除层和 keepout/zone 层。
- 针对扫地机器人低速、圆形底盘和较大 footprint，标定 footprint、膨胀半径、停止距离和倒车安全距离。
- 优先调参 Nav2 的行为树、全局规划器、局部控制器、速度/加速度限制和重规划触发条件；避免一开始引入难以解释的复杂新规划器。
- 对窄门、家具 U 形区域、动态行人和局部障碍设计专门 recovery sequence：等待、原地旋转、后退、清除局部代价地图、重新规划。
- 对“被抱起后放到新位置”的 kidnapped robot 场景增加定位置信度监测、全局重定位和安全交接。

**指标建议：** 到点成功率、规划时间、路径长度比最短路、最近障碍距离、局部振荡次数、重规划次数、恢复成功率和定位收敛时间。

### 阶段 3：覆盖路径与首次清扫优化（P0，3 周）

**目标：** 从“能导航”升级为“完整、少重复地清扫”。

实施内容：

- 将可清扫区域从地图中提取为 free-space，并进行机器人 footprint、墙边安全距离和不可达区域膨胀。
- 采用分区/单元分解后生成 boustrophedon 往返路径；对长走廊、房间连接处和窄通道单独处理。
- 路径顺序采用“边缘/墙边优先 + 房间连通性 + 距离/电量代价”的启发式，避免频繁跨房间。
- 加入边缘清扫补偿：沿墙跟随、角落短段覆盖、刷头有效宽度重叠和转弯半径约束。
- 建立覆盖栅格状态：未覆盖、已覆盖、暂不可达、动态阻塞、碰撞确认障碍；路径被阻塞时只重规划受影响区域。
- 将 bumper 命中位置投影为膨胀障碍，执行有界后退/转向后重新规划，防止反复撞击 LiDAR 看不到的障碍。
- 以覆盖完成和地图完成双条件定义 done；清扫中断时持久化覆盖状态，支持充电/服务后从断点继续。

**指标建议：** 可达区域覆盖率、边缘覆盖率、漏扫面积、重复覆盖率、单位面积路径长度、清扫总时长、碰撞次数、分区切换次数和断点续扫覆盖损失。

### 阶段 4：动态障碍与近场避障融合（P1，2 周）

**目标：** 解决 2D LiDAR 对低矮、透明或临时障碍感知不足的问题。

实施内容：

- 将前向相机/ToF 作为近场补充，建立统一 obstacle observation adapter，不直接耦合上层覆盖算法。
- 用时间衰减的动态障碍层区分“短暂占用”和“稳定静态障碍”；动态障碍消失后自动清除，避免地图永久污染。
- 采用障碍物速度估计和预测安全区，在局部规划中加入 TTC（碰撞时间）约束；低速清扫优先停车/等待，再决定绕行。
- 视觉或 ToF 只负责提供候选障碍，最终运动安全仍由 costmap、bumper 和 MCU 安全层共同约束。
- 对宠物、人、移动家具、玻璃和低于 LiDAR 平面的物体分别建立仿真/实测样本。

**指标建议：** 障碍检测 precision/recall、误停率、漏检率、动态障碍绕行成功率、停止响应延迟、障碍层残留时间和清扫任务恢复时间。

### 阶段 5：恢复、安全与任务闭环优化（P0/P1，2 周）

**目标：** 让长时间清扫在故障、低电量和外部干预下可恢复。

实施内容：

- 将恢复策略组织为有限状态机：正常清扫、受阻等待、碰撞恢复、反复卡困、定位丢失、低电量返航、充电、服务、断点续扫和错误终止。
- 每个恢复动作设置最大尝试次数、时间上限和速度上限；超过阈值必须暂停并发布稳定 reason code，而不是无限重试。
- 统一 `/cmd_vel` 仲裁，明确 Nav2、覆盖节点、恢复节点和 dock-cycle 的优先级，杜绝多个节点互相抢占速度命令。
- 低电量返航采用预计返航能耗 + 安全余量，而不是固定电量阈值；充电完成后校验定位和覆盖状态再恢复任务。
- 将悬崖、轮离地、急停、过流、通信超时等硬安全事件下沉到 MCU，并验证 Linux/ROS2 失联时仍能停机。

**指标建议：** 各类故障恢复成功率、恢复事件端到端延迟、误触发率、无限重试率、低电量返航成功率、断点恢复后的覆盖完整度和安全停止响应时间。

### 阶段 6：计算资源与实时性优化（P1，持续）

**目标：** 在 Pi 4/CM4 类资源上维持稳定运行，为低成本硬件留出余量。

实施内容：

- 先用 profiling 找热点，再优化；重点观察 SLAM、Nav2 costmap、Python 节点、图像/点云转换和 rosbag 录制。
- 统一 QoS：传感器使用 sensor-data QoS，地图使用 transient-local，控制命令小队列并带超时。
- 对不需要高频的任务状态、覆盖统计和诊断采用降频/事件触发；避免复制大消息和无意义的全图重算。
- 评估 ROS2 composable nodes、进程内通信以及将高频/高 CPU 节点迁移到 C++/Rust 的收益，但保留可维护的 Python 原型。
- 任何资源优化都必须证明没有把 LiDAR 更新率降到约 5 Hz 以下，也不能增加恢复事件延迟。

**指标建议：** 各进程 RSS/PSS、空闲/建图/导航 CPU、峰值内存、启动时间、LiDAR 实际更新率、消息延迟 P50/P95/P99 和恢复事件延迟。

## 4. 推荐实现顺序与提交拆分

1. `baseline/observability`：仿真场景、bag、指标采集、固定种子和回归报告。
2. `localization/slam`：TF/传感器标定、EKF、SLAM 参数和地图续接。
3. `navigation/recovery`：Nav2 参数、costmap、行为树和恢复状态机。
4. `coverage/first-clean`：可达空间、分区、往返覆盖、边缘补偿和覆盖持久化。
5. `obstacle/near-field`：相机/ToF 适配器、动态障碍层和 TTC 安全约束。
6. `jobs/dock-resume`：任务状态机、返航充电、服务触发和断点续扫。
7. `benchmark/optimization`：CPU/RAM、延迟、通信和部署优化。

每个提交只解决一个模块问题，必须附带：参数文件、启动方式、测试场景、指标 JSON/CSV、失败案例和回归结论。跨模块接口变更先更新 `docs/SOFTWARE_INTERFACES.md`，再修改实现。

## 5. 测试矩阵与验收门槛

### 5.1 仿真场景

- 单房间空旷场景：建立基本建图、导航和覆盖基线。
- 多房间场景：验证房间连通、分区顺序和跨房间覆盖。
- 窄门/窄走廊：验证 footprint、膨胀和局部控制器。
- 家具密集场景：验证局部规划和反复卡困恢复。
- 动态行人/宠物场景：验证动态障碍层、等待和绕行。
- 玻璃、低矮物体、门槛和 LiDAR 不可见障碍：验证 bumper 建障与重规划。
- 混合地面场景：为后续 wall/edge following、地面识别和拖布动作留下接口。
- 被抱起/随机初始位姿场景：验证全局重定位和任务恢复。

### 5.2 最低验收门槛

- 关键场景 headless 回归测试可在 CI 运行，并保存 Git SHA、配置和结果。
- 已知地图任意可达目标导航成功率目标不低于 95%；失败必须可分类且不出现无限重试。
- 首次清扫的可达区域覆盖率目标不低于 95%，边缘覆盖率和重复覆盖率在基线测量后设定改善目标。
- 动态障碍不会导致任务永久中断；LiDAR 不可见障碍触发 bumper 后能停止、标记、恢复并绕开。
- 断点续扫后最终覆盖结果与不中断清扫相比，漏扫面积增加不超过预设容差。
- LiDAR 实际更新率维持约 5 Hz；资源优化后恢复事件延迟和 MCU 安全停止能力不退化。
- 实机 bring-up 前，Gazebo 仿真必须通过；实机只允许在低速、限速区域和物理急停可用时进行验证。

## 6. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 完整算法包尚未全部入库 | 无法直接做端到端性能比较 | 先建立最小可运行 baseline，并将外部包/配置记录在版本清单中 |
| 硬件接口仍有 TBD | 算法参数可能反复调整 | 先依赖现有 ROS2 逻辑接口，硬件差异隔离在 bridge/adapter |
| 2D LiDAR 对透明/低矮障碍能力有限 | 漏检、碰撞、地图错误 | bumper + ToF/相机近场融合，MCU 硬安全兜底 |
| Pi 资源不足 | SLAM/Nav2 延迟和掉帧 | profiling、组件化、消息降频/零拷贝评估，禁止牺牲安全和 LiDAR 频率 |
| 动态障碍污染静态地图 | 重规划失败或永久绕行 | 动态层时间衰减，不把短时观测直接写入永久地图 |
| 多模块同时发布速度 | 运动冲突 | 单一 `/cmd_vel` 仲裁器和明确优先级/超时机制 |

## 7. 近期行动清单（进度见标注，`[x]`已完成 / `[~]`部分完成 / `[ ]`待机器人仿真）

- [ ] 固化 `urdf-gazebo-sim`、Nav2 和 SLAM 的最小启动链路。— 待装 `ros_gz`/`urdf-gazebo-sim`（sudo）。
- [~] 定义并验证 `base_link` 与传感器 frame、TF 发布者和时间戳策略。
      — TF 延迟监测已实现并**实跑验证**（p95≈14.9ms / p99≈16.0ms）；frame-graph 全量校验待机器人运行期。
- [~] 生成第一版单房间、多房间和窄通道回归场景。
      — `scenario_registry.yaml` 已落地 §5.1 八场景（world/初始位姿/seed）配置；Gazebo world 文件待装 ros_gz 后生成。
- [ ] 完成 EKF + `slam_toolbox` 与 AMCL 的基线对照。（待机器人仿真）
- [ ] 实现最小 boustrophedon 覆盖器和覆盖栅格持久化。（Phase 1/3）
- [~] 加入 bumper 建障、有限恢复和 `/cmd_vel` 安全仲裁。
      — **KPI 采集链路已验证**：`ros_gz_interfaces` 已装（用户 2026-08-28 装），重跑 `demo_run.sh` 实测 `bumper 左事件=1 / 总事件=1`，订阅按需自动激活无需改代码；**建障/恢复/仲裁行为**仍待机器人仿真运行期实现。
- [x] 建立性能基准报表，确认 Pi 4 GB / CM4 类配置的资源余量。
      — `baseline_report.py` 已对**真实 ROS2 节点运行**产出 `DEMO_BASELINE_REPORT.md`（scan 5.00Hz 达标、odom 漂移≈1.0、TF p95≈15ms）；系统资源指标需 `system_sampler.py` 在 ROS2 机器采集。
- [ ] 仿真回归通过后，再进入真实 LiDAR、编码器、MCU 和充电 dock 联调。

## 8. 结论

OOMWOO 当前最值得投入的不是立即替换某一个局部规划器，而是补齐“可运行基线 + 可观测性 + 覆盖状态持久化 + 安全恢复闭环”。建议以 P0 的定位/导航/覆盖/恢复为主线，以 P1 的近场感知和计算优化为并行支线；所有算法改进都通过 ROS2 接口、Gazebo 回归和明确指标验收，最终再迁移到真实硬件。

## 9. 执行进度（Phase-0 基础件已落地，2026-08-26）

> 状态：**阶段 0「可复现实验基线 + 可观测性」的代码与配置已完整落地，并已在真实
> WSL + ROS2 环境中验证通过**——`colcon build` 成功（生成 `metrics_collector_node` /
> `run_metadata_node` 节点脚本），`colcon test` **10/10 单测通过**（2026-08-27 实跑）。
> 注意：用户 WSL 实际为 **ROS2 Humble（Ubuntu 22.04）**（非计划假定的 Jazzy），
> 包为发行版无关，`colcon build` 在 Humble 下正常。Gazebo 仿真另需 WSL 内安装
> `ros_gz`/`urdf-gazebo-sim` 与 `ros-humble-ros-gz-interfaces`（节点运行期依赖）。

### 9.1 已交付（代码/配置，可运行于 ROS2 环境）

新增 ROS2 包 `oomwoo_baseline`，位于
`robot_src/contributions/baseline-observability/oomwoo/oomwoo_baseline/`：

- **KPI 计算核心** `metrics.py`（与 ROS 解耦，可单测）：LiDAR 更新率/达标、
  TF 延迟分位数、里程计漂移比、cmd_vel 陈旧、bumper 事件、地图空闲率、恢复延迟。
- **采集节点** `metrics_collector.py`（rclpy）：订阅 `SOFTWARE_INTERFACES.md`
  的基线 topic（`/scan /odom /tf /cmd_vel /bumper_left /bumper_right /map
  /oomwoo/status`），周期写 JSON 快照 + CSV 时间序列。
- **运行元数据** `run_metadata.py`：采集 ROS_DISTRO / RMW / Git SHA / seed /
  scenario / config（对齐 compute-benchmark 可复现要求）。
- **场景注册表** `scenario_registry.py` + `config/scenario_registry.yaml`：
  实现 §5.1 **八个场景**（单房间/多房间/窄门/家具密集/动态行人宠物/
  玻璃低矮不可见/混合地面/被抱起随机位姿），每场景固定 world / 初始位姿 / seed。
- **启动文件** `launch/baseline_record.launch.py`：rosbag2 录制基线 topic +
  启动采集节点 + 元数据节点。
- **报表生成** `scripts/baseline_report.py`：聚合指标 + 元数据 + 可选系统指标
  → markdown 基线报表（含 §5.2 验收自查）。
- **资源采样** `scripts/system_sampler.py`：Linux `/proc` 采样进程 RSS/PSS/CPU
  （compute-benchmark 指标，无第三方依赖）。
- **一键编排** `scripts/run_experiment.sh`：固定 seed+scenario 编排单次实验。
- **单测** `test/test_metrics.py`：10/10 通过（KPI 数学 + 场景解析）。
- **README.md**：构建/运行/报表/采样的完整说明，及与计划章节的对应关系。

### 9.2 环境约束与后续步骤

1. **构建/运行环境 = 本机 WSL + ROS2 Humble（Ubuntu 22.04）**：用户确认 WSL 内已具备
   `ros2`/`colcon`/`ament`（本会话早前误判为"无 ros2"，已修正；计划原假定 Jazzy，
   实际为 Humble，包发行版无关）。Phase-0 包已在 WSL 中 `colcon build` 并 `colcon test`
   **10/10 通过**（2026-08-27 实跑，见 `scripts/colcon_validate.sh`）。
   ⚠️ 环境坑：WSL 内 `launch_testing` pytest 插件与 pytest 9.x 不兼容，会在插件注册阶段
   崩溃；`colcon_validate.sh` 已用 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` 绕开（与本包无关）。
   `metrics_collector`/`run_metadata` 节点运行需 `rclpy` 与 `ros_gz_interfaces`
   （`sudo apt-get install ros-humble-ros-gz-interfaces`，**已于 2026-08-28 装好并验证**：重跑
   `demo_run.sh` 产出 `bumper 左事件=1 / 总事件=1`，KPI 采集链路闭环）。

   **Gazebo 仿真正确前置（2026-08-28 纠正）**：仓库官方仿真**不**在 WSL 内 `apt` 装 `ros_gz`，
   而是跑在官方 Docker 镜像 `makerspet/oomwoo:jazzy-dev`（含 `kaiaai_gazebo` / `kaiaai_bringup`），
   GUI 经 VcXsrv（Display=0）。流程见 `docs/blog/oomwoo-one-simulate-in-gazebo.md`：
   拉镜像→`docker run`（`DISPLAY=host.docker.internal:0.0`）→容器内 `kaia config robot.model oomwoo_one`
   →`ros2 launch kaiaai_gazebo world.launch.py`。本包 `baseline_record.launch.py` 订阅的 topic
   （`/scan /odom /tf /cmd_vel /bumper_left /bumper_right /map /oomwoo/status`）与仿真接口一致，
   仿真起来后 `ros2 launch oomwoo_baseline baseline_record.launch.py use_sim_time:=true` 即可采集。
   已加 `scripts/run_in_gazebo.sh`（容器内构建+启动采集的便捷脚本）。WSL Humble 仅用于本包构建/单测验证。
2. **§7 行动清单对应**：运行元数据捕获、场景注册、KPI 采集、报表生成**已落地为
   代码**；「固化最小启动链路」「frame/TF 验证」「EKF+SLAM/AMCL 基线对照」
   「boustrophedon 覆盖器」「bumper 建障/恢复/仲裁」「性能基准报表」等需
   在 ROS2 环境执行本包后完成，并据此勾选 §7 各子项。
3. **复用约定**：后续 P1–P6 每次算法改动，均应在**同一固定场景 + 固定 seed +
   固定 Git SHA** 下复用本包做对照，确保改动可量化（先基线、后优化）。
4. **验收门槛**（§5.2）建议在基线报表第 9 节持续跟踪：导航成功率 ≥95%、覆盖
   率 ≥95%、LiDAR 率 ~5 Hz、动态障碍不永久中断、断点续扫漏扫在容差内。

### 9.3 节点真实运行验证（2026-08-27 实跑）

在 WSL（Ubuntu-22.04 + ROS2 Humble）中，以**真实 ROS2 节点**方式跑通了
Phase-0 采集管线（见 `scripts/demo_run.sh`、`scripts/demo_publisher.py`）：

- `run_metadata_node` 实跑：写入 `DEMO_RUN_METADATA.json`（Git SHA
  `3b57982…`、`ros_distro=humble`、硬件平台、LiDAR 目标 5 Hz）。
- `metrics_collector` 实跑：由 `demo_publisher.py` 注入合成基线 topic
  （/scan /odom /tf /cmd_vel /map @5Hz），节点作为真实 ROS2 节点订阅并产出 KPI。
  实测：scan 更新率 **5.00 Hz（达标）**、odom 漂移比 **≈1.000**、
  TF 延迟 p95≈14.9ms / p99≈16.0ms、map 空闲率 0.942。
- `baseline_report.py` 聚合生成 `DEMO_BASELINE_REPORT.md`（含 §5.2 验收自查）。

**实跑暴露并修复的 3 个真实缺陷**（纯单测无法发现，只有节点实跑才触发）：
1. `metrics_collector` 用 `QoSPresetProfiles.get("sensor_data")` —— 该 API 不存在，
   应为 `qos_profile_sensor_data`（即便改成枚举成员也会传入 enum 类本身而报错）。已修。
2. `metrics.py:record_cmd_vel` 只更新陈旧事件计数，**从未把样本计入
   `_cmd_vel_recv`**，导致 `cmd_vel.samples` 恒为 0。已补 `append` 修复。
3. `ros_gz_interfaces` 未装时节点直接 import 失败无法启动；改为 **try/except
   容错 + bumper 订阅按需启用**，未装时仅禁用 bumper KPI（其余指标正常）。
   ⚠️ 仍未安装 `ros-humble-ros-gz-interfaces`（需 sudo，本会话无法执行），
   bumper KPI 暂为 0；安装后订阅自动激活，无需改代码。

**WSL 实跑要点（已固化进 `demo_run.sh` / `colcon_validate.sh`）**：
- 用 `bash 脚本文件` 执行（`bash -c` 变量不展开）；必须 `wsl -d Ubuntu-22.04`。
- 该 `/mnt/e` 工作区 ament 资源索引未生成，`ros2 run` 找不到可执行；改为
  **直接 `python3 <包>/metrics_collector.py` 源模块运行**，并把包父目录加入
  `PYTHONPATH`。
- `colcon build` 仍用 `colcon`（在 `$HOME/.local/bin`，非登录 shell 需手动加 PATH）。

### 9.4 回归护栏与提交（2026-08-27 续）

- **新增节点级集成测试** `test/test_node_smoke.py`（2 用例）：直接实例化真实
  `MetricsCollectorNode` 与 `capture_run_metadata`，作为 **QoS/import/订阅建立类
  崩溃** 的回归护栏（正是 §9.3 那 3 个真实缺陷的同类）。`colcon test` 现
  **12/12 通过**（10 逻辑 + 2 节点冒烟）。
- **`colcon_validate.sh` 增加 launch 文件可解析校验**：importlib 加载
  `baseline_record.launch.py` 并 `generate_launch_description()`，确认 11 个实体 /
  2 个 Node 动作；launch 文件语法与 import 持续受控。
- **已提交并开 PR**：`git commit 045cc6b`（20 文件 +2050 行，`.gitignore` 已忽略
  `DEMO_*`/`EXAMPLE_*` 运行时产物）。本机 `main` 已 `reset` 回 `origin/main`，提交保留在
  分支 `feat/phase0-baseline-observability` 并推送到 fork `yueqin22/oomwoo`，已开 PR
  **#60** 到上游 `makerspet/oomwoo`(main)：
  https://github.com/makerspet/oomwoo/pull/60
  （令牌 `yueqin22` 对上游无写权，走 fork→上游 PR；临时凭证文件推送后已清除）。
- **下一步（仍需用户侧 sudo）**：`sudo apt-get install ros-humble-ros-gz-interfaces`
  后重跑 `demo_run.sh` 验证 bumper KPI 自动激活；以及安装 `ros_gz`/`urdf-gazebo-sim`
  跑起 Gazebo 机器人仿真，推进 §7 后续项（最小启动链路 / EKF+SLAM 对照 / 覆盖器 /
  bumper 恢复 / 真实联调）。
