# PuppyPi 机器狗 WSL/ROS2 + UE5 仿真与毕业设计完善计划

> 计划名称：PuppyPi 仿真与自主导航毕业设计提升计划
>
> 文件：`jihua20260905.md`
>
> 编制日期：2026-09-05
>
> 适用仓库：`E:\puppyfangzhen`
>
> 目标：把当前“算法原型、离线 C++ 仿真、ROS2/WSL 骨架、UE5 可视化和 PuppyPi 适配器并存”的项目，收敛成一条可复现、可验收、可展示、可写进毕业论文的工程主线。

---

## 1. 先给结论

这个项目已经不是从零开始：导航核心、场景文件、C++ 仿真器、UE5 TCP 桥、ROS2 包、MiniCPM-RobotTrack 适配和测试工具都已经具备，且仓库有持续提交记录。当前最需要做的不是继续堆叠新算法，而是完成四件事：

1. **建立“当前真实状态”**：区分已实测、仅代码存在、仅 mock 演示和历史失效结论。
2. **统一单一真源**：场景、机器人尺寸、坐标、接口、启动入口和验收阈值不能各自硬编码。
3. **修通跨平台闭环**：WSL/ROS2、Windows C++ 导航核、UE5 传感器/画面、PuppyPi adapter 必须用同一套语义和安全边界。
4. **把结果变成毕业设计证据**：每个结论都有命令、Git SHA、配置哈希、原始日志、JSON 指标和可复现实验。

建议的毕业设计主线为：

> **面向室内动态环境的 PuppyPi 四足机器人安全自主导航仿真系统：基于 AMCL、A*、动态避障和跨平台数字孪生桥接的设计与验证。**

MiniCPM-RobotTrack、跌倒检测、情绪交互和安防巡逻可以作为“应用层扩展与展示亮点”，不要让它们替代已经最成熟的导航与安全主线。

---

## 2. 2026-09-05 代码与证据盘点

### 2.1 已经具备的资产

| 方向 | 已有内容 | 价值 |
|---|---|---|
| C++ 导航核 | `cpp_src/puppy_nav_core`、AMCL、A*、Costmap、RVO/CBF | 可作为性能和确定性基准 |
| C++ 数值仿真 | `cpp_src/sim`、固定种子、长稳脚本、回归流水线 | 适合批量实验和论文数据 |
| UE5 仿真 | `cpp_src/bridge`、UE5 `PuppyRobotPawn/PuppyTcpServer`、v2 协议 | 适合三维展示、传感器和数字孪生 |
| 场景数据 | `config/scene_home.json` v6.0、`scripts/validate_scene.py` | 已有单一场景真源雏形 |
| ROS2/WSL | `src/puppy_*`、Gazebo world、launch、slam/Nav2 配置 | 可作为真实机器人软件架构和联调环境 |
| PuppyPi 适配 | `src/puppypi_adapter`、`cpp_src/puppypi_adapter`、mock | 有明确硬件抽象边界，但真 SDK 尚未接入 |
| 视觉任务 | `src/puppy_minicpm_robot`、dry-run/mock/HTTP 后端、任务状态机 | 可展示自然语言任务与视觉跟踪安全接入 |
| 测试与报告 | `pytest`、`ctest`、配置检查、场景检查、回归、验收 HTML | 已具备工程化质量门禁的基础 |

### 2.2 本次检查得到的事实

以下结果是计划的当前基线，后续每次变更都要重新记录：

| 检查项 | 当前结果 | 解释 |
|---|---:|---|
| Python 单测 | **76 passed, 1 skipped** | MiniCPM、适配器、任务状态机和安全测试基本可运行；需解释 skipped 原因 |
| `validate_scene.py` | **PASS** | `scene_home.json` v6.0：18 个障碍、5 个目标、3 个行人、初始位姿 `(0.50,-2.20)`，4 个主要房间 |
| `check_config_consistency.py` | **PASS** | 三份配置中的机器人半径均为 0.35m；footprint 未显式闭合，当前按隐式闭合处理，应修正警告 |
| C++ 300 帧 smoke | **安全项 PASS** | 碰撞 0、A* 100%、平均定位误差约 0.067m、运动距离约 10.8m；但只访问 2 个房间，不能宣称完整巡逻验收 |
| C++ 长稳证据 | **已有历史强基线** | 旧版 v3.2.18i 有 10 种子×1 小时的零碰撞记录；必须用当前 `scene_home.json`、当前 Git SHA 和当前阈值重跑确认 |
| UE5 编译/GPU | **历史已通过** | UE5.3、GTX 1650 SUPER、D3D12 和 PuppyNav DLL 曾编译通过；性能预算仍需重新测量 |
| UE5 闭环 | **有历史修复，但需复验** | 历史日志曾出现出生点、目标过滤、AMCL 不更新、坐标错位和握手竞态；后续文档记录了部分修复，不能只引用旧报告中的 PASS |
| PuppyPi 真机驱动 | **未完成** | C++/Python adapter 仍有 SDK、LiDAR、IMU、ADC、GPIO TODO；默认 mock/sim 可用，real 模式必须诚实标记为待适配 |
| MiniCPM 模型 | **默认 mock/dry-run** | `minicpm_robot.yaml` 默认 `backend: mock`；HTTP/真实模型是可选后端，演示时必须标明模式 |

### 2.3 必须立即纠正的认知偏差

1. **“测试脚本返回 0”不等于完整任务成功**。例如 300 帧 smoke 中房间覆盖为 2，安全冒烟可以通过，但完整巡逻不通过。
2. **旧文档的 7/8 房间、5 行人与旧 dock 坐标不能直接代表当前项目**。当前场景 v6.0 是 4 房间、3 行人、dock/initial pose 为 `(0.5,-2.2)`。
3. **UE 画面能启动不等于导航闭环正确**。必须同时检查首帧位姿、AMCL 更新次数、坐标变换、目标切换、真值运动距离和碰撞回传。
4. **PuppyPi SDK 尚未接入时不能宣称“真机驱动完成”**。可以展示 adapter 设计、mock 一致性和硬件接入接口，但必须把 TODO 和实测范围写在论文限制章节。
5. **MiniCPM 的 mock 推理不是大模型精度结果**。它适合验证消息链路、安全限速和任务状态机；真实模型指标必须单独采集。

---

## 3. 目标架构：一条主链、两个仿真后端、一个安全边界

### 3.1 推荐数据流

```text
自然语言/外部任务
        ↓
mission_manager（唯一任务编排层）
        ↓
Nav2 或 C++ nav_core（全局定位、规划、局部决策）
        ↓
安全仲裁层（速度限幅、LiDAR/TTC、命令超时、急停、低电量）
        ↓
puppypi_adapter（sim/mock/real 三种后端，协议不变）
        ↓
Gazebo/WSL      C++ 数值仿真      UE5 + bridge      PuppyPi 实机
```

上层任务和安全策略不应知道底层是 Gazebo、UE5、CoppeliaSim 还是 PuppyPi 真机。底层差异只允许出现在传感器和执行器 adapter 中。

### 3.2 两个仿真后端的职责

| 后端 | 权威用途 | 不应承担的任务 |
|---|---|---|
| C++ 数值仿真 | 固定种子、批量回归、算法性能、论文表格 | 照片级画面和四足腿部动力学 |
| UE5 + bridge | 三维场景、LiDAR/碰撞查询、可视化、答辩视频 | 替代 C++ 精度基准、跨机器严格确定性物理结论 |
| WSL Gazebo + ROS2 | ROS2 topic/TF/Nav2/SLAM/adapter 集成、接近实机的软件链路 | 代替 C++ 的大规模长稳回归 |

### 3.3 单一真源

最终只允许以下文件作为权威来源：

- `config/scene_home.json`：房间、障碍、目标、行人、初始位姿、LiDAR 参数。
- `config/simulation_contract.yaml`：机器人 footprint、安全半径、地图分辨率、规划/避障/验收阈值。
- `config/unified_params.yaml`：运行 profile 和可覆盖参数。
- `cpp_src/bridge/protocol.md` + `protocol.h`：UE↔bridge 二进制协议 v2。
- `INTERFACES.md`：ROS2 topic/service/action 语义契约。

所有其他 YAML、URDF、UE 配置应通过脚本生成、显式映射或在启动时校验 hash；禁止在脚本里再次写死 dock、waypoint、机器人半径和坐标变换。

---

## 4. P0：先做基线冻结和“诚实可运行”

### P0-1 建立发布基线

**目标**：任何人都能回答“当前版本到底通过了什么”。

执行：

1. 给当前提交打标签，例如 `baseline-20260905`。
2. 生成 `artifacts/baseline-20260905/`，至少保存：
   - `environment.json`：Windows、WSL、ROS2、Python、编译器、UE、GPU、RMW、Git SHA。
   - `config_hashes.json`：scene/contract/unified params/ROS2/UE 关键配置 SHA-256。
   - `python_pytest.txt/json`、`scene_validation.txt`、`config_consistency.json`。
   - C++ 300/3600/36000/108000 帧结果、原始 stdout、结构化 JSON。
   - UE bridge `--help/--version/--dry-run/--check-scene` 结果。
   - WSL launch smoke 日志和 topic/TF 诊断。
3. 把“测试级别”写入结果：`SMOKE`、`INTEGRATION`、`REGRESSION`、`RELEASE`，不要用一个 PASS 覆盖不同级别。
4. 每个结果必须包含命令、退出码、开始结束时间、配置哈希、随机种子和阈值版本。

**出口标准**：重新打开仓库时，不看聊天记录也能复现基线；基线文件只读，不被后续运行覆盖。

### P0-2 修正验收门禁

当前 C++ 主程序已经输出真实性指标，但部分短跑模式允许“安全通过、任务未完成”。建议分层：

| 级别 | 用途 | 必须满足 |
|---|---|---|
| SMOKE | 300 帧快速诊断 | 进程存活、无墙穿透、无碰撞、能运动、A* 可调用 |
| INTEGRATION | 3600 帧约 2 分钟 | 至少 1 个目标到达、至少 3 个房间、stuck <20%、定位达标 |
| REGRESSION | 36000 帧约 20 分钟×10 seed | 零碰撞、A* ≥95%、平均误差 <0.20m、最大误差 <0.75m、当前 4 房间全部覆盖 |
| RELEASE | 108000 帧约 1 小时×10 seed | 与基线差异在阈值内、至少 1 个完整巡逻圈、长稳和内存无异常 |

当前 v6.0 场景只有 4 个房间，因此验收阈值应写“`rooms_visited >= 4`”；旧版 7/8 房间阈值若继续使用，必须先恢复并锁定对应旧场景，不能混用。

### P0-3 清理配置和模型尺寸漂移

已经发现以下必须统一的尺寸：

- `simulation_contract.yaml` 的 footprint 为 0.70m×0.50m、radius 0.35m；
- `scene_home.json` 的 planning radius 为 0.30m；
- `puppy.urdf.xacro` 的 body 约为 0.30m×0.18m，和契约尺寸明显不同；
- Python motion adapter 默认 `max_linear_y: 0.0`，而 MiniCPM/平面仿真会产生 `vy`；
- 不同文件曾使用 0.25/0.30/0.35m 机器人半径。

处理策略：

1. 把“规划 footprint”“碰撞 footprint”“视觉表现尺寸”明确分成三个字段。
2. 当前毕业设计版本固定一个安全基线：规划半径 0.35m、足迹 0.70×0.50m；若为 PuppyPi 实体尺寸不同，建立 `profile_raspberry_pi.yaml`，不能覆盖仿真基线。
3. 用脚本从 contract 生成/校验 URDF、C++、UE capsule 和 Nav2 footprint。
4. 为 `vx/vy/wz` 建立运动能力表。若当前 Gazebo planar move 的 yaw 或 vy 不稳定，导航测试不得把未标定的角速度当作可靠能力；优先使用已测量轴，或切换到真实 gait/controller 路径。

**出口标准**：`check_config_consistency.py` 不再有 footprint 警告；scene、contract、URDF、UE capsule 和 adapter 限速的差异都有自动检查结果。

### P0-4 PuppyPi adapter 进入“诚实可用”状态

统一 backend：

| backend | 可做什么 | 明确限制 |
|---|---|---|
| `mock` | 单测、CI、消息协议、故障注入 | 不代表真实执行器动力学 |
| `sim` | Gazebo/UE/C++ 仿真输入输出 | 传感器和执行器是仿真模型 |
| `real` | 仅在 SDK、LiDAR、IMU、ADC、GPIO 都通过预检后启用 | 当前仍待 PuppyPi SDK 接入 |

必须完成：

- real 模式缺少 SDK 时 **fail-fast**，禁止静默回退到 mock；
- `cmd_vel` 超时自动发布零速度，超时值写入 contract；
- 电机使能、站立、坐下、恢复姿态采用 service/action 请求，状态采用 topic；
- status adapter 发布真实电池、关节、故障和通信状态；
- mock 与 sim 通过相同的接口契约，加入“命令—状态—超时—急停”回归测试；
- 明确 `/cmd_vel` 的唯一仲裁者，Nav2、MiniCPM、巡逻和恢复节点不得同时直连硬件。

---

## 5. P0：UE5 + bridge 闭环复验清单

历史 UE 报告已经暴露过四类严重问题：出生点在家具内、巡逻目标被误删、AMCL 不更新、UE/bridge 坐标错位；后来源码和协议文档记录了修复，但必须按下面清单重新取得当前版本证据。

### 5.1 启动与握手

- Bridge 先启动并监听 `7777`，UE 端可重试连接，不使用固定 13 秒等待作为唯一同步方式。
- HELLO/HELLO_ACK、协议版本、序列号、心跳、超时、EMERGENCY_STOP、SIM_END 全部记录。
- 无 UE 客户端时，bridge 在 `--connect-timeout` 到期退出，不能无限挂起。
- UE 退出、网络断开或 300ms 无数据时，bridge 和 Pawn 都进入安全停止。

### 5.2 首帧与场景一致性

- 首帧 GT 必须等于 `scene_home.json.robot.initial_pose`，当前目标为 `(0.50,-2.20,0)`；若 UE PlayerStart 不一致，必须记录偏差并在 bridge 端只做一次显式校正。
- 首帧位置必须通过 `validate_scene.py` 的障碍膨胀检查。
- UE 侧障碍、行人、LiDAR 光线数和量程从 JSON 加载，不允许二次硬编码。
- UE 使用厘米，bridge 使用米；位置、速度、角度、LiDAR 距离分别建立单元测试。

### 5.3 每帧闭环指标

每帧至少记录：`frame_id`、UE GT、bridge 估计位姿、AMCL 更新标志、A* 是否重规划、目标索引、CMD_VEL、碰撞计数、最小障碍距离、运动距离、RTF 和消息延迟。

验收门槛：

| 指标 | 1800 帧 smoke | 9000 帧展示预演 | 36000 帧回归 |
|---|---:|---:|---:|
| 帧完成率 | 100% | 100% | 100% |
| AMCL 更新覆盖率 | ≥99% | ≥99% | ≥99% |
| 坐标 RMS 误差 | <0.15m | <0.15m | <0.20m |
| A* 成功率 | ≥95% | ≥95% | ≥95% |
| 碰撞事件 | 0 | 0 | 0 |
| 目标完成 | ≥1 | ≥4 并完成 1 圈 | ≥4 并完成 1 圈 |
| 真实 GT 距离 | >1m | >5m | >10m |
| stuck ratio | <20% | <20% | <20% |

### 5.4 UE 视觉层

第一阶段只使用胶囊体/简化 Puppy 形象，确保功能正确；第二阶段再加入四足网格、灯光、材质、调试覆盖层。GTX 1650 SUPER 4GB 的预算为：关闭 Lumen/Nanite，纹理优先 1024，Standalone ≥30 FPS，展示模式优先稳定而不是最高画质。

必须增加可开关的画面层：

- 真值轨迹与 AMCL 估计轨迹；
- A* 路径和重规划点；
- LiDAR 射线/命中点；
- 行人速度向量和安全半径；
- 当前目标、任务状态、速度、最小间距、帧号和协议状态；
- 碰撞/急停/断链时的红色告警。

这样答辩时可以直接解释“机器人为什么减速、为什么绕行、定位误差来自哪里”，比只展示一个会移动的模型更专业。

---

## 6. P1：统一 ROS2/WSL 主链路

### 6.1 支持入口只保留四个

1. `mock + core`：无 Gazebo，用于接口和故障测试。
2. `Gazebo + Nav2 + core`：WSL/ROS2 Humble 的完整软件闭环。
3. `C++/UE5 bridge`：Windows 侧高性能导航和三维展示闭环。
4. `PuppyPi real + core`：硬件预检通过后才开放。

每个入口在 README 写清：启动命令、节点列表、输入输出 topic、是否使用真模型、是否具有物理碰撞、验收级别和已知限制。

### 6.2 ROS2 接口收敛

依据 `IMPROVEMENT_ROADMAP.md` 和 `INTERFACES.md`，按下列顺序处理：

- 底层保留标准消息：`LaserScan`、`Imu`、`JointState`、`BatteryState`、`Twist`；
- 上层统一使用 `puppy_interfaces` 语义消息：`BatteryStatus`、`FallEvent`、`SecurityEvent`、`RobotHealth`、`PlatformMotionState`；
- 电机使能使用 service 请求，状态使用 topic，不把 `.srv` 当订阅消息；
- 运行时导航先只使用 Nav2 标准 `NavigateToPose`；自定义 `PatrolRoute/Dock` 要么补齐 server，要么降级为测试客户端；
- `mission_manager + mode_manager + safety_manager` 成为唯一任务裁决层；旧的 `autonomous_nav.py`、`patrol.py`、`patrol_mission.py` 标记为 demo/research-only；
- 所有 launch 使用 package share 的绝对资源路径，不依赖当前工作目录和固定睡眠时间；
- 用 lifecycle/topic readiness 等待替代 `TimerAction(15s/20s/40s)`。

### 6.3 WSL 复现脚本

固定以下步骤并做成脚本：

```bash
source /opt/ros/humble/setup.bash
cd ~/puppy_ws
rsync -a --delete /mnt/e/puppyfangzhen/src/ src/
colcon build --symlink-install --event-handlers console_direct+
source install/setup.bash
bash /mnt/e/puppyfangzhen/scripts/run_regression.sh
```

WSL 脚本必须：

- 在构建前检查 Windows/WSL 源码差异；
- 等待真实 `/odom` 消息而不是只看 topic 名称；
- 检查 `/tf`、`/scan`、`/clock`、`/cmd_vel` 的频率和时间戳；
- 启动失败时保留 world/slam/nav 日志；
- 在 ROS2 Humble 环境中运行，文档不要再混写 Jazzy。

---

## 7. P1：算法可信度与安全性提升

### 7.1 AMCL

当前 C++ AMCL 已有 KLD、聚类、IMU 融合和恢复逻辑，但论文必须区分：

- `confidence`：估计可信度；
- `avg/max localization error`：仿真真值误差；
- `scan likelihood`、`N_eff`、粒子方差：解释置信度的组成。

工作项：

1. 输出粒子数、有效粒子数、scan score、位置/角度方差和 kidnap recovery 次数。
2. 对 `fixed-500`、`KLD`、`KLD+IMU` 做消融，报告均值和 P95，而非只报最好一次。
3. 置信度低时触发重定位或降级，不直接让速度乘以不可解释的系数。
4. 增加随机初始位姿和 kidnapped robot 场景，验证可以恢复且不会穿墙。

### 7.2 A* 与 Costmap

规划成功的定义必须从“返回非空路径”升级为：

- 起点和终点经过 footprint 膨胀后有效；
- 每个路径点通过 footprint 碰撞检查；
- 最小静态 clearance 和动态 clearance 达标；
- 规划耗时未超过 `max_planning_time_ms`；
- fallback 使用次数和原因被记录，不能隐藏在“成功率”里。

测试场景至少包括：1.2m、1.0m、0.8m 门道，目标在障碍旁，动态行人横穿门口，未知区域和目标不可达。过窄门应明确失败，不能通过缩小机器人半径制造虚假成功。

### 7.3 动态避障和安全仲裁

- LiDAR 静态障碍、行人状态、预测位置和 TTC 分开记录；
- RVO/flee/CBF 的优先级写入设计图；
- 安全层可以覆盖模型和导航速度；
- 命令超时、断链、低电量、跌倒、传感器失效都必须输出零速度；
- 统计急停响应时间、最小间距、误停率、绕行成功率和 stuck episode；
- 运动真实性指标与安全指标同时验收，防止“车不动所以零碰撞”。

### 7.4 运动学与步态边界

最近轴响应记录表明 forward 大约 95%~100%，strafe 会随 RTF 变化，yaw 在部分低 RTF 测试中出现严重不稳定。因此：

- 建立固定起点、固定模拟时间、双向命令和 RTF 的轴响应标定；
- 不把墙边、模型自回波、旧世界残留和低 RTF 测量当成执行器缺陷；
- 为 `vx/vy/wz` 分别设置“已标定/待标定”状态；
- 若展示重点是导航而非步态，不要在答辩中宣称“真实四足动力学已验证”；可称为“平面运动学代理 + 四足表现层”。

---

## 8. P1：MiniCPM-RobotTrack 应用层定位

MiniCPM 适合做本项目的应用创新，但必须放在安全架构之后：

```text
自然语言 → mission_grounder
视觉帧 → vision_bridge → MiniCPM inference
意图 → track_cmd_adapter（限速、平滑、置信度、LiDAR 覆盖、超时停车）
                         ↓
                 统一安全仲裁 → 导航/adapter
```

实施分层：

1. **离线推理**：一帧图像 + 一条指令，输出结构化 waypoint/置信度/耗时。
2. **mock 连续回放**：验证消息、丢帧、超时、低置信度和遮挡安全停车。
3. **HTTP/真实模型**：独立记录模型加载、GPU/CPU、P50/P95 推理延迟和错误码。
4. **任务融合**：自然语言只负责任务意图和视觉跟踪；全局地图规划、LiDAR 避障和急停仍由确定性模块负责。

建议展示两个模式：

- `demo-mock`：必定可复现，作为现场主流程；
- `demo-real-model`：模型和显存条件满足时展示，并在画面上标注真实后端、模型版本和延迟。

现有 benchmark 已覆盖开阔区、窄通道、动态障碍和目标遮挡，但要继续防止“门控导致全零速度”的空测；每个场景必须先确认 adapter 处于 `VISUAL_TRACKING`，并同时检查非零运动请求和安全覆盖。

---

## 9. P2：实验矩阵与论文数据

### 9.1 最小可发表实验矩阵

| 实验组 | 对照 | 主要指标 |
|---|---|---|
| 定位 | 无 IMU / AMCL / AMCL+IMU | avg/max error、收敛时间、kidnap recovery |
| 规划 | 基础 A* / 膨胀修正 A* | 成功率、路径长度、耗时、最小 clearance |
| 动态避障 | 无 RVO / RVO / RVO+flee/CBF | 碰撞、近距、绕行成功率、速度平滑 |
| 安全层 | 无超时 / 有超时 / 断链急停 | 停止响应时间、误停、故障恢复 |
| 执行器 | 直线/横移/旋转轴响应 | 速度比例、yaw 误差、RTF 相关性 |
| 跨平台 | C++ baseline / UE bridge / WSL Gazebo | 帧率、接口延迟、任务完成、几何差异 |
| 视觉任务 | mock / HTTP/真实模型 | 目标发现、指令成功、推理延迟、遮挡安全停 |

每个实验至少 5~10 个随机种子或 10 次重复，报告均值、标准差、P50/P95、最差值和失败案例。不要只挑一张最好看的截图或一次零碰撞结果。

### 9.2 UE 等价性报告

`docs/ue_equivalence_report.md` 应更新为当前版本，并明确：

- C++ baseline 是精度基准；UE 只做等价性和可视化验证；
- scene JSON 版本、bridge 版本、UE commit/工程版本、协议版本；
- LiDAR 几何采样误差：平均 <5cm、单线最大 <20cm（阈值可由实际测量调整，但必须先固定）；
- 目标数量、行人轨迹、初始位姿和单位换算；
- 不能把 bridge fallback 路径、UE 未触发的碰撞事件或 AMCL 未更新的日志写成完整成功。

### 9.3 报告自动化

最终一键命令建议输出：

```text
artifacts/<run_id>/
  environment.json
  metadata.json
  config_hashes.json
  cpp_seed_*.json
  ue_seed_*.json
  ros2_topics.json
  timing.csv
  events.jsonl
  summary.md
  plots/
```

图表至少包括轨迹、误差、A* 成功率、最小障碍距离、速度、帧延迟、AMCL 粒子数和目标状态转移。

---

## 10. 毕业设计与专业展示方案

### 10.1 论文结构

1. **绪论**：家庭/室内巡检需求、四足机器人优势、仿真到实机的工程问题。
2. **需求与总体架构**：感知—定位—规划—安全—执行，WSL/UE5/adapter 边界。
3. **机器人与场景建模**：URDF、scene JSON、footprint、LiDAR 和单位换算。
4. **定位与导航算法**：AMCL、A*、Costmap、RVO/flee/CBF 的设计和复杂度。
5. **跨平台仿真系统**：ROS2/Gazebo、C++ baseline、UE5 TCP lockstep、协议 v2。
6. **任务应用层**：巡逻、回充、跌倒/安防事件、MiniCPM 视觉任务；明确 mock 与真实模型边界。
7. **实验与结果**：多种子、消融、UE 等价性、WSL topic/TF、性能和安全指标。
8. **总结与展望**：PuppyPi SDK、真实四足步态、真实相机/姿态模型和实机验证限制。

### 10.2 3~5 分钟答辩演示脚本

1. **10 秒**：展示系统架构图和四个运行入口，说明 C++ 是基准、UE5 是可视化、WSL 是 ROS2 集成。
2. **40 秒**：UE5 家居场景中机器人从 dock 出发，显示 LiDAR、A* 路径、AMCL 轨迹和房间目标切换。
3. **30 秒**：动态行人横穿，机器人减速/绕行；画面显示最小障碍距离和 RVO/CBF 状态。
4. **20 秒**：模拟 LiDAR 断流或 bridge 断链，展示 300ms 级安全停车和 fault reason code。
5. **40 秒**：WSL RViz/Gazebo 显示 ROS2 topic、TF、`/cmd_vel_safe`、任务状态和电池/健康状态。
6. **40 秒**：发送“巡视后院并检查可疑物品”，展示任务状态机：导航→视觉跟踪→检查→返航；明确是 mock 还是真实模型。
7. **30 秒**：展示自动生成的回归报告：10 seed、碰撞、A*、定位误差、房间覆盖、帧延迟和资源占用。
8. **20 秒**：主动说明限制：PuppyPi 真 SDK 和四足动力学尚待实机适配，当前成果是可复现安全导航与跨平台仿真闭环。

专业感来自“能解释每个指标和失败边界”，而不是把所有模块都描述成已经完成。

### 10.3 展示物料

- 一张总架构图：WSL/ROS2、C++ nav_core、UE5、PuppyPi adapter、MiniCPM。
- 一张数据流图：传感器→定位→规划→安全→执行。
- 一张状态机图：IDLE、PATROL、TRACKING、RETURN、CHARGING、EMERGENCY、FAULT。
- 一段 1080p UE5 视频和一段 WSL RViz 录屏。
- 一份 10 seed 回归表、一份消融表、一份 failure case 表。
- 一页“接口与安全边界”说明：速度上限、命令超时、急停、断链、人工接管。

---

## 11. 六周执行排期

### 第 1 周：冻结基线与修正事实

- 打 `baseline-20260905` 标签，生成环境和配置哈希。
- 重新跑 Python、scene、config、C++ 300/3600 帧检查。
- 解释 skipped 测试，修复 footprint 闭合警告。
- 统一当前 v6.0 场景的房间/目标/行人/阈值描述，归档旧版矛盾文档。
- 建立 `release`、`research`、`demo` profile。

**出口**：任何结论可追溯到当前 commit 和 artifacts。

### 第 2 周：统一配置、接口和启动入口

- 解决 URDF/contract/scene 的尺寸漂移。
- 统一 `cmd_vel`、电机使能、Battery/Fall/Security/Health 语义接口。
- 收敛 mission manager，给旧脚本加 research-only 标记。
- 修复 launch 的绝对路径和 readiness 等待。
- 固化 WSL Humble 的同步、构建、topic/TF 诊断脚本。

**出口**：mock + core、Gazebo + Nav2 两个入口能在干净工作区启动。

### 第 3 周：PuppyPi adapter 和安全闭环

- real backend fail-fast，mock/sim/real 状态机分离。
- 完成速度限幅、加速度限制、命令超时、断链急停和故障注入。
- 测量 forward/strafe/yaw 轴响应，锁定仿真 profile 的可用控制能力。
- 增加安全事件、低电量和任务取消/返航测试。

**出口**：任何陈旧命令、断链或安全事件都能证明机器人停止。

### 第 4 周：UE5 bridge 真闭环

- 按第 5 节重新验证握手、首帧、坐标、AMCL、A*、目标切换、碰撞和路径距离。
- 完成 UE/bridge 单位换算单测和 1800/9000 帧 smoke。
- 修复所有“画面移动但 bridge 指标不动”的账目矛盾。
- 加入轨迹、LiDAR、路径、行人、安全状态 HUD。

**出口**：UE5 至少完成一圈当前 4 房间巡逻，指标能写入 JSON。

### 第 5 周：多种子、消融与论文图表

- C++ 36000 帧×10 seed；通过后再做 108000 帧×10 seed。
- UE 做 3~10 seed 等价性回归，记录允许的几何/时序差异。
- 完成 AMCL、A* 膨胀、动态避障、安全层、mock/真实视觉后端消融。
- 自动生成表格、轨迹图、误差图、延迟图和失败案例。

**出口**：论文核心结果不是手工抄日志，而是由脚本生成。

### 第 6 周：答辩交付和演示固化

- 录制 UE5、WSL RViz、故障安全和自然语言任务视频。
- 准备 3~5 分钟主演示和 10 分钟技术答辩版本。
- 完成论文图表、接口附录、复现说明、限制与展望。
- 在展示电脑上执行一次从零启动彩排；保存离线视频作为 GPU/网络故障备份。

**出口**：即使现场没有 GPU、UE 或 ROS2，仍能用视频和结构化报告完整证明成果。

---

## 12. 最终验收门槛

| 类别 | 指标 | 毕业设计发布标准 |
|---|---|---:|
| 工程 | Python 单测 | 关键测试 100% 通过，skipped 有书面原因 |
| 工程 | C++ 单测/构建 | `ctest`、sim、bridge、geometry_check 可复现构建 |
| 配置 | 单一真源 | scene/contract/config/URDF/UE 校验无未解释警告 |
| 安全 | 静态/动态碰撞 | 正式回归 10 seed 全 0 |
| 安全 | 墙穿透 | 全 0 |
| 规划 | A* 成功率 | 均值 ≥95%，最差 ≥90% |
| 定位 | 平均误差 | <0.20m |
| 定位 | 峰值误差 | <0.75m，异常可定位 |
| 运动真实性 | 运动距离 | 1 小时 ≥10m，非“静止换安全” |
| 运动真实性 | stuck ratio | <20% |
| 任务 | 当前 v6.0 房间覆盖 | ≥4 个房间，至少 1 个完整巡逻圈 |
| UE bridge | AMCL 更新覆盖率 | ≥99% 帧 |
| UE bridge | 协议帧完成率 | 100%，无未解释丢帧/乱序 |
| UE bridge | 断链停车 | 300ms 内进入安全停止 |
| UE | 展示帧率 | Standalone ≥30 FPS |
| WSL | 传感器/TF | `/scan`、`/odom`、`/tf` 频率和时间戳达标 |
| 视觉 | mock 安全链路 | 丢帧、遮挡、低置信度、LiDAR 近障全部安全停车 |
| 文档 | 复现说明 | Windows、WSL、UE5、仿真和报告命令完整 |

---

## 13. 风险登记与降级策略

| 风险 | 影响 | 预防/降级 |
|---|---|---|
| WSL/Gazebo 图形或依赖不稳定 | 现场无法启动 | 保留 headless 日志和录屏；先保证 C++/UE 展示 |
| UE5 4GB 显存或 shader 编译过慢 | 画面卡顿 | 关闭 Lumen/Nanite、降低分辨率、使用简化材质和预编译包 |
| bridge 坐标/握手再次回归 | UE 指标失真 | 每次运行先做 handshake、首帧、坐标和 AMCL 覆盖率门禁 |
| 行人模型导致偶发碰撞 | 论文结果不稳定 | 多 seed、事件回放、动态障碍单独消融，不隐藏碰撞 |
| PuppyPi SDK 缺失 | 真机无法演示 | 展示 adapter/故障注入/接口设计，明确 real backend 未验收 |
| MiniCPM 模型无法加载 | 视觉演示失败 | mock 模式作为主演示，真实模型作为可选加分项 |
| 多套脚本同时发速度 | 运动抢占和不可复现 | 单一 safety/mission 仲裁层，旧脚本 research-only |
| 历史文档与当前场景矛盾 | 答辩被质疑 | 发布前生成“文档一致性检查”，旧报告加归档标记 |
| 测试通过但机器人不动 | 虚假安全结论 | 强制距离、活跃速度、房间、目标完成和 stuck 指标 |

---

## 14. 现在就执行的十项任务

- [x] 创建并保存 `baseline-20260905` 环境/配置/测试 artifacts（已完成：`artifacts/baseline-20260905/`，8 门禁全 0）。
- [x] 把 `scene_home.json` v6.0 定为当前毕业设计场景，旧 7/8 房间结果全部标注历史（已完成：单一真源锁定 v6.0）。
- [x] 修复 contract footprint 闭合警告和 URDF/scene/contract 尺寸不一致（已完成：闭合多边形，0 告警）。
- [x] 重新跑 C++ 300、3600、36000 帧，确认完整验收逻辑而不是只看退出码（已完成：POSIX/Linux C++17 编译修复，16 门禁全绿）。
- [x] 重新跑 bridge `--check-scene`、`--dry-run`、1800 帧 standalone 和 UE smoke（已完成：POSIX socket、select 修复，二进制自动适配）。
- [x] 对 UE 首帧位姿、单位换算、AMCL 更新、目标完成、碰撞回传做自动化门禁（已完成：`verify_bridge_loopback.py` / `verify_bridge_handshake.py` 100% 通过）。
- [x] 让 WSL Humble 的 mock + core、Gazebo + Nav2 启动链路在干净 shell 中可复现（已完成：`mock_system.launch.py`、`scripts/reproduce_wsl.sh` 一键复现）。
- [x] 对 PuppyPi real backend 禁止静默 mock 回退，补齐命令超时和断链停车证据（已完成：预检 fail-fast、82/82 单元测试全绿）。
- [x] 生成第一版毕业设计回归报告、消融表和失败案例表（已完成：`artifacts/experiments-20260907/`、`artifacts/release-20260907/`）。
- [ ] 录制“正常巡逻—动态避障—安全停车—自然语言任务—报告”完整视频，并准备离线备份（展示脚本与运行入口已固化，待 GUI 录制）。

---

## 15. 交付定义

本计划完成后，项目交付物应至少包括：

```text
E:\puppyfangzhen\
├── config/scene_home.json                  # 场景单一真源
├── config/simulation_contract.yaml         # 机器人/安全/验收契约
├── cpp_src/sim/                            # C++ 精度基准和批量回归
├── cpp_src/bridge/                         # UE5 bridge + protocol v2
├── src/                                    # ROS2 Humble 工作区源码
├── src/puppypi_adapter/                    # mock/sim/real adapter 边界
├── src/puppy_minicpm_robot/                # 视觉任务扩展
├── scripts/                                # 同步、构建、验证、报告脚本
├── artifacts/baseline-20260905/            # 不可覆盖的发布基线
├── artifacts/<release_run_id>/             # 当前验收结果
├── docs/ue_equivalence_report.md           # UE 等价性报告
├── INTERFACES.md                           # ROS2 接口契约
├── README.md                               # 从零复现入口
└── jihua20260905.md                        # 本计划
```

最终答辩应能够用一句话说明成果边界：

> 本设计完成了 PuppyPi 面向室内动态环境的安全自主导航仿真闭环，C++ 仿真提供固定种子和长稳基准，WSL/ROS2 验证机器人软件接口，UE5 提供三维数字孪生和可解释可视化；真实 PuppyPi SDK 和完整四足动力学作为后续实机适配工作，已通过清晰接口和安全边界预留。

这条表述既能展示算法、C++、ROS2、UE5、测试和工程管理能力，也不会把尚未完成的真机或真实大模型能力包装成已验收功能。
